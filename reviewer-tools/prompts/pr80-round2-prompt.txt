You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 2 of PR #80 (branch codex/r2-memory-nightly-backup, nightly verified backup to R2, migration 0031 — unapplied, so edit it in place) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #80 max review at cb8941d: changes requested"); reviewer-tools/pr80-adversarial.md and the executable test reviewer-tools/pr80/agent/adversarial-pr80.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/backup/memory-backup.ts, migration 0031, job-table.ts backup wiring, cron-router.ts, the backup tests; and, to classify tables, the CREATE TABLE statements across apps/cloud-gateway/src/persistence/migrations (read-only except 0031).

Fix per the entry: B1 integer (or fixed-width zero-padded) keys everywhere a numeric cursor is compared, with tests crossing 9->10, 99->100 and 9,999->10,000 at the default page size; B2 back up every authoritative Jarvis data table (memory items/versions/sources/runs/prices/receipts, ledger tables, conversation turns and events not covered by sealed archive segments, school, university, study coach, deadlines, principals/identities/devices metadata that restores need, owner controls), excluding only derived tables (FTS5, history chunks, current-state projections, Vectorize) — plus a test that enumerates every table created by migrations 0001-0031 and fails if a table is in neither the backup list nor an explicit excluded-derived list, and a restore test that imports a verified set into a fresh D1 with all migrations and foreign keys on and compares row counts and sampled hashes; S1 multiple pages per invocation within CPU/subrequest/D1 budgets, retry transient errors, alert when the newest verified set is older than 36 hours; S2 mark verified in D1 before writing latest.json and never delete the set latest.json names; N1 cut each table on an insertion-ordered key (rowid or sequence) and compare row counts to the cut; N2 cleanup/retention errors after a verified publish are logged and retried, not reported as backup failure; N3 the 0031 object-receipt insert guard rejects any existing object key (INSERT OR REPLACE cannot erase another run's receipt), with whole-trigger removal tests for changed triggers; N4 named tests for the backup job wiring; N5 delete objects before the manifest before the D1 status change, and add a KNOWN_ISSUES note that R2 bucket locks are an owner setting not yet enabled. Copy the reviewer's adversarial-pr80.test.ts into apps/cloud-gateway/test/backup/ temporarily: its 7 failing assertions (ADV1, ADV1b, ADV2, ADV4, ADV5, ADV3b, ADV6) describe correct behaviour and must PASS after your fix; delete the copy before committing (keep your own equivalent named tests).

Merge origin/main first (keep every AGENT_LOG entry). Remote-D1-safe triggers only (SELECT RAISE ... WHERE; a CASE used as a value expression is fine). Do not touch voice/**, calls/**, the Telegram reply path. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review with the final table classification, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
