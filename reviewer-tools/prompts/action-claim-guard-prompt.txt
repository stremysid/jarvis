You are a Jarvis build chat (repo ksid1229-ops/jarvis) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch codex/telegram-action-claim-guard from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Owner requirement (Sid): Jarvis must never tell him it did something it did not do. Today Jarvis has no tool that emails, texts, calls, books, pays, submits, registers or contacts anyone, yet model free text can still say so. Claude's PR #64 review (docs/AGENT_LOG.md on main, "PR #64 ... cleared", F1) measured that on turns that save nothing, 115 of 152 false external-action claims still reach Sid through the school reply guard, while only some turns are guarded at all. Sid also wants, later, an instant "on it" and a follow-up "booked at X" once real tools exist, so the rule must be receipt-based rather than a ban on the words.

Read only: apps/cloud-gateway/src/school/school-catchup-model.ts (guardReplyClaims, FIRST_PERSON_ACTION_CLAIM, FALSE_EXTERNAL_COMPLETIONS, PASSIVE_*, PLAN_SAVE_COMPLETIONS, presentsUnsavedSchedule, safeOrdinaryReply), the university tracker and study coach model adapters' reply guards, apps/cloud-gateway/src/index.ts replyTo (the Telegram model composition), memory/telegram-memory-controls.ts receipts, and their tests. The measurement corpora are on branch claude/reviewer-tools: reviewer-tools/pr64c/agent/b1r3.mjs (false claims, grouped), b2r3.mjs (benign honest replies), replyfp.mjs (true in-app actions) — read them with git show origin/claude/reviewer-tools:<path> and copy their strings into a committed test fixture.

Build:
1. One structural action-claim check (a pure module, e.g. src/channels/reply-action-claims.ts) applied to every owner Telegram reply at the outermost model composition in replyTo, after the school, university, study-coach and memory adapters, so ordinary chat is covered too. Keep the adapters' existing guards; the new check is the final gate.
2. Rule: a reply may state or promise an external action (email, text, message, call, book, reserve, pay, buy, submit, upload, register, apply, contact, notify, share with someone, accept or decline an offer, schedule with a third party) only if a code-issued action receipt for that action exists in this turn. No such receipts exist yet, so all are refused. Cover completed, passive, present-progressive, imminent-future ("I'll email Ms. Lee now", "Booking it now") and bare-completion ("Booked!", "Done, sent.") forms. In-app actions Jarvis really performs (saving school notes or plans, remembering or forgetting) are allowed only when that turn's code-issued receipt shows them; drafts Sid sends himself stay allowed.
3. Replacement: remove only the offending sentences and append one honest line (for example "I can't send emails or make bookings yet; I can draft it for you."), keeping the rest of a helpful reply. If nothing useful remains, send the honest line alone.
4. Measure: the b1r3 claims must all be caught (list any you leave with a reason); the b2r3 benign set must be no worse than main (2 of 41 over-refused); replyfp true in-app statements must show when their receipt exists. Add Sid-style cases: "Booked a table at Pai for 7pm.", "Your reservation is confirmed.", "Message sent to Mom.", "I'll call the dentist now.", "On it, booking now.", plus honest ones: "I can't book tables yet, but here's Pai's number.", "Want me to draft a text to Mom?".
5. Same PR, separate commit, PR #75 follow-ups from Claude's #75 entry: named tests pinning the three replay branches (conversation-repository getOrCreateTurn/stageAssistantDelivery replay reads and recordDeliverySuccess `returned ?? readDeliveryRow`) with a D1 proxy that throws after a real batch commit or races a write first, a composition test that stagingMs/telegramSendMs reach telegram_turn_outcome, and rename the misleading `d1` failure reason to `conversation`.

No migration. Never call real DeepSeek or Telegram. Do not touch voice/**, calls/**, memory/** except reading receipts, or backup/**. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push, open a draft PR against main titled "Telegram: never claim actions Jarvis didn't take", post a ready entry at the top of docs/AGENT_LOG.md for Claude max review with the corpus numbers before and after, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
