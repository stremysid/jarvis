You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 2 of PR #76 (branch codex/r2-memory-production-wiring, automatic memory and history indexing with a monthly cap) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #76 max review at 446ea2d: changes requested"); reviewer-tools/pr76-adversarial.md and the executable test reviewer-tools/pr76/agent/zz-pr76-adversarial.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); and the source and test files those name (mainly apps/cloud-gateway/src/memory/automatic-distillation.ts, memory-extraction-budget.ts, jobs/job-table.ts, index.ts scheduled composition, providers/deepseek-provider.ts, migration 0016 time checks read-only).

Fix per the entry: B1 never re-query DeepSeek on continuation (commit the remaining proposals from the already-paid response), dedupe already-saved facts by source event ids plus normalised text, and send temperature 0; S1 use a live clock for run/ledger rows and wall-clock budgets in the scheduled job; S2 validate proposals individually, keep valid ones, record rejected ones with a fixed code and advance past the window so a single bad proposal never re-bills the batch; S3 bound every cap lookup to the current America/Toronto month with an index-using predicate (no migration) and a query-plan test; S4 on DeepSeek 402 (or a low credit balance where the API reports it) send one owner notice per day through the existing monthly-notice path; N1 empty MEMORY_EXTRACTION_MODEL is unset and composition never throws out of scheduled(); N2 charge history indexing the statements actually used; N3 record the settled cost on a failed-after-payment run; N4 named tests for the JSON provider timeout, month-start bound, price-id check and production 80% notice wiring; N5 one exact extraction JSON schema with an example in both prompts; N6 keep the conservative reservation but settle at DeepSeek's actual time-of-day rate. Copy the reviewer's zz-pr76-adversarial.test.ts into apps/cloud-gateway/test/ temporarily: its defect assertions pass at 446ea2d and each must FAIL after your fix; delete the copy before committing.

Merge origin/main first (it now includes PR #73 migration 0030 and PR #64 migration 0029; keep every AGENT_LOG entry). No migration. Never call the real DeepSeek API in tests. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
