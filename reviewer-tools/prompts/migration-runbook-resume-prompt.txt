You are resuming a docs-only Jarvis task (repo ksid1229-ops/jarvis) that was interrupted when Windows refused to start a process (0xC0000142) while four Codex sessions ran at once. Your earlier work is preserved and uncommitted.

The worktree is C:\Users\Sid\OneDrive\Documents\ChatGPT\jarvis-migration-scratch-proof on branch codex/migration-scratch-proof-runbook at 282f066. Work only in that worktree. It already holds an untracked docs/runbooks/migration-scratch-proof.md and a modified NEXT_STEPS.md. Read what is there first and continue it rather than starting over.

The task, unchanged: the runbook documents the owner-attended scratch remote-D1 proof that NEXT_STEPS.md and docs/HANDOFF.md both require before any production migration apply, and which currently has no written procedure. Nine reviewed migrations are merged and unapplied: 0016_cloud_memory, 0017_owner_passphrase, 0018_owner_call_step_up, 0019_memory_ingress, 0020_school_catchup, 0021_voice_owner_delivery, 0022_university_tracker, 0023_study_coach and 0025_archive_literal_history. Written for Windows 11 and PowerShell 7 only, from the repository root, using pnpm.cmd and wrangler the way docs/runbooks/deploy.md and docs/runbooks/owner-passphrase.md already do — never bash, chmod or systemd. Every command block names the shell and starts with the cd. It must cover: what the proof does and does not prove; creating and naming a throwaway D1 and confirming out loud it is not production, with the name always taken from a prompt or variable; applying the nine in order with expected output; the checks worth running on scratch (every named trigger exists; INSERT OR REPLACE and INSERT OR IGNORE against each new unique key abort rather than delete; a CASE-wrapped RAISE would have been rejected) with copy-pasteable statements; cleanup and what to record; the production apply pointing at deploy.md rather than repeating it, stating plainly that `wrangler d1 migrations apply` applies EVERY pending migration, not a chosen subset, so the owner runs `migrations list` first and reconciles the count against the nine; and stop conditions, including that earlier migrations stay applied when a later one fails and that the owner never deploys after an unresolved migration failure. Do not invent a wrangler flag you have not seen in the existing runbooks.

Finish it: review the draft, add the NEXT_STEPS pointer if it is not already there, run lint and typecheck, commit, push, open a draft PR, and post a ready entry in docs/AGENT_LOG.md for Claude review. Standing rules: never merge, deploy, apply a migration, create or delete a real database, touch secrets, spend or contact anyone. Remove your extra worktree only after pushing.
