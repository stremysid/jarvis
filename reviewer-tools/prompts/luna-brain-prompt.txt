You are a Jarvis build chat (repo ksid1229-ops/jarvis) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch codex/brain-luna-instructions from origin/main (which must already contain the merged agent-tools PR), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Owner decisions (Sid, 2026-09-17): the live brain for Telegram chat and phone calls becomes OpenAI GPT-5.6 Luna (fast non-reasoning mode for ordinary turns, low reasoning effort for turns that use tools); DeepSeek V4.1 Flash stays on background memory extraction and consolidation. The brain must stay a swappable setting. Sid dislikes sycophantic AI and wants Jarvis to push back; Luna guesses more than peers, so standing instructions must counter that.

Read only: providers/deepseek-provider.ts and provider-types.ts (OpenAI-compatible chat completions, tool calling added by the agent-tools PR), the agent adapter and index.ts replyTo composition, voice/production-runtime.ts model wiring (read; change only the model selection seam), env.ts, memory-extraction-budget.ts (spend cap pattern), and the related tests.

Build:
1. An OpenAI provider for the brain (Chat Completions or Responses API — pick the one that supports tool calling, prompt caching and reasoning effort for gpt-5.6-luna; verify model id and parameters against current OpenAI docs), behind the same provider interface: streaming, tool calls, timeouts, failure reasons, usage accounting including cached tokens. Secret OPENAI_API_KEY; setting BRAIN_MODEL (e.g. openai:gpt-5.6-luna, deepseek:deepseek-flash). If the key is absent, fall back to DeepSeek with a named log code. Voice calls use the same BRAIN_MODEL in fast mode.
2. A monthly spend cap for the brain (setting, default USD 15) with the owner warning at 80% and a clear owner notice plus DeepSeek fallback when reached; per-turn usage logged as integers.
3. Standing instructions: a D1-stored owner note "How Sid wants Jarvis to act", seeded with exactly these rules: (1) Disagree directly whenever Sid is wrong or a plan is weak; argue on the merits, don't soften or defer; settle it before acting. (2) No flattery, praise or filler. (3) Don't guess about Sid's life: if memory doesn't show it, say "I don't know"; label inferences as guesses. (4) Multi-step tasks: go ahead alone, but check in at real decision points — a plan change, money, sending as Sid, anything hard to undo. (5) "Check or audit this" means thorough and critical, worst problems first. (6) Short replies by default; long content as a document. (7) Tell Sid when a longer task finishes or needs his input. (8) Never touch the St. Remy codebase. The note is placed in the stable prompt prefix (tools, system rules, instructions, then profile) so provider prompt caching hits, and Sid can change it in plain speech through an agent tool (update_instructions) with a receipt and full version history; only Sid's direct messages may change it.
4. An evaluation runner (script, not run by you against real APIs) that replays a corpus through both brains and reports tool-choice accuracy, honesty (claimedActions vs receipts), pushback on wrong claims, "I don't know" on unknown facts, flattery, latency and cost. Include at least 30 sycophancy/guessing cases in addition to the agent-tools corpus.

Tests (fake providers only): provider request/response shapes against documented OpenAI formats (tool calls, streaming, cached token usage, errors, timeouts), fallback when key absent or cap reached, cap warning once, instructions seeded and placed in the stable prefix, plain-speech instruction update with receipt and permission refusal for non-owner or forwarded text, voice model selection. No migration unless genuinely needed (then additive, remote-D1-safe triggers only, with whole-trigger removal tests). Never call the real OpenAI, DeepSeek or Telegram APIs. Do not touch calls/**, backup/**, school/**, university/**. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push, open a draft PR against main titled "Brain: GPT-5.6 Luna with Sid's standing instructions", post a ready entry at the top of docs/AGENT_LOG.md for Claude max review including Sid's one-time secret commands, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
