You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) in a fresh session, doing the required follow-ups to merged PR #85 (Telegram memory retrieval latency). Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-retrieval-fu) on a new branch codex/telegram-retrieval-followups from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's entry "PR #85 max review at 5b05f62: cleared with required follow-ups" in docs/AGENT_LOG.md on main; reviewer-tools/pr85-adversarial.md and the executable test reviewer-tools/pr85/agent/adversarial-pr85.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/memory/telegram-memory-retriever.ts, memory-repository.ts (batched item reader), literal-history.ts, archive/archival-service.ts (circuit), conversation/context-retriever.ts, and the related tests.

Fix per the entry: F1 literal-history failure (including an open archive circuit) must never remove canonical memories — do not start literal search when its result cannot be used, or treat its failure as no hits, with a named fixed log code; F2 validate batched candidate items concurrently preserving order, cache receipt checks per (eventId, sequence), read each archived segment at most once per retrieval, and pin the archived-source case at <= 500 ms with 25 ms per round trip; F3 exclude the current turn's own event from recentContextCoversQuery and the recent-event/excerpt de-duplication so literal history actually reaches real Telegram turns, with an end-to-end test through the production Telegram service (a statement pushed out of the recent window is recalled by a later question); F4 run memory work under its own 800 ms deadline independent of the base lookup and merge/de-duplicate after both settle, so a slow base (inside its 2,500 ms deadline) never discards ready memory; F5 replace wall-clock 250/350 ms assertions with round-trip counts plus a generous time bound, add a differential test of the batched reader against readCurrentItem + readItemVisibility, kill tests for the literal suppressed-row skip, archived event-id check, per-row seal check, batched creation_event_suppressed and suppressed-source list, and the recentEventIds de-duplication, and a race test inserting a suppression between the FTS query and the candidate batch; F6 allow 128 combined topic-walk steps or document the cap in a named test.

Copy the reviewer's adversarial-pr85.test.ts into apps/cloud-gateway/test/memory/ temporarily (run from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/memory/adversarial-pr85.test.ts`): A1, B1, B2 and B3 must PASS after your fix and B0, C1, C2 stay passing; delete the copy before committing (keep your own equivalent named tests). No migration. Never call real services. Do not touch voice/** (D1ContextRetriever output must stay identical), calls/**, school/**, university/**, backup/**, meaning-search.ts. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push, open a draft PR against main titled "Telegram: memory recall follow-ups (history recall, archive-safe, slow-base-safe)", post a ready entry at the top of docs/AGENT_LOG.md for Claude max review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
