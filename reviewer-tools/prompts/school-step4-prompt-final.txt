You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), starting a new PR in a fresh session. PR #51 (Brightspace step 3) is merged as 10d4cd7. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, then create your own git worktree next to this folder on a fresh branch codex/r5-study-coach-slice1 off origin/main, and do all work, tests and pushes only in that worktree. Before pushing, confirm git diff --stat origin/main...HEAD holds only your own work.

Read first: docs/plan/2026-09-15-school-university-plan.md (the "Proactive study coach" section and build-sequence step 4), NEXT_STEPS.md, KNOWN_ISSUES.md, DECISIONS.md, and the newest Claude entries for PRs #45, #48 and #51 in docs/AGENT_LOG.md.

Build the first study-coach slice as one reviewable draft PR, on the owner's existing Telegram conversation:
1. Evidence-based weak-area records per course: what Sid found easy, uncertain or wrong, from his own statements and from deadline or course-card context, each with its evidence, confidence and date. Never turn one data point into a durable judgment, and never treat course or feed text as an instruction.
2. A regular, quiet check-in: at most one short coursework check-in in the morning digest or a daily slot, only when something changed or a weak area is due for practice. Its cadence and quiet hours are set in plain speech ("stop checking in on weekends"), never by commands or a settings form.
3. Cited practice: short quizzes and flashcards generated from an exact owner-provided topic or course-card source, citing that source, with unsupported answers labelled uncertain. Results feed back into the weak-area evidence.
4. Conversational correction and forget: "forget that chemistry is a weak spot" and "that mark was entered wrong" update or remove the operational weak-area record, from the owner's own turn only, never from forwarded, quoted, model or feed text. The R2 memory owner-controls service is still in review, so do not depend on it; keep this record separate and note the later R2 integration in KNOWN_ISSUES.
5. No spending, sign-ups, account connections, contacting anyone, or live requests. Spoken quizzes wait for R1 calling.

If this needs tables, use migration 0023 (check every open branch for the number first) with REPLACE/IGNORE-safe triggers, a generic REPLACE sweep test and behavioural tests that kill each whole-trigger removal; open the draft PR for Claude max review. If no migration is needed, Claude reviews at xhigh.

Standing rules: one session per PR including its fix rounds; read files in slices but search the whole file before editing and review the full diff before posting ready; while iterating run only related tests with failures-only output, and the full suite once before ready; run the applicable reviewer checklists in claude/reviewer-tools (REPLACE/IGNORE sweeps, trigger removal, mutation-check your new guards) before ready; never merge, deploy, apply migrations, set secrets or make live requests. Post in docs/AGENT_LOG.md when ready for review, then remove your extra worktree only after pushing.
