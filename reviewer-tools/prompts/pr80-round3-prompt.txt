You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 3 of PR #80 (branch codex/r2-memory-nightly-backup, nightly verified backup to R2, migration 0031 — still unapplied, so edit it in place) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #80 max re-review at 07dd1f5: changes requested"); reviewer-tools/pr80r2-narrow.md and the executable test reviewer-tools/pr80r2/adversarial-pr80r2.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/backup/memory-backup.ts, migration 0031, the backup tests and restore test, job-table.ts backup wiring, and — to write the restore rebuild — the migrations that define the derived tables (memory_item_state, memory_item_placement_state, memory_cursors, the FTS tables, memory_topics apply triggers) and the jobs that maintain history chunks, coverage and vectors (read-only except 0031).

Fix per the entry. B1: exclude memory_backup_runs/objects/alerts from the backup as operational records of R2 itself; back up only the last 48 hours of scheduled_runs; test that N unchanged nights export the same row count. B2: a real restore path in code plus docs/runbooks (not only a test): apply all migrations to a fresh D1, drop every trigger, insert the verified set with foreign keys deferred, recreate the triggers from the migration SQL, then rebuild every excluded derived table (item state and placement state from transitions/placement events, FTS via rebuild, history chunks/coverage/vectors by their existing jobs, and every memory_cursors row derived from its authoritative receipts so a restore never re-pays distillation for events that already have receipts); the restore test fixture must put at least one post-initial-state row in every table that has an insert guard (finished memory run, cost entry older than 5 minutes, topic event, non-pending guest notice, non-staged verifier, finished search/reprocess job, school evidence, …) and compare row counts, sampled hashes and rebuilt state with the source. S1: accept exported <= expected per table and record the shortfall in the manifest instead of failing the run; exclude request_nonces, sync_snapshots, identity_challenges and authentication_attempt_reservations; document in the restore runbook that a set is not one exact point in time. N1: remove the eventsThrough branch of memory_backup_objects_insert_guard with a whole-trigger/branch removal test. N2: the classification test enumerates tables from the migration files themselves, and loadDescriptors fails closed with an owner alert when the live schema has an unclassified table. N3: page WITHOUT ROWID tables by ordinal range using stored key columns (no whole-table scan per page), with a query-plan or row-read test. N4: the restore rebuild derives cursors and marks from restored rows.

Copy the reviewer's adversarial-pr80r2.test.ts into apps/cloud-gateway/test/backup/ temporarily (run it from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/backup/adversarial-pr80r2.test.ts`; if the Workers pool times out starting, your worktree path is too long — use a short path like C:\Users\Sid\jarvis-pr80r3): its 5 failing assertions (GROWTH, DELETE, GUARD, RESTORE-A, RESTORE-B) must PASS after your fix and its 4 passing ones must stay passing; if you change the restore procedure the RESTORE tests call, adapt only their call site, never their expectations, and say so in the ready entry. Delete the copy before committing (keep your own equivalent named tests). Merge origin/main first (keep every AGENT_LOG entry). Remote-D1-safe triggers only (SELECT RAISE ... WHERE; a CASE used as a value expression is fine). Do not touch voice/**, calls/**, the Telegram reply path, school/** or university/**. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review with the final table classification and the restore runbook path, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
