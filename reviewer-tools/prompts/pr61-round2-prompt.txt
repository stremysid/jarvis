You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), doing round 2 of PR #61 (branch codex/r5-school-grades-step5, verified grades and derived missing-work checks, migration 0027) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: the newest Claude entry for PR #61 at the top of docs/AGENT_LOG.md on that branch; reviewer-tools/pr61-adversarial.md and reviewer-tools/pr61/zz-reviewer-pr61-probes.test.ts on branch claude/reviewer-tools (read with git show origin/claude/reviewer-tools:<path>); and the files those name.

Claude's max review at c696e45: changes requested, 2 High, 3 Medium, 3 Low. The one line this PR exists to get right can be false. H1: a handed-in assignment is still reported "no submission seen" and stamped with the newest scan, because the digest query never re-checks the basis observation's current submission_state and last_success_at is set before derivation finishes. H2: an observation read as NEW before the deadline produces "no submission seen" once the scan completes after the deadline; derive that state only from a read at or after due_at, in the repository AND the 0027 transitions guard, and otherwise derive nothing. M1: a replayed derivation after a lost write plus a due-date change can never complete (UNIQUE derived_at collision) and nothing ages the derivation phase out; make replay idempotent per deadline and give derivation the same age limit and visible reset as the scan. M2: render the new lines in DIGEST_TIMEZONE like the rest of the digest, not raw UTC. M3: add load-bearing tests for derivation paging/resume across more than 64 deadlines with a digest read in between, advancing to the next course, the stale and never-scanned gap lines, and a due-date extension after derivation. L1–L3: fix or record each in KNOWN_ISSUES.md with the reason.

Copy the reviewer's probe file into apps/cloud-gateway/test/school/ temporarily: all four probes (P1–P4) PASS at c696e45 and each must FAIL after your fix. Delete the copy before committing; do not commit it. Also run the whole-trigger removal for every 0027 trigger you change and require a named test to fail for each.

Engineering constraints the reviewer enforces: every trigger in remote-D1 form, WHEN ... BEGIN SELECT RAISE(ABORT, ...) END, never the SELECT CASE ... RAISE(...) statement form (a plain CASE ... END value expression is fine); every unique key keeps an insert guard; every repeated step keeps a declared D1 statement budget and a reachable failure state. Nothing may invent a date, mark, weight or course. Do not change voice/**, calls/**, D1ContextRetriever or voice/production-runtime.ts. A lone owner-passphrase-routes failure is a known word-collision test defect being fixed separately.

Merge origin/main first; if docs/AGENT_LOG.md conflicts, keep every entry from both sides, newest first. Run lint, typecheck and the full suite once (rerun only unrelated files that time out under load). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop — do not wait for or poll for a review. Standing rules: never merge, deploy, apply migrations, touch secrets, request OAuth consent, spend, sign up, submit or contact anyone. Remove your extra worktree only after pushing.
