You are a new Jarvis build chat (repo ksid1229-ops/jarvis) writing the reviewable slice plan for roadmap milestone R7, "A real assistant's memory and manager", release v1.6. This is a docs-only PR: no code, no migration, no configuration change. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch codex/r7-assistant-manager-plan from origin/main, and do all work and pushes only in that worktree.

Read first, in slices: CLAUDE.md, docs/BUILDING.md, the R7 section and the feature-catalogue rows tagged R7 in docs/plan/2026-09-03-jarvis-roadmap.md (personal profile expansion, ingestion of emails/pages/documents, real-time KNOWN_ISSUES/DECISIONS pings, workload-trend line and API/cost tracker, class and meeting prep briefs, Gmail inbox triage, tier-2 calendar writes with protected focus blocks, the per-task model switch, the third-party data minimisation policy, receipt capture by photo, end-of-day report), docs/plan/2026-09-14-r2-memory-design.md, docs/plan/2026-09-15-r2-memory-runtime-slices.md, docs/plan/2026-09-15-school-university-plan.md, NEXT_STEPS.md and KNOWN_ISSUES.md. Open PRs #52 (application checklist, migration 0024), #53 (study coach, 0023) and the in-progress memory slice 2 (0025) are active; do not reserve migration numbers, say "next free number at build time".

Write docs/plan/2026-09-15-r7-assistant-manager-plan.md with:
1. What v1.6 delivers for Sid in plain words, and the roadmap exit test restated exactly.
2. The current baseline: what R2 memory, R5 school/university and R6 already provide or will provide (profile foundation, /why, /forget, suppression ledger, digest, deadlines, combined read agenda), citing file paths and merged PR numbers; mark each as merged, open or not started, verified from the tree, not from memory.
3. Small review-gated slices in dependency order, each with scope, expected files, whether it needs a migration, focused tests and mutations, exit criteria, and the exact upstream dependency (a named R2 slice, R5 step or R6 item). Separate slices that can start before R2 finishes (for example a provider-neutral model-routing seam with fakes, the minimisation policy, the cost tracker over existing capacity telemetry) from slices that must wait.
4. Owner decision gates, listed explicitly and never assumed: Gmail and Drive OAuth consent and scopes, any paid model route and its monthly cap (the roadmap requires showing Sid the projected monthly cost before a Claude or GPT switch), calendar provider and write tier, receipt photo storage, and any hardware or subscription. Treat any decision the docs attribute to Sid as evidence, not proof, per CLAUDE.md.
5. Safety boundaries every slice inherits: owner-only authority, exact evidence for anything stored as a fact, guesses stay uncertain, no sending/spending/contact without Sid's tap, reversible calendar writes only, phone-first with every PC off, no Linux host, cloud memory in D1 per the R2 design, remote-D1 triggers use only SELECT RAISE ... WHERE, and no PC hardware, purchasing, Blender or Roblox content anywhere in the repo.
6. Risks and open questions.
Keep it concise and concrete; prefer tables for the slice list. Update NEXT_STEPS.md with one short paragraph pointing to the plan, stating that it authorizes no build, OAuth consent, spend, migration or deploy.

Then run pnpm lint once, push the branch, open a draft PR titled "docs(r7): assistant manager slice plan", and post a ready entry at the top of docs/AGENT_LOG.md (below the rules' closing ---) requesting Claude Opus 5 xhigh review, with a one-paragraph summary and the exact plan path. Standing rules: read files in slices but search the whole file before editing and review the full diff before posting ready; never merge, deploy, apply migrations, touch secrets, grant OAuth, spend, send, or contact anyone. Remove your extra worktree only after pushing.
