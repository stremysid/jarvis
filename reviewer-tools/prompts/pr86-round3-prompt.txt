You are a Jarvis build chat (repo ksid1229-ops/jarvis) doing round 3 of PR #86 (branch codex/telegram-agent-tools, "one AI decides and uses tools instead of code routers") in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-pr86r3) for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #86 max re-review at 3c25b32: changes requested"); reviewer-tools/pr86r2-narrow.md and the executable test reviewer-tools/pr86r2/adversarial-pr86r2.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); src/channels/telegram/owner-telegram-agent.ts, telegram-types.ts, telegram-reply-markup.ts, index.ts, memory/memory-owner-controls.ts, memory-repository.ts, school/school-catchup-model.ts (guardReplyClaims, safeOrdinaryReply), study-coach-model.ts, the university tracker adapter, providers/deepseek-provider.ts, conversation-repository.ts decision/markup code, and the related tests.

Fix per the entry: B1–B4 and N1–N7 exactly as written there (Done only after a completed receipt; no-signal means not_saved and every non-save pipeline path yields not_saved; remember grounding with word boundaries, content words, negation parity and an allowlist, falling back to an uncertain model-inferred memory with the exact excerpt instead of stated; a webhook-anchored deadline with time reserved for staging and send and the signal passed to Brightspace refresh; sentence-level guard removal with receipted internal verbs allowed; draft/sample span and owner-report exemptions; UTF-16 truncation; swipe-reply target id and offer-shaped confirmed questions; control intent for single forget/restore/explain; normalised remember dedupe appending sources; named tests for every listed mutation survivor and step-based assertions for the two flaky wall-clock tests).

Copy the reviewer's adversarial-pr86r2.test.ts into apps/cloud-gateway/test/channels/ temporarily (run from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/channels/adversarial-pr86r2.test.ts`): its 13 failing assertions must PASS and G3, G4, M2, M3, A2 and F6 stay passing; delete the copy before committing (keep your own equivalent named tests). Merge origin/main first (keep every AGENT_LOG entry). No migration unless genuinely needed (then additive, remote-D1-safe, with whole-trigger removal tests). Never call real DeepSeek or Telegram. Do not touch voice/**, calls/**, backup/**, meaning-search.ts, telegram-memory-retriever.ts (another builder is changing retrieval). Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
