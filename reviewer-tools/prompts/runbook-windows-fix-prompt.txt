You are a Jarvis build chat (repo ksid1229-ops/jarvis) fixing the scratch-rehearsal runbook in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch codex/fix-scratch-runbook-windows off origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on main ("remote-D1 scratch rehearsal run for real … the runbook needs six fixes"), docs/runbooks/migration-scratch-proof.md, scripts/prepare-d1-scratch-baseline.mjs and its test scripts/test/prepare-d1-scratch-baseline.test.mjs, and the first 60 lines of docs/runbooks/deploy.md for the established Wrangler invocation pattern.

Fix all six defects Claude hit on real remote D1 from Windows 11 / PowerShell 7: (1) step 2's database id regex must accept Wrangler 4's JSON output ("database_id": "…") as well as TOML; (2)+(3) the baseline script must spawn process.execPath with the resolved node_modules/wrangler/bin/wrangler.js (no pnpm.cmd, no shell) and pass SQL as --command=<sql>, because a statement starting with a -- comment is otherwise parsed as flags; (4)(5)(6) every runbook command must use deploy.md's pattern — $wrangler = (Resolve-Path 'node_modules/wrangler/bin/wrangler.js').Path and & node $wrangler … — never pnpm.cmd exec wrangler, so empty --env '' arguments, multi-line SQL and SQL containing double quotes reach Wrangler intact. Keep every existing check, confirmation and stop condition. Refactor minimally so the script's argument construction is exported and add tests pinning that the executable is process.execPath, the wrangler path ends in wrangler/bin/wrangler.js, and the SQL is passed as a single --command= argument (including a statement beginning with a -- comment and one containing double quotes). Keep the gap-reporting behaviour.

Do not create, delete or touch any real D1 database and do not run wrangler against a remote database. Run node --test scripts/test, lint and typecheck. Open a draft PR and post a short ready entry at the top of docs/AGENT_LOG.md for Claude review, then stop — do not wait for or poll for a review. Standing rules: never merge, deploy, apply migrations, create or delete a database, touch secrets, spend or contact anyone. Remove your extra worktree only after pushing.
