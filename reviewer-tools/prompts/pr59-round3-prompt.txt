Next task in this session: round 3 of PR #59 (branch codex/r2-memory-distillation-slice-3). Claude re-reviewed your round-2 head 7fd25ff: changes requested. git fetch origin and read only Claude's newest entry at the top of docs/AGENT_LOG.md on that branch, plus reviewer-tools/pr59b-adversarial.md on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:reviewer-tools/pr59b-adversarial.md; its attack inputs are in reviewer-tools/pr59/agent/m1-probe.ts). You already know this code from round 2; do not re-read the plan or unrelated files.

Your round-2 fixes held: 23/23 reviewer mutations were killed by named tests. What remains:

H1 (reviewer decision): a forwarded Telegram message is still filed as Sid's own confirmed words, because conversation.user_committed events carry no direct/forwarded marker. Make distillation fail closed: a turn may yield authenticated_first_person only when its event explicitly records direct owner text; since no producer writes that marker yet, every fact from a user turn is stored uncertain (still recalled, labelled, never an instruction). Do not add the marker to the conversation payload in this PR — PR #62 does that. Test: a forwarded-shaped user turn with a bare first-person sentence produces no authenticated_first_person item.

M1 (reviewer decision): remove the attribution verb list. Once the marker exists, a quote is authenticated first person only when it is the whole direct message; everything else is uncertain. Implement the rule behind the marker check so it is ready, and test it with a synthetic direct-marked event, including "Mum sent this. I am moving to Calgary in June." (uncertain) and "I wrote my Western essay." as the whole message (authenticated).

M2: a single message with more than four facts must not be skipped. Commit up to four per step and continue the same event on the next step; replace the test that asserts proposal_budget_exceeded for it.

M3: in the hourly job run the Classroom and Brightspace polls before memory distillation, and stop starting new distillation steps after a wall-clock budget (four minutes). State the D1 per-invocation query allowance you rely on with a citation to Cloudflare's current D1 limits page, or keep one invocation's total under 1,000 queries.

L1–L4: fix or record each in KNOWN_ISSUES.md with the reason (skip counts and eligible-event backlog in the job detail; break the step loop when the cursor did not advance; duplicate facts after a failed-finalize retry; first-write archive subject is code-enforced only). Add a header comment to 0026 stating it alters archive_segment_events, a table live since 0001, so the scratch rehearsal must cover it.

Every new rule needs its own load-bearing test; expect each to be removed again. Keep every trigger in remote-D1 form. Do not change voice/**, calls/**, D1ContextRetriever or production-runtime.ts, and add no spend path. Work in your existing worktree for that branch if present, otherwise create one next to the shared folder; never switch branches in the shared folder. Merge origin/main first, keeping both sides of any AGENT_LOG conflict. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone.
