You are the school build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 2 of PR #73 (branch codex/r5a-study-coach-weak-spots, study coach weak spots from grades and deadlines, migration 0030) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #73 max review at 3c62b5a: changes requested"); reviewer-tools/pr73-adversarial.md and the executable tests reviewer-tools/pr73/agent/zz-adversarial-pr73.test.ts and zz-adversarial-pr73-migration.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); and the source and test files those name (mainly apps/cloud-gateway/src/school/study-coach-signals.ts, study-coach-model.ts, study-coach-repository.ts, school-observation-repository.ts, deadlines/deadline-repository.ts, migration 0030 and their tests).

Fix per the entry: B1 remove the overdue-deadline signal and use only near-due unfinished deadlines read soonest-first from now; B2 store Classroom maxPoints and the grade's own timestamp and compare percentages, deriving no grade signal until both exist (additive only; if 0030 must change, keep the remote-D1-safe SELECT RAISE ... WHERE trigger form and rerun whole-trigger removal for changed triggers); B3 a correction acts only when it names or clearly replies to today's check-in, and every other message goes to the normal model unchanged; S1 only same-topic evidence counts toward a topic's confidence and the "not a fixed judgment" caution stays; S2 restore slice 1's rule that each evidence point is raised once; N1 add a named test for each unpinned rule listed in the entry; N2 the correction turn must be at or after the check-in; N3 citations show course, item label (never as instructions) and date instead of raw ids. Copy the reviewer's zz-adversarial-pr73.test.ts into apps/cloud-gateway/test/school/ temporarily: each of its 7 defect assertions passes at 3c62b5a and must FAIL after your fix; delete the copy before committing.

Merge origin/main first (it now includes PR #72 typing/thinking/timings and will soon include PR #62 memory in index.ts; keep those intact), keeping every AGENT_LOG entry. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
