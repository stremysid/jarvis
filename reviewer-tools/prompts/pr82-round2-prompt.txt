You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 2 of PR #82 (branch codex/r2-memory-topic-filing, automatic topic filing) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #82 max review at 0394455: changes requested"); reviewer-tools/pr82-adversarial.md and the executable test reviewer-tools/pr82/agent/adversarial-pr82.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/memory/automatic-distillation.ts, memory-repository.ts, memory-types.ts, providers/provider-types.ts, jobs/job-table.ts, and the related tests.

Fix per the entry. B1: a bad or missing topic path or filingConfidence never discards the memory; validate them separately from the fact, file such items to Inbox with decision inbox_invalid_path and a reason without the path, treat missing/invalid filingConfidence as 0, and allow those two keys to be absent. B2: validatedTopicPath applies exactly the topicComponent rules (controls, U+2028/2029, byte limit consistent with the schema) and building the placement reason can never throw (fallback reason without path), so no model-chosen name can fail a step. S1: the Inbox re-file filters decisions in SQL and cannot be starved by unmovable rows (bounded larger candidate read, stop after 10 moves, or rotation), with a test for ten older unmovable items. N1: drop a leading component equal to the root name, never create or file into areas under the Inbox, reject '>' '/' and Unicode Cf characters in names, and match siblings with NFKC case folding (existing topics keep working; no migration — if stored normalized names make NFKC matching impossible without one, stop and report it in the ready entry). N2: create missing areas in the same D1 batch as the item, or document why not in KNOWN_ISSUES. N3: charge a measured per-filing D1 figure with a maximum-step test whose events are actually filed, and count the re-file tail against the allowance. N4: named tests pinning re-file skipping proposed/uncertain and low-confidence items, exact-name-only matching, both Inbox-target checks, the conditional child-cap insert, the path byte bound, topic-path redaction, the filingConfidence range and the memory_corrupt rethrow.

Copy the reviewer's adversarial-pr82.test.ts into apps/cloud-gateway/test/memory/ temporarily (run it with `npx.cmd vitest --config ../../vitest.workspace.ts run apps/cloud-gateway/test/memory/adversarial-pr82.test.ts` from apps/cloud-gateway): its 13 failing assertions describe correct behaviour and must PASS after your fix, and its 7 passing ones must stay passing; delete the copy before committing (keep your own equivalent named tests). Merge origin/main first (keep every AGENT_LOG entry). No migration. Never call the real DeepSeek API. Do not touch voice/**, calls/**, school/**, university/**. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
