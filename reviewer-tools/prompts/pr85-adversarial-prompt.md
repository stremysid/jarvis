You are an adversarial second reviewer for the Jarvis project (repo ksid1229-ops/jarvis; owner "Sid"). Find real defects in PR #85 before it merges. Treat the builder's claims as unproven.

PR #85 (branch `codex/telegram-memory-retrieval-latency`, head `5b05f62`, base `origin/main` `1e9e5ca`) is a production latency hotfix. On production (2026-09-17 01:05–01:07 UTC) Telegram memory retrieval hit its 800 ms bound (`telegram_memory_retrieval_memory_timeout {baseMs: 450–484, memoryMs: 800}`), so Jarvis told Sid it didn't know his favourite subject although D1 held it. Root causes: `retrieveMemory` awaited the base recent-context promise before starting literal history search; candidate items were re-read with many single-statement round trips. The PR starts literal search in parallel, batches D1 reads in `telegram-memory-retriever.ts`, `memory-repository.ts` (+440 lines of batched readers), `literal-history.ts` and `conversation/context-retriever.ts`, and adds sub-timing telemetry. Builder claims: round trips 2/13 → 1/4, 43 ms base and 78 ms total retrieval under its latency harness, and a 700 ms slow base no longer causes a memory timeout.

WHAT MATTERS MOST
1. Forgetting and suppression must be exactly as strict as before. The batched readers re-implement item visibility, suppression anti-joins, uncertain/proposed eligibility, lifecycle states, source visibility and literal-history suppression checks. Find any case where a forgotten, suppressed, superseded, expired, other-principal or not-yet-eligible item, history chunk or recent turn now reaches the model, or where an eligible one is now dropped. Diff the old per-item logic (`readCurrentItem`, `readItemVisibility`, `searchLiteral`, `withoutForgottenTurns`) against the new batched SQL line by line and prove differences with tests.
2. Recent-context de-duplication after moving literal search in parallel: the same event must not appear twice (once as recent turn, once as history evidence); `recentContextCoversQuery` semantics preserved; byte budgets and ordering unchanged.
3. `D1ContextRetriever` is shared with voice: outputs must be identical for voice inputs; voice tests unchanged.
4. Real latency: measure round trips and wall time with a counting D1 proxy that adds 20–25 ms per round trip on a production-shaped fixture (about 185 events, recent turns, 2 proposed uncertain items, 2 active items, forgotten items, suppressions, 8 literal-history hits, archived segments). Does total context retrieval stay well under 800 ms? Does any path still issue unbounded or per-item sequential statements (e.g. per archived hit)? Is the telemetry accurate?
5. D1 limits and correctness of batched statements: IN-list/parameter limits (D1 allows at most 100 bound parameters per statement), JSON arrays passed as parameters, statement-count budgets vs the declared ceilings, behaviour with many candidates.
6. Scope: no migration; voice/**, calls/**, school/**, university/**, backup/** unchanged; identify guards whose deletion no named test catches.

WHERE / HOW
- Work from your session's repository worktree. The diff is saved at `<scratchpad>/pr85/source.diff`. Read files with `git show 5b05f62:<path>`; base with `git show origin/main:<path>`. Builder's claims: top PR #85 entry of `docs/AGENT_LOG.md` on the branch.
- To execute, create a short-path worktree: `git worktree add C:\Users\Sid\jarvis-pr85-adv 5b05f62`, `pnpm.cmd install --frozen-lockfile`, put tests in `apps/cloud-gateway/test/memory/adversarial-pr85.test.ts`, run from that worktree root: `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/memory/adversarial-pr85.test.ts`. Tests assert CORRECT behaviour (a failing test = a proven defect); where useful run the same test with main's `src` to show a regression. Do NOT use `C:\Users\Sid\jarvis-pr39` or `C:\Users\Sid\jarvis-pr40`. Do not push, merge, deploy or call real services. Leave your worktree in place.

OUTPUT: write `<scratchpad>/pr85-adversarial.md` and return it. One-line verdict and counts; findings High → Low, each with **Where** (file:line at the head), **Proven** (test name and result, or "by reading"), **Effect for Sid** in plain words, **Fix**; then short "checked and sound" and "unverified" lists. No padding; an unsubstantiated Medium is worse than none.

<scratchpad> is C:\Users\Sid\AppData\Local\Temp\claude\C--javis--claude-worktrees-jarvis-code-review-0b1695\f3f2b419-52e6-4c42-b275-f2c7a15d27ac\scratchpad
