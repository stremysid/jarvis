You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch codex/r2-memory-meaning-search from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Owner requirement (Sid): Jarvis must recall what he said even when he asks in different words ("what's my favourite subject?" must find "I really like math"), from the phone with every PC off, without making replies slow. Today (main) recall is keyword-only: FTS5 over memory items and history chunks (literal-history.ts, telegram-memory-retriever.ts). The design already reserves meaning search: Workers AI @cf/baai/bge-m3 embeddings in a Vectorize index, with the D1 ledger table memory_vectors and cursor name 'embeddings' (migration 0016, applied in production).

Read only: docs/plan/2026-09-14-r2-memory-design.md sections 3.1, 3.4, 5.2, 5.3, 8 (forget/lift effects on indexes), 9 step 8 and 11; migration 0016 definitions of memory_vectors, memory_history_chunks, memory_history_coverage, memory_cursors, memory_item_state and memory_active_event_suppressions (read-only); apps/cloud-gateway/src/memory/literal-history.ts (history chunk indexing job and its limits), telegram-memory-retriever.ts, memory-repository.ts, automatic-distillation.ts (where new versions land), memory-owner-controls.ts (forget/lift), apps/cloud-gateway/src/env.ts, src/scheduler/job-table.ts, src/index.ts (retriever composition), wrangler.toml, and the related tests.

Build:
1. Bindings: add `[ai] binding = "AI"` and a Vectorize binding MEMORY_VECTORS (index name jarvis-memory-bge-m3) to wrangler.toml, both optional in env.ts. With either binding absent, meaning search is disabled with a named log code and everything else works exactly as today. Put Sid's one-time commands in the operations runbook: `wrangler vectorize create jarvis-memory-bge-m3 --dimensions=1024 --metric=cosine`, plus create-metadata-index for every metadata field you filter on, run before the first deploy that carries the binding.
2. Indexer (hourly job step, after distillation and the FTS history step): embed active memory item current versions and memory_history_chunks that lack a memory_vectors row for their current content hash, in bounded batches (state the per-run caps and the Workers AI/Vectorize/D1 budgets they respect). Vector id and metadata carry principal, item kind, item id and content hash only; never put memory text in metadata. Record each upsert in memory_vectors exactly once (the UNIQUE key makes retries idempotent), advance nothing on failure, and use a retryable observation. Forgotten, superseded or expired items get a queued vector delete recorded with deleted_at; lift re-embeds if needed. Embedding failures never block distillation, FTS or replies.
3. Retrieval: in TelegramMemoryRetriever, run meaning search in parallel with keyword search inside the existing 800 ms memory bound, with its own hard sub-deadline (at most 450 ms, embedding plus query). Skip it entirely when the message has no content terms after the existing stopword filter (a "hi" costs zero extra calls). Fuse keyword and meaning candidates with reciprocal-rank fusion preserving source ids. Every meaning hit is re-read from canonical D1: items must be eligible in memory_item_state, and history chunks must pass the active-suppression anti-join before any text enters context, so a forgotten memory never returns even while its vector still exists. Timeout, missing binding or provider error falls back to keyword-only with a redacted fallback code; add meaningSearchMs and the fallback code to the telegram_turn_outcome log. Recent conversation context (the base retriever) is never delayed or dropped by this work.
4. Coverage: a read-only coverage count (eligible items and chunks versus live vector rows for the current model) exposed where the existing memory coverage/status lives, so a stale index is visible.
5. Do not change voice/**, calls/** (meaning search on calls is a later slice), school/**, university/**, or the backup code on branch codex/r2-memory-nightly-backup. No migration unless a genuine schema gap exists; if you must add one, stop and report the gap in the ready entry instead.

Tests (fakes for AI and Vectorize behind narrow interfaces; never call real Workers AI, Vectorize or DeepSeek): the paraphrase case (a "favourite subject" query finds the "I really like math" item through meaning only, where keyword search misses); fusion order; forgotten item and suppressed chunk never returned while the vector still exists; lift makes it eligible again; indexer idempotency across a retry and a duplicate run; batch caps; delete queue; binding-absent path; the 450 ms sub-deadline with injected latency (keyword results still return within the 800 ms bound, and base recent context is unaffected); "hi" makes zero embedding calls; the coverage count; job-table wiring named tests (removing the indexer step must fail a test). Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push, open a draft PR against main titled "R2: meaning search for memory recall", post a ready entry at the top of docs/AGENT_LOG.md for Claude max review listing Sid's one-time Vectorize commands, then stop. Standing rules: never merge, deploy, apply migrations, create cloud resources, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
