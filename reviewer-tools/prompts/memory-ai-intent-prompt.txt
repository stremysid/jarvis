You are a Jarvis build chat (repo ksid1229-ops/jarvis) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch codex/telegram-agent-tools from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Owner requirement (Sid, 2026-09-17, his words): "it's supposed to be like a human brain and infer, not be hardcoded"; "it's an AI just like you, you can infer even if I spell badly or am vague — Jarvis is literally just an AI like you"; "it shouldn't run through code, it should run through AI." Target architecture: Jarvis works like an AI coding assistant — ONE model reads the conversation, works out what Sid means (typos, slang, vague references, answers to its own questions), and calls tools. Code only executes tools with permission checks, issues receipts, keeps actions reversible and enforces budgets. No keyword list, regex parser or per-feature router decides what a message means.

Today (main) the owner Telegram path is a stack of code routers in index.ts replyTo: TelegramMemoryControlModelAdapter decides memory intent with parseTelegramMemoryControl phrase matching; StudyCoachModelAdapter, SchoolCatchupModelAdapter and the university tracker adapter each decide by their own rules whether a message is theirs before the model sees it. In production this failed Sid twice: "Remeber that my fav subject is math" was not treated as a remember request, and "Math" — his answer to Jarvis's own "Want me to note it?" — was saved as a school course instead of a memory.

Read only: index.ts replyTo composition; memory/telegram-memory-controls.ts, telegram-memory-language.ts, telegram-memory-reference.ts, memory-owner-controls.ts, memory-repository.ts (remember/forget/lift/explain/confirm and receipts), telegram-memory-retriever.ts (item ids in context); school/school-catchup-model.ts, study-coach-model.ts, university tracker model adapter (their validated save pipelines, receipts and reply guards); providers/deepseek-provider.ts and provider-types.ts (add OpenAI-compatible tool/function calling if absent); model/model-types.ts; docs/plan/2026-09-14-r2-memory-design.md sections 7–8; the related tests.

Build:
1. An owner Telegram agent adapter at the top of the composition: one non-thinking DeepSeek call with the conversation, retrieved context and tool definitions. If the model answers without tools, that is the reply (one model call, as today). If it calls tools, code validates and executes them and one follow-up model call writes the reply from the tool results.
2. Tools: memory_remember (fact + supporting excerpt from Sid's current message, or from Jarvis's immediately previous offer plus Sid's direct answer now), memory_forget (item ids from context or the last referenced item), memory_restore, memory_confirm (promote a proposed/uncertain item), memory_explain; school_update, university_update and study_coach, which hand Sid's current message and context to the EXISTING validated pipelines (their structured model calls, validation, receipts and reply guards stay) — the agent, not code, decides when to call them.
3. Permission checks in code, not weakened: a tool executes only when the turn is Sid's own direct, current Telegram text (directOwnerText true — not forwarded, quoted, captioned, edited, channel/group/bot, guest, retrieved or model-authored); ids must be Sid's and eligible; arguments must be grounded as above; forgetting more than one item or an ambiguous forget returns a one-tap confirm button instead of executing. Receipts keep the existing exactly-one-token contract; evidence class follows design section 7 (explicit owner request → stated; answer to Jarvis's offer → confirmed) with the exact excerpt stored. Tool JSON that is malformed, oversized, repeated beyond a small per-turn cap, or names unknown tools is refused without breaking the reply.
4. Remove the code routers as intent deciders: parseTelegramMemoryControl and the adapters' "is this message mine" rules no longer choose the path. Deterministic parsing may remain only inside permission/safety checks.
5. Keep ordinary replies fast: exactly one model call when no tool is used; state the added latency for tool turns.

Tests (fake provider tool calls only): Sid's two production failures now work ("Remeber that my fav subject is math" → stated memory; "Want me to note it?" then "Math" → confirmed memory and no school course); typo/slang/vague variants; school, university and study-coach turns still save through their pipelines with receipts; permission refusals for forwarded, quoted, captioned, edited, guest, group and model-authored text and for ids that are not Sid's or not eligible; multi-item forget needs the button; malformed/unknown/over-cap tool calls; ordinary turns make exactly one model call; existing memory, school, university and study-coach integration tests keep their outcomes (update only their routing setup, never their expected results, and list each change). Add an offline evaluation corpus of at least 80 realistic Sid messages (typos, slang, vague references, answers to Jarvis's questions, school, university, memory, chit-chat) with expected tool use, runnable later against the real model by the reviewer; do not run it against the real API yourself.

Merge origin/main first if it moved. No migration unless a genuine schema gap exists (then stop and report it). Never call the real DeepSeek or Telegram API. Do not touch voice/**, calls/**, backup/**, meaning-search.ts. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push, open a draft PR against main titled "Telegram: one AI decides and uses tools instead of code routers", post a ready entry at the top of docs/AGENT_LOG.md for Claude max review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
