You are the calling build chat for Jarvis (repo ksid1229-ops/jarvis), starting a new PR in a fresh session. PR #46 (owner passphrase PR 3) is merged as MERGE_SHA. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, then create your own git worktree next to this folder on a fresh branch codex/r1-step-up-release-evidence off origin/main, and do all work, tests and pushes only in that worktree. Before pushing, confirm git diff --stat origin/main...HEAD holds only your own work.

Read first: docs/runbooks/voice-smoke.md (especially the "Superseded owner contract" note), docs/superpowers/specs/2026-09-14-owner-call-passphrase-design.md, the R1 section of NEXT_STEPS.md, KNOWN_ISSUES.md, DECISIONS.md ("Owner calls require a spoken step-up"), scripts/voice-release-gate.mjs, and the newest Claude entries for PRs #28 and #46 in docs/AGENT_LOG.md.

Build one reviewable draft PR that moves the R1 live-evidence contract from the superseded PIN-free owner schema to the passphrase design, updating code, validators and the runbook together:
1. The release gate requires six retained live records instead of five, adding `owner-step-up-refused` (an owner-path call whose spoken step-up is refused, ending with the fixed refusal, end, rejection row and a single owner alert).
2. The evidence schema for owner scenarios records step-up outcomes (verified, refused, waived-passed-A only when that policy is on) instead of `owner_identity_pin_free`. Validators reject any record that claims owner authority without a verified or explicitly waived step-up, and reject missing or duplicate scenarios.
3. Update the scenario driver contract and the enrolled-operator evidence query adapter interfaces so the new scenario can be injected and verified. No real driver, no network request, no Twilio or provider call, and the default command stays non-live and skipped.
4. Rewrite docs/runbooks/voice-smoke.md to the passphrase contract and remove the superseded-contract warning only once code and validators match it.
5. Keep pnpm test:voice-smoke, pnpm test:voice-access and pnpm typecheck:voice-access green, and add contract tests that fail against the old five-record schema.

Run the reviewer's contract-gap and mutation checklists in claude/reviewer-tools for the new validators before ready. No migration is expected; if one is needed, use the next free number after checking every open branch (0023 study coach, 0024 application track and 0025 memory slice 2 are reserved). Claude reviews this at max because R1 is the v1.0 release gate.

Standing rules: one session per PR including its fix rounds; read files in slices but search the whole file before editing and review the full diff before posting ready; while iterating run only related tests with failures-only output, and the full suite once before ready; never merge, deploy, apply migrations, touch secrets, open inbound calling or place a live call. Post in docs/AGENT_LOG.md when ready for review, then remove your extra worktree only after pushing.
