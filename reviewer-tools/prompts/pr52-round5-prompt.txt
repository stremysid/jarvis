You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #52 (branch codex/r5-application-track-slice1, the university application checklist, migration 0024) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for codex/r5-application-track-slice1 (latest head, which includes Claude's round-4 entry), and do all work, tests and pushes only in that worktree.

To save tokens, read only: the newest Claude entry for PR #52 in docs/AGENT_LOG.md on that branch, the report reviewer-tools/pr52d-adversarial.md on branch claude/reviewer-tools, and the files those name. Do not re-read the roadmap, the plan or the earlier rounds.

Claude's round-4 max re-review at 5f150b1: changes requested, 2 High, 1 Medium, 4 Low. Both round-3 Highs are genuinely fixed and proven load-bearing by mutation, the gates are clean (3,591/3,591, 9/9 trigger removals killed, all four round-1 probes still fail), and M3, M4, L2, L3 and L4 from round 3 are confirmed closed. Two new defects block, and they share a root cause: the fix widened what counts as evidence for an item further than intended.

H1. In apps/cloud-gateway/src/university/university-tracker-model.ts:487-497, evidenceClauses now includes every clause containing "it" or "that", plus the whole message for some items, gated only by evidenceNamesOnlyItem — which asks whether the message names exactly one tracked item, not whether the claim is about that item. Two proven consequences, both end-to-end through the real repository and 0024:
(a) "I'm drafting the Western essay. The Common App is done and I submitted it." writes submitted_by_sid on the WESTERN ESSAY and drops it from the digest, because the Common App is not a tracked item so the message "names only" the Western essay. The same class was proven for retire, reactivate and un-submit.
(b) itemNameContainsConnector at :379-385 tests programAliases(program) as well as the label, so every item in a program called "Arts and Science" gets whole-message evidence even with an ordinary label. With the plain label "UofT transcript" in that program, "I submitted my scholarship form today. The UofT transcript is next." marks the transcript submitted, and "I have a dentist appointment on Feb 1, 2027. The Arts and Science essay is next." sets that date. The identical messages against "Western essay" are refused, which isolates the fallback as the cause.
This is round-2's H1b message-wide binding back in an everyday form: Sid writes two sentences about two different things and one of his application items silently flips state or takes an unrelated date, and a wrong submitted_by_sid needs explicit correction wording to undo.
Fix: resolve "it"/"that" only within the SAME SENTENCE as the naming clause — carry the last named item forward inside a sentence and reset at . ; ! ? and newline — not across the whole message. For (b), mask matched labels and program aliases before splitting on connectives, which is what the round-3 review suggested first, and drop the whole-message branch entirely. That also fixes M1 below. Do not solve this by adding more negation patterns; the failing clauses are grammatically clean and about something else.

H2. The ordinary-reply guard in apps/cloud-gateway/src/school/school-catchup-model.ts now misses 11 of 18 external-action claims it blocked at 12a7bbf, proven through the real adapter against both heads: "I've emailed your essay to Ms. Chen.", "I've requested your reference from Ms. Chen.", "I've requested your transcript from the school.", "I've messaged your reference request to Ms. Chen.", "I've requested the reference from your teacher.", "I put in your scholarship application.", "I've put in your essay.", "I put in the scholarship for you.", "I've applied on your behalf.", "I've applied you to Western.", "I've booked your guidance meeting." The cause is at :39 (applied|booked|put in dropped from the bare verb list), :43 (the third-party noun must now be the verb's immediate object), :44 (the loose form kept only for sent|forwarded|shared) and :46 (the three verbs re-added behind narrow object lists), plus the removal of the "for you" alternative. The natural shape of these claims is verb, then Sid's object, then the third party later in the sentence. Round 3 broke this guard toward over-refusal; round 4 broke it toward under-refusal, which is the dangerous direction — this guard's only job is to stop Jarvis claiming it contacted Sid's school, submitted something or spent money, none of which it can do.
Fix: keep the object-position idea but allow the third-party noun anywhere in the SAME CLAUSE after the verb (split on , ; . and scan the remainder), and restore applied/put in/booked as bare verbs, exempting the proven-benign objects ("applied your feedback", "put in a note", "booked nothing") with a negative lookahead rather than whitelisting the harmful objects. Add all 11 replies above as BLOCK rows beside the 16 round-3 PASS rows so neither direction can regress silently again.

M1. :496 hands the entire owner message to the NEGATION, CONDITIONAL_OR_QUESTION and RETRACTION checks at :500-526, so "I submitted my Arts and Science essay. What's next?" refuses while the identical text with "Western essay" accepts — proven as four minimal pairs. The label/alias masking in H1 removes this.

Two test gaps to close, both cases where the code is right but nothing pins it — the reviewer proved each by deleting the fix and watching all 135 model tests still pass:
- The H2 date-path fallback: conjunctionSnapshot holds a single item, so namedApplicationItems' kind-only branch resolves "Science essay" without the fallback. Give that test a two-essay snapshot so the fallback is actually exercised.
- M4's widened REPORTED_OWNER_SUBMISSION alternatives: add refusal tests for "Mom writes: ...", "Dad sent me this: ...", "Guidance forwarded this: ...", "Ms. Lee says ...", "Ms. Lee wrote that ...".

Lows, your call to fix or record with the reason: an active duplicate is silently dropped with no reply line (repository:566); a new-item update that dedupes onto a retired item reactivates it without the REACTIVATION gate running (repository:567-568), which would also accept submitted_by_sid on a retired item; 0024 still permits an unverified row to carry source_url/admission_cycle at INSERT because the new guard is UPDATE-only; and M2's punctuation folding is not mirrored in normalizedKey (repository:167-170), so one message can create both apostrophe spellings as two active rows.

Run lint, typecheck and the full suite once (if unrelated tests time out under load, rerun just those files). Merge origin/main first if it has moved, keeping every entry from both sides in any docs conflict. Push and post a ready entry in docs/AGENT_LOG.md for Claude max re-review. Standing rules: never merge, deploy, apply migrations, touch secrets, or change voice/** or calls/**. Remove your extra worktree only after pushing.
