You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #56 (branch codex/r2-archive-literal-history, R2 memory slice 2, migration 0025) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for codex/r2-archive-literal-history (latest head, which includes Claude's review entry), and do all work, tests and pushes only in that worktree.

To save tokens, read only: the newest Claude entry for PR #56 in docs/AGENT_LOG.md on that branch, the report reviewer-tools/pr56-adversarial.md on branch claude/reviewer-tools (probe scripts in reviewer-tools/pr56/), and the files those name. Do not re-read the plan or the other slices.

Claude's max review at 7c57d6c: changes requested, 1 High, 5 Medium, 9 Low. The schema is well built and the suppression storage boundary is proved; nothing is composed yet, so nothing can break an ordinary reply today. Fix these:

H1. KNOWN_ISSUES.md deletes the "Archive-history handoff" limit, but validateArchivedEventEvidence (memory-repository.ts:1734) refuses immediately when archivedEventReader is undefined, and the only non-test constructor, memory-owner-controls.ts:375 new MemoryRepository(database), supplies none. Both proving tests inject a reader. So forget and lift on a purged turn still fail, only with memory_refused instead of memory_corrupt. Either give MemoryOwnerControlsService a default repository built with an archivedEventReader over env.ARCHIVE, or restore the KNOWN_ISSUES entry narrowed to the truth. Pin it with a test that constructs MemoryOwnerControlsService with its default repository, archives and purges the source event, and asserts forget and lift succeed.

M1. The 0025 job update guard (lines 154-180) never bounds how far one statement may move the checkpoint, so a single UPDATE setting status succeeded, checkpoint 5, scanned 5, matched 0 is accepted on a job that read nothing, and readExhaustiveSearchResult then reports a confident "searched everything, found nothing". The existing migration test named "rejects completion without scanning the snapshot" only aborts because it leaves scanned at 0. Add a per-step ceiling (for example OR NEW.checkpoint_event_sequence - OLD.checkpoint_event_sequence > 8) and pin the forged UPDATE with scanned set to match.

M2. matched_event_count is never reconciled with memory_literal_search_hits, so matched 3 with 0 receipt rows is accepted and matchedEventCount is public on the result type. Add the reconciliation clause to the update guard (the service batches hit inserts before the job update, so a BEFORE UPDATE trigger sees the right count) and pin an update whose matched count exceeds the receipt rows.

M3. PR #50's N7 is still open and this slice widens it: packages/contracts/src/calls.ts:112 returns the text unredacted when the whole string is a canonical ULID, on every channel including voice, and historyEvent now indexes such a turn and returns it verbatim as a literal excerpt. envelope.test.ts:71-79 only pins the partial case. Make the passthrough opt-in at the structural call sites that need it, the way fieldMarker gates the field-marker branch, and pin that redacted("01abcde123456fghjkmnpqrstv") returns 01abcde[REDACTED_AUTH_DIGITS]fghjkmnpqrstv.

M4. Nothing ever writes status failed, deletes are forbidden and job_key is unique, so a step that throws before its batch (empty read, contiguity check or text-budget check) leaves the job running forever and the key unusable. Let the service record failed with a failure code on an unrecoverable step, and let createExhaustiveSearch mint a fresh job for a key whose previous job is terminal (that needs a generation or attempt column). Pin both halves.

M5. The archive branch of memory_literal_search_hits_insert_guard (0025:211-214) matches only event_sequence, event_id and content_hash, so a hit receipt for an archived event owned by a different principal is accepted, while the live branch rejects it. Carry the subject into the archive tier or require a matching memory_history_coverage row in the archive branch, and pin that the archived cross-principal insert aborts.

Low, fix or record in KNOWN_ISSUES with a reason, your call: L1 declare and count a statement budget for searchLiteral (about 62 at maxResults 8) the way the exhaustive path does; L2 pass cursor.updatedAt as the floor in indexSequences line 898 so a backwards clock cannot wedge indexing; L3 name a test for the index-versus-archival race; L4 record that forget cannot remove a hit receipt; L5 note in NEXT_STEPS that MAX_JOB_EVENTS 16 is unreachable (the real per-step max is 8) and that slices 3 and 4 need a driver; L6 memory_history_chunks has no delete guard; L7 comment the deliberate sourceLocation divergence between canonicalSources and inspectReplay; L8 restore the blank line before "## PR #46 notification delivery"; L9 job progress does not re-check the principal (defence in depth only). Also add a named test that makes the T-memory_history_chunks_insert_guard trigger removal fail by assertion rather than by two whole test files, and finish PR #50's F1 by pinning the operation = 'item.transition' clause or memory_valid_owner_commands membership.

Run lint, typecheck and the full suite once (if unrelated tests time out under load, rerun just those files; the owner-passphrase-routes failure under load is a known flake, not yours). Merge origin/main first if it has moved, keeping every entry from both sides in any docs conflict. Push and post a ready entry in docs/AGENT_LOG.md for Claude max re-review. Standing rules: never merge, deploy, apply migrations, touch secrets, or change voice/** or calls/**. Remove your extra worktree only after pushing.
