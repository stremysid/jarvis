You are a narrow second reviewer for the Jarvis project (repo ksid1229-ops/jarvis; owner "Sid"). PR #80 (nightly verified backup of Jarvis's D1 data to R2, migration 0031 unapplied and unchanged this round) was sent back at `01292f2`; round 4 is head `6693f21` (implementation `fcc2424`, after a main merge). The main reviewer is re-running gates and the round-3 narrow tests (R1–R7). Your job: verify the new resumable restore entry point, its safety refusals and the PowerShell runbook, and find regressions. Treat claims as unproven.

Read first: Claude's entry "PR #80 max re-review at 01292f2" and the builder's round-4 entry (with its PowerShell rehearsal commands) at the top of `docs/AGENT_LOG.md` on `origin/codex/r2-memory-nightly-backup`; `<scratchpad>/pr80r3-narrow.md`. The backup-only diff is at `<scratchpad>/pr80r4/source.diff` (backup source, restore, tests, `docs/runbooks/memory-backup-restore.md`, `scripts/check-memory-backup-restore-target.mjs`). Read with `git show 6693f21:<path>`.

CHECK ONLY THESE
1. Resumability: kill the restore at every phase boundary and mid-phase (after trigger drop, mid-row-insert, after trigger recreate, mid-cursor rebuild, mid-derived rebuild) and rerun: it must finish with the same result as an uninterrupted restore, never duplicate rows, never leave triggers missing after completion, and report progress truthfully. Prove each invocation stays under 1,000 D1 queries (count with a proxy) on a fixture of at least a few thousand rows.
2. Safety refusals before any DDL: non-empty authoritative tables, the production database id/name from wrangler.toml, a target name that doesn't match the separately typed name, a set whose databaseSchemaVersion is newer than the code, a partially restored target from a DIFFERENT set. Show whether any refusal happens after the first DROP TRIGGER.
3. Round-3 fixes hold on production-shaped data: ≥2 distillation runs, a failed last run (cursor = highest contiguous succeeded/nothing_new chain), changed seeded singleton rows, older-set migration bound, only distillation + fts_history cursors rebuilt.
4. The runbook and `check-memory-backup-restore-target.mjs` are executable by Sid on Windows PowerShell exactly as written: every command, quoting, `& node node_modules/wrangler/bin/wrangler.js`, the scratch-D1 rehearsal first, never `wrangler d1 export` against production, how the restore is actually invoked (is there a real runnable entry — a script or a Worker route — or still "an operator program"?), and whether any step could touch production without a separate explicit confirmation. Run the script's own tests and try it with production-looking inputs.
5. Identify guards whose deletion no named test catches (mutation) among the new refusal and resume checks.

HOW
- Do NOT use `C:\Users\Sid\jarvis-pr39` or `C:\Users\Sid\jarvis-pr40`. Reuse the short-path worktree `C:\Users\Sid\jarvis-pr80r2-adv`: `git -C C:\Users\Sid\jarvis-pr80r2-adv checkout --detach 6693f21` (keep its untracked tests), `pnpm.cmd install --frozen-lockfile`, add tests in `apps/cloud-gateway/test/backup/adversarial-pr80r4.test.ts`, run from that worktree root: `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/backup/adversarial-pr80r4.test.ts`. Tests assert CORRECT behaviour (a failing test = a proven defect). Do not push, merge, deploy, apply migrations, create cloud resources or call real services (no wrangler commands against any remote database). Leave the worktree in place.

OUTPUT: write `<scratchpad>/pr80r4-narrow.md` and return it. One-line verdict and counts; findings High → Low, each with **Where** (file:line at 6693f21), **Proven** (test name and result, or "by reading"), **Effect for Sid** in plain words, **Fix**; then short "checked and sound" and "unverified" lists. No padding; an unsubstantiated Medium is worse than none.

<scratchpad> is C:\Users\Sid\AppData\Local\Temp\claude\C--javis--claude-worktrees-jarvis-code-review-0b1695\f3f2b419-52e6-4c42-b275-f2c7a15d27ac\scratchpad
