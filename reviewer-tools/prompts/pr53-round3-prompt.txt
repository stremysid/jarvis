You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #53 (branch codex/r5-study-coach-slice1, the study coach) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for codex/r5-study-coach-slice1 (latest head, which includes Claude's round-2 entry), and do all work, tests and pushes only in that worktree.

To save tokens, read only: the newest Claude entry for PR #53 in docs/AGENT_LOG.md on that branch, the report reviewer-tools/pr53b-adversarial.md on branch claude/reviewer-tools (probes in reviewer-tools/pr53/round2/), and the files those name. Do not re-read whole plans or unrelated docs.

Claude's round-2 max re-review at 2d3bac6: changes requested (small). Every round-1 High is confirmed fixed; 3,357/3,357 tests pass, 17/17 trigger removals are killed, and all five reviewer probes now fail as required. Fix these four, all small:
S1: in claimDigestCheckIn the counts query's WHERE already filters outcome IN ('uncertain','wrong'), so COUNT(*) FILTER (WHERE outcome = 'easy') AS easy_count is structurally always 0 and a topic with 3 uncertain plus 5 easy points shows "high confidence" in the digest while summariseTopic says low. Delete that clause from the WHERE and let the FILTER clauses do the work; keep evidenceCount = weak_count; pin with a 3-uncertain/5-easy repository test asserting the check-in confidence equals summariseTopic's.
S2: plausiblyAnswersQuiz disqualifies any text containing is|was|feels|found|finished|got, so correct sentence answers ("Mitosis is cell division", "It was the Krebs cycle", "Water is the reactant", "The answer is 42") are refused, and the dismissal closes every open item silently. Treat is/was as disqualifying only alongside a request or acknowledgement shape (or drop them and rely on the existing question-mark, request-prefix and keyword tests, keeping the round-1 table refusing), and when the gate dismisses an open quiz prefix the fallback reply with the same notice the practice path uses ("I closed the previous quiz..."). Pin both with tests.
S3: the retirement statements are unconditional while the evidence insert is conditional on answerSupport === "supported", and recordOwnerObservation prepends retirement to an INSERT ... WHERE NOT EXISTS, so an owner-topic answer or a retried turn at 24 active points destroys the oldest point and inserts nothing; superseded is terminal. Only prepend retirementStatements when an insert will actually run. Pin with a test asserting 24 active points stay 24 after an owner-topic answer.
S4: syncCourseContext's active_course_count and outer LIMIT still charge course_context rows against the 24/96 budget, but 0023's cap trigger now exempts course_context, so a course with 24 owner points syncs 0 of 3 new weak_area facts and course-card weak areas stop reaching the coach permanently. Drop the course_context arm from both counts so the repository counts exactly what the trigger counts. Pin with a test asserting 3 course_context rows after sync in that fixture.
Low, fix if small otherwise record in KNOWN_ISSUES: N1 the 30-day retirement window is one-way and documented only in the mailbox (either retire only to make room at the cap, or record the window and its irreversibility); N2 phraseMatches admits a one-word topic found in an unrelated fact ("Friday is hard" against a "Lab report due Friday" fact) so require at least two tokens or match only weak_area facts; N3 guardSchoolReply catches its own BRIGHTSPACE_CHECK_REPLACEMENT output; and round-1 L6, forgetting needs exact wording, cannot be undone, and superseded is a second state forget cannot reach.

While iterating run only related tests with failures-only output; run whole-trigger removal only for triggers you changed; run the full suite once at the end (if unrelated tests time out, rerun just those files). Copy reviewer-tools/pr53/zz-reviewer-pr53-probes.test.ts locally and confirm all five still fail (do not commit it). Merge origin/main first if it has moved, keeping every entry from both sides in any docs conflict. Push and post a ready entry in docs/AGENT_LOG.md for Claude max re-review. Standing rules: triggers use only SELECT RAISE ... WHERE; never merge the PR, deploy, apply migrations, touch secrets, or contact anyone. Remove your extra worktree only after pushing.
