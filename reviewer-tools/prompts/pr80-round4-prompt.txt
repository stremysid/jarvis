You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 4 of PR #80 (branch codex/r2-memory-nightly-backup, nightly verified backup to R2, migration 0031 still unapplied) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-pr80r4) for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #80 max re-review at 01292f2: changes requested"); reviewer-tools/pr80r3-narrow.md and the executable test reviewer-tools/pr80r3/adversarial-pr80r3.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/backup/memory-backup-restore.ts, memory-backup.ts, docs/runbooks/memory-backup-restore.md, the backup tests, migration 0026 (distillation cursor guards, read-only), automatic-distillation.ts and literal-history.ts cursor writes (read-only), and the scripts/ folder for how operator scripts run on Windows.

Fix per the entry. B1: write rebuilt cursors while triggers are dropped (or with the cursor guards suspended), with a restore test covering at least two distillation runs. B2: derive the distillation cursor as the highest contiguous end_event_sequence of succeeded/nothing_new distillation runs chained from 1, with a test whose last run failed. S1: replace migration-seeded singleton rows (autonomy_mode, outbound_runtime_controls, capability_tiers and any other seeded table) with the backed-up values instead of requiring equality, tested with changed autonomy and outbound controls. S2: a resumable restore entry point bounded per invocation (well under 1,000 D1 queries), runnable from PowerShell against a named scratch D1 through `& node node_modules/wrangler/bin/wrangler.js`; it refuses any target whose authoritative tables are non-empty (apart from seeded rows) before its first DDL, requires the target database name typed separately, and refuses the production database id from wrangler.toml; a crash between trigger drop and recreate is recoverable by rerunning; rewrite docs/runbooks/memory-backup-restore.md as exact PowerShell steps (scratch rehearsal first, never `wrangler d1 export` against production, no R2 object versions). N1: apply and recreate only migrations up to the set's databaseSchemaVersion, enforced in the API. N2: rebuild only the cursors production maintains (distillation, fts_history) and compare cursor names with the source in the test.

Copy the reviewer's adversarial-pr80r3.test.ts into apps/cloud-gateway/test/backup/ temporarily (run from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/backup/adversarial-pr80r3.test.ts`): its failing assertions R1, R2a, R2, R3, R4 and R7 must PASS after your fix (R1b and R2b are diagnostics that bypass a guard; they must pass or be adapted only at the bypass call site, never their expectations, and say so), and R5 and R6 must stay passing; delete the copy before committing (keep your own equivalent named tests). Merge origin/main first (keep every AGENT_LOG entry). Remote-D1-safe triggers only. Do not touch voice/**, calls/**, the Telegram reply path, school/**, university/**, memory/** source. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review with the exact PowerShell restore rehearsal commands, then stop. Standing rules: never merge, deploy, apply migrations, create cloud resources, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
