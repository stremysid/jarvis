You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #52 (branch codex/r5-application-track-slice1, the university application checklist, migration 0024) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for codex/r5-application-track-slice1 (latest head, which includes Claude's round-3 entry), and do all work, tests and pushes only in that worktree.

To save tokens, read only: the newest Claude entry for PR #52 in docs/AGENT_LOG.md on that branch, the report reviewer-tools/pr52c-adversarial.md on branch claude/reviewer-tools, and the files those name. Do not re-read the plan, the roadmap or the earlier rounds.

Claude's round-3 max re-review at 12a7bbf: changes requested, 2 High, 4 Medium, 4 Low. Every round-2 finding is confirmed fixed in code, the gates are clean (3,408/3,408 tests, 9/9 trigger removals killed, all four round-1 probes still fail). Two new defects came in with the fix and both block:

H1. clauses() in apps/cloud-gateway/src/university/university-tracker-model.ts:150 splits sentences on [^.;!?\r\n]+, so every real URL is cut in half at its first dot before lines 323-326 require the source URL and the cycle phrase to sit inside one clause. Proven: "The Waterloo AIF is due Feb 15 2027 for the 2027 cycle, confirmed at https://uwaterloo.ca/future-students/admissions" splits into ["...confirmed at https://uwaterloo.", "ca/future-students/admissions"], so the verified due date is refused with university_application_model_date_invalid; the same message with a dotless host is accepted. The verified-deadline path is therefore dead for application items: every official Waterloo or OUAC deadline Sid pastes with its link is refused and the whole turn's university plan is discarded. The suite stays green because the only verified-application-date test (test/university/university-application-workflow-model.test.ts:700-715) asserts a rejection. Fix: mask https?://\S+ spans before the sentence match at :150 and restore them after, or keep the URL check whole-message the way verification() does at :213 and clause-bind only the date and the cycle phrase. Add a positive parser test asserting state "verified" survives for "Waterloo AIF due Feb 1, 2027 per https://uwaterloo.ca/aif for the 2027 cycle".

H2. clauses() at :151-153 also splits on \b(?:and|but|then)\b, and :437 then requires one clause to name the item. Proven: "I submitted my UofT Arts and Science essay" splits into ["I submitted my UofT Arts", "Science essay"], so nothing names the item. Against a snapshot holding UofT "Arts and Science" with item "Arts and Science essay", all six of submit, drafting, ready, retire, set date and create are refused, while "I submitted my Western essay." is accepted; "I finished the Western essay then submitted it" is refused too. "Arts and Science" (UofT), "Arts and Business" and "Computing and Financial Management" (Waterloo) are ordinary Ontario program names, so for any of them Sid can never update an item and gets only the generic save-failure line. Fix: mask every snapshot label and program alias in the message before splitting and unmask after, or fall back to the whole-message naming test when no clause names the item and only one snapshot item is named message-wide. Add a parser table over an "Arts and Science" program covering submit, drafting, ready, retire, date and create, plus "I finished the X then submitted it".

M1. The widened ordinary-reply guard in apps/cloud-gateway/src/school/school-catchup-model.ts now blocks 16 of 16 plausible benign replies, each proven with a minimal pair. THIRD_PARTY at :37 has no object requirement, so teacher|referee|counsellor|guidance|school|university|OUAC within 48 characters of a first-person verb refuses the reply ("I asked earlier which university you are aiming for." is blocked while "...which program you are aiming for." passes). The passive alternative at :47 matches any "is in" ("Your essay is in good shape." is blocked while "Your essay looks good." passes). The new verbs at :39 and :46 add "I've applied your feedback to the outline.", "I booked nothing; only you can book the interview.", "I've put in a note about the Waterloo deadline." and "Submitted. Is that what you meant?". Fix: require the third-party noun to be the verb's object rather than anywhere within 48 characters; drop the bare school|university nouns or require "your ..." plus a contact verb; restrict the passive alternative to "is (now )?in with <proper noun>"; drop applied|booked|put in unless followed by an external object; require ^Submitted[!.] to be followed by a claim rather than a question. Extend the guard it.each table with all 16 as PASS rows and keep the existing BLOCK controls.

M2. containsLabel at :138-143 only collapses whitespace, unlike mentions() at :133 which folds all non-alphanumerics. Proven: "Add the Queen’s Commerce reference" against label "Queen's Commerce reference" is refused, and so is the reverse, and "video-interview" against "video interview" is refused. Sid texts from an iPhone, which types the curly apostrophe, so creating any item named for Queen's, St. Michael's or Western's fails the whole turn. Fix: normalise apostrophes and dashes on both sides, or keep mentions() for containment and handle the emoji case from round-2 L3 with a separate rule. Test the four apostrophe permutations plus the hyphen case, keeping the "Waterloo AIF with a check mark" refusal as a control.

M3, M4 and the four Lows are your call to fix or record, with the reasons in the entry: the prompt budget still overflows when the message names several programs (10 programs by 6 items reaches 47,956 bytes; 12 by 6 falls back to ordinary chat); forwarded text such as "Mom says: I submitted the Western essay for you" still records submitted_by_sid; a duplicate item now fails the whole turn instead of being skipped; reactivate-before-retire plan order still aborts at the 32-item cap; 0024 still lets a row keep a stale source_url and admission_cycle after un-verifying; and KNOWN_ISSUES.md:5-7 and :31-34 are now inaccurate.

Run lint, typecheck and the full suite once (if unrelated tests time out under load, rerun just those files). Merge origin/main first if it has moved, keeping every entry from both sides in any docs conflict. Push and post a ready entry in docs/AGENT_LOG.md for Claude max re-review. Standing rules: never merge, deploy, apply migrations, touch secrets, or change voice/** or calls/**. Remove your extra worktree only after pushing.
