You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #53 (branch codex/r5-study-coach-slice1, the proactive study coach slice) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for codex/r5-study-coach-slice1 (latest head, which includes Claude's round-3 entry), and do all work, tests and pushes only in that worktree.

To save tokens, read only: the newest Claude entry for PR #53 in docs/AGENT_LOG.md on that branch, and the files it names. Do not re-read the plan, the roadmap or the earlier rounds.

Claude's round-3 max re-review at 29c141d: changes requested, one High. S1, S2, S3, S4, N1, N2 and N3 are all confirmed fixed in code. Gates are green: lint, typecheck, 3,365/3,365 tests with no timeouts, migration 0023 unchanged so the 17/17 trigger-removal result stands, and all five reviewer probes still fail as required. One defect came in with the S2 fix and blocks the merge:

H1. In apps/cloud-gateway/src/school/study-coach-model.ts around lines 526-529 the dismissal path yields { index: 0, CLOSED_QUIZ_FALLBACK_PREFIX } and then does yield* this.dependencies.fallbackModel.stream(input). The fallback adapter starts its own stream at index 0, so the turn emits two tokens both numbered 0. conversation-service.ts:911 feeds every token to StreamingOutputRedactor, and streaming-output-redactor.ts:189 requires strictly sequential indices from 0; on a mismatch terminate() at :321-324 throws stream_redaction_input_invalid. Proven by execution: pushing those two tokens through new StreamingOutputRedactor(new Redactor()) throws. Your own Q1 test proves the adapter emits exactly that pair, because it asserts the concatenated text while FakeModel yields index 0; the test collect() helper ignores token.index, which is why the suite stays green. The effect on the owner is that with a quiz open, any ordinary message now fails the whole turn instead of replying — every case your it.each(["ok", "check D2L now", "line one\nline two"]) test covers.

Fix it the way the catch-up adapter already does in fallbackWithSaveFailure (apps/cloud-gateway/src/school/school-catchup-model.ts:417-433): collect the fallback stream, prepend the notice, and yield one token at index 0. Do not emit a prefix and then delegate. Then add a test that asserts the token sequence rather than only the concatenated text — assert the indices are 0, 1, 2, ... (or that exactly one token is emitted), and ideally push the tokens through a StreamingOutputRedactor in the test so the real contract is exercised. Put that check in the shared study-coach test helper so a later prefix cannot reintroduce this.

Optional, not blocking: syncCourseContext in study-coach-repository.ts now builds one D1 statement per unsynced active weak_area fact with no explicit ceiling; it is bounded only by school_course_facts_active_cap_insert in migration 0020 (48 active facts per principal). Add one comment naming that bound so a later cap change does not quietly unbound the batch.

Run lint, typecheck and the full suite once (if unrelated tests time out under load, rerun just those files). origin/main is 4262024 and is already in the branch, so no merge is needed unless it has moved. Push and post a ready entry in docs/AGENT_LOG.md for Claude re-review. Standing rules: never merge, deploy, apply migrations, touch secrets, or change voice/** or calls/**. Remove your extra worktree only after pushing.
