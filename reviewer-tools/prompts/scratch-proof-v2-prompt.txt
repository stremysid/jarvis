You are a Jarvis build chat (repo ksid1229-ops/jarvis) starting a docs-and-tooling PR in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch off origin/main, and work only there. This is a single-session task: use ordinary file edits, do not use orchestration or task-delegation tooling, and do not spawn sub-tasks.

Read only: the newest Claude entry titled "the merged scratch-proof runbook cannot complete" in docs/AGENT_LOG.md (it is on branch codex/r2-memory-distillation-slice-3 if not yet on main), the current docs/runbooks/migration-scratch-proof.md, splitMigration in apps/cloud-gateway/test/persistence/migration.ts, and docs/runbooks/deploy.md's "R0 item 5" section. Do not read the roadmap or plans.

Background, all proven tonight with the owner at the keyboard against real throwaway D1 databases, then deleted:
- The merged runbook cannot complete. It rehearses from an EMPTY scratch database, and `wrangler d1 migrations apply --remote` fails immediately on 0001 with `incomplete input: SQLITE_ERROR [code: 7500]`, applying nothing. 0001, 0002 and 0006 use the old `SELECT CASE WHEN ... THEN RAISE(...) END;` statement form that remote D1 rejects; they were applied to production years-equivalent ago by some other route and are never re-applied in practice.
- A plain `CASE ... END` used as a value expression inside a trigger is FINE on remote D1; that was proven directly. So remote-d1-migration-syntax.test.ts is aimed correctly at `SELECT CASE ... RAISE(` and must NOT be widened. Add a short comment there explaining why plain CASE is deliberately allowed, so nobody over-tightens it later.
- Wrangler ignores the configured migrations_dir for a database its config does not declare: passing `--config apps/cloud-gateway/wrangler.toml` with a scratch database name fails with `No migrations present at <repo>/apps/cloud-gateway/migrations`. A scratch-only config file OUTSIDE the repository, declaring just the scratch database with migrations_dir pointing at src/persistence/migrations, works.
- Production is at 0015, so a production apply starts at 0016 and never re-executes 0001. None of the nine unapplied candidates (0016-0023 and 0025) uses the rejected form.

Rewrite docs/runbooks/migration-scratch-proof.md so it rehearses the real production path instead of an impossible one:
1. Bring the scratch database to a 0015-equivalent baseline the way production genuinely is. Apply 0001 through 0015 using the repository's own splitMigration, which lifts trigger bodies out before splitting on semicolons — that is exactly what wrangler's splitter fails to do. A small committed Node script under scripts/ is the right home for this; it reads each migration, splits it with the same function the tests use, and sends the statements with `wrangler d1 execute`. Then record the matching d1_migrations rows for the files actually applied. State explicitly in the runbook that this is not fabricating receipts: every receipt corresponds to SQL genuinely executed against that database, and the runbook must refuse to record a receipt for a file whose statements did not all succeed.
2. Then let `wrangler d1 migrations apply` run only 0016 onward, with the scratch-only config from the bullet above. That is the real production path and the only one worth rehearsing.
3. Now that a real baseline exists, add the production-shaped seeding that the current step 4 correctly says is missing: one human principal, one device, one active Telegram channel identity and one conversation event, all with obviously fake identifiers and no real phone number, token or account id, seeded BEFORE applying 0016 onward. The proof then covers the NOT NULL, existing-row-guard and unique-index classes that the current text lists as uncovered. Narrow the remaining-gap step to what is still genuinely uncovered, which is production's data volume and its real row contents.
4. Keep everything already good: Windows 11 and PowerShell 7 only, from the repository root, pnpm.cmd or npx.cmd, the double scratch-name confirmation, the case-sensitive comparisons, extracting trigger names from the files rather than a hand list, the exit-code check on every command, the stale-candidate-list and stale-trigger-count guidance, the account-database-limit note, the CASE/RAISE wording note, cleanup and deletion, and the final step pointing at deploy.md rather than duplicating the production procedure. Keep the rule that the returned database id never lands in a repository file — the scratch-only config lives outside the repo.
5. Every command block still names the shell and starts with the cd. Never bash, chmod or systemd.

Also correct the two places that now say something untrue: the opening step's claim about what the proof establishes, and anything implying an empty-database rehearsal is the procedure.

Add tests for the new script the same way the repo tests other tooling, at minimum that it splits a trigger-bearing migration into the same statements splitMigration produces and that it refuses to record a receipt when a statement fails. Run lint, typecheck and the full suite once. Open a draft PR and post a ready entry in docs/AGENT_LOG.md for Claude review. Standing rules: never run a migration against a real database, never create or delete a database, deploy, merge, touch secrets, spend or contact anyone — this PR writes the procedure, it does not perform it.
