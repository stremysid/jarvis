You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 5 of PR #80 (branch codex/r2-memory-nightly-backup, nightly verified backup to R2 with a resumable restore, migration 0031 still unapplied) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-pr80r5) for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #80 max re-review at 6693f21"); reviewer-tools/pr80r4-narrow.md and the executable test reviewer-tools/pr80r4/adversarial-pr80r4.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/backup/memory-backup.ts (MEMORY_BACKUP_TABLES order), memory-backup-restore.ts, memory-backup-restore-operator.ts, scripts/check-memory-backup-restore-target.mjs, docs/runbooks/memory-backup-restore.md, the backup tests, and the migrations defining foreign keys (read-only).

Fix per the entry: B1 no restore page may fail on a forward foreign key — order voice_access_grants (and its events) before call_sessions and derive/verify the whole table order from the migrated schema's foreign keys, insert self-referencing columns (memory_topics.redirect_to_topic_id, parent_topic_id and any other self-reference) as NULL and set them in a later resumable phase while triggers are still dropped (or order parents first), with a permanent P0-style test that fails on any forward foreign key and restore tests with a guest call and a merged topic each more than one page away from its target; N1 rebuild_cursors reports the count of existing rebuilt cursors (or batches insert with progress); N2 finalize is idempotent via a finalized marker and the runbook readiness loop stops retrying on an HTTP response and prints its error; N3 permanent tests for the reviewer's S1 cases b–f and a trigger-classification refusal; N4 parse the TOML in the target checker, refuse any preview_database_id and escaped production ids, and test the name-typed-twice, outside-repository and scratch-name refusals; N5 verify and hash the set once per restore and cache it in progress instead of re-downloading it on every /step, and state steps and time for a 5,000-row restore in the ready entry.

Copy the reviewer's adversarial-pr80r4.test.ts into apps/cloud-gateway/test/backup/ temporarily (run from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/backup/adversarial-pr80r4.test.ts`): P0, P1, P2, K1 (including its report check) and F1 must PASS and S1, S2 stay passing; delete the copy before committing (keep your own equivalent named tests). Merge origin/main first (keep every AGENT_LOG entry). Do not touch voice/**, calls/**, the Telegram reply path, school/**, university/**, memory/** source. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop. Standing rules: never merge, deploy, apply migrations, create cloud resources, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
