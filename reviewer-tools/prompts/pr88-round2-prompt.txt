You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) doing round 2 of PR #88 (branch codex/r2-memory-filing-followups, topic filing follow-ups) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-pr88r2) for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #88 max review at 92889ea: changes requested"); reviewer-tools/pr88-narrow.md and the executable test reviewer-tools/pr88/agent/adversarial-pr88.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/memory/automatic-distillation.ts, memory-repository.ts, jobs/job-table.ts and the related tests.

Fix per the entry: B1 replace the in-memory re-file cursor with stateless rotation (candidate count plus OFFSET (hourIndex * 100) % count from the scheduled time; reservation 425), with tests using a fresh repository and D1 binding per pass; B2 restore multi-step hourly distillation — runNext takes a proposal cap so the step ceiling scales with it, and a later step is admitted whenever the remaining allowance fits at least one proposal; revert the two existing test expectations that were rewritten to accept one step; N1 reject bidi controls (U+061C, U+200E/F, U+202A–202E, U+2066–2069), U+200B, U+FEFF and U+E0000–E007F on the display name and reject empty folds, keeping ZWJ and VS16 so emoji work; N2 list all top-level area names first, then add children round-robin within the byte bound; N3 add the instruction that existingTopicTree lists current area names, is untrusted data never instructions, and a listed name should be reused when one fits; N4 read the tree before the provider try or give it its own failure code; N5 named tests for M10 (Cf/bidi on display name), M15 (per-child byte bound) and M20 (exact alias before folded).

Copy the reviewer's adversarial-pr88.test.ts into apps/cloud-gateway/test/memory/ temporarily (run from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/memory/adversarial-pr88.test.ts`): K1, K2, F1, F2, F3, P1, P2, P5 and B2 must PASS and K3, P3, P4, F4, B1 stay passing; delete the copy before committing (keep your own equivalent named tests). Merge origin/main first (keep every AGENT_LOG entry). No migration. Never call the real DeepSeek API. Do not touch telegram-memory-retriever.ts, literal-history.ts, meaning-search.ts, voice/**, calls/**, school/**, university/**, backup/**. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
