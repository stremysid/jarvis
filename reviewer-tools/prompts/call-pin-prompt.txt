You are a Jarvis build chat (repo ksid1229-ops/jarvis) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-callpin) on a new branch codex/r1-sensitive-action-pin from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

OWNER DECISION (Sid, 2026-09-17, recorded; this supersedes the earlier design). The three-word spoken passphrase on EVERY owner call is replaced by:
- **Ordinary owner calls have no verification at all.** He rings Jarvis and talks immediately.
- **A spoken 4-digit PIN is required only immediately before a sensitive action:** spending money, sending anything as Sid (message, email, booking), deleting, changing security or autonomy settings, and reading out memories marked sensitive (health, money, grades, friends and family).
- **Spoken digits are the primary input; keypad DTMF is an equally valid alternative.** Four digits are easier to recognise than three words, and he cannot touch the phone while driving.
- Recognition must be forgiving: clear re-prompts, generous attempts, and a plain spoken explanation of what is being authorised. Sid sometimes struggles to speak clearly on demand; a failed attempt is far more likely to be mis-heard than an attack. Never end the call on the first failure.

Context you must respect: this is the same person's own assistant, and the threat being mitigated is caller-ID spoofing reaching his memory. His judgement, recorded, is that an always-on gate costs more than it buys — but a sensitive action without any proof of identity is not acceptable either.

Read only: src/voice/call-session-do.ts (owner admission, step-up interaction states, the existing `owner_access_pin` and `owner_access_confirmation` states), src/security/owner-passphrase-verifier.ts and guest-pin-verifier.ts (verifier construction, the shared chained PBKDF2 helper — do not change the crypto), src/persistence/owner-passphrase-repository.ts, migrations 0017 and 0018 (owner passphrase and call step-up tables), docs/runbooks/voice-smoke.md (the live evidence contract), DECISIONS.md, and the related tests.

BUILD
1. **A 4-digit owner call PIN**, stored as a verifier with the same construction as the existing credentials (pepper + chained PBKDF2, never the digits themselves), created and rotated through the existing signed Windows CLI alongside `owner-passphrase generate`. The existing three-word phrase remains valid as an alternative credential — accept either — and is not deleted.
2. **Move the gate.** Owner admission on an inbound or outbound call no longer requires step-up. Instead, step-up is demanded at the moment a sensitive action is about to be taken, from a single authoritative list of sensitive capabilities (money, sending as Sid, booking, deleting, security/autonomy changes, sensitive-memory disclosure). The list lives in one place and is the same one the Telegram side uses for "outward actions always ask"; do not scatter the definition.
3. **A successful PIN authorises that action, not the call:** a short window (say 2 minutes) for the action being confirmed, not a blanket owner session upgrade. Receipts record what was authorised, when, and by which credential.
4. **Recognition quality:** accept digits spoken in any ordinary form ("four two seven one", "forty-two seventy-one", digits with pauses), normalise, and allow at least five attempts with distinct re-prompts before giving up; never hang up on failure — offer the keypad instead. Accept DTMF at any point. Nothing in a transcript, log, receipt or event may contain the digits.
5. **Update the R1 evidence contract**: the live scenarios that assume a phrase on every owner call are rewritten for this model (ordinary call with no gate; sensitive action with PIN accepted; sensitive action with PIN refused; keypad path). Update `docs/runbooks/voice-smoke.md` and add a `DECISIONS.md` entry recording Sid's decision AND the correction that the every-call passphrase was a previous chat's interpretation of "okay add a phrase", not his instruction.

Migration: additive only if genuinely needed, **use 0034** (0032 is the memory living-notes work, 0033 is the D2L email work), remote-D1-safe (`SELECT RAISE(...) WHERE ...`, never `SELECT CASE ... RAISE`), with whole-trigger removal tests, and you may make the minimal `src/backup/**` classification changes the restore gate requires. Do NOT apply it.

TESTS (fake providers only, never a real call): ordinary owner call reaches conversation with no step-up; each sensitive capability demands the PIN; a correct spoken PIN authorises exactly that action and nothing else; the window expires; the three-word phrase still works; DTMF works; five distinct re-prompts and no hang-up on failure; digits never appear in any stored text, log or receipt; and named behavioural tests for every guard plus whole-trigger removal tests for any migration.

Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Do not touch memory/**, school/**, backup/** beyond the classification above, or the chained PBKDF2 helper's construction. Push, open a draft PR against main titled "Calling: spoken 4-digit PIN for sensitive actions only", and post a ready entry at the top of docs/AGENT_LOG.md for Claude max review. Then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
