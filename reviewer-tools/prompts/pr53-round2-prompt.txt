You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #53 (branch codex/r5-study-coach-slice1, the study coach) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, then create your own git worktree next to this folder for codex/r5-study-coach-slice1 (latest head, which includes Claude's max review entry), and do all work, tests and pushes only in that worktree.

Read first: the newest Claude entry for PR #53 in docs/AGENT_LOG.md on that branch, the adversarial report reviewer-tools/pr53-adversarial.md, the probes reviewer-tools/pr53/zz-reviewer-pr53-probes.test.ts and reviewer-tools/pr53/regex-probe53.mjs (all on branch claude/reviewer-tools).

Claude max-reviewed PR #53 at 30ec39e: changes requested. Lint, typecheck and 3,311/3,311 tests pass, CASE count is 0, and all 17 whole-trigger removals in 0023 are killed. Migration 0023 is not applied, so fix any schema need in place in 0023; do not add a new migration. Keep the isDirectText flag, but fix how it is used (S4): SchoolCatchupModelAdapter.stream now returns the raw model stream when ownerTurnAuthoritative is false, which skips guardedOrdinaryReply, so forwarded or quoted turns lose the secret-request, false external-completion and D2L check-claim guards that main applied; and Telegram adds quote when Sid highlights part of Jarvis's own message to reply, so his own quote-replies lose school, university and study handling. On non-direct turns skip only mutations and keep the guarded ordinary reply; treat a quote of Jarvis's own message as direct; add tests for both.

B1: an open quiz takes over the conversation. StudyCoachModelAdapter.stream sends every owner message that matches no earlier intent to answerActiveQuiz, open quizzes never expire, so "What's due tomorrow?" days later is graded as a wrong answer and the ordinary bot never answers (probe Q1). Evidence never leaves the active set, so once a course reaches the 24 active cap an answer batch aborts and every later message gets "I couldn't update the study-coach record." until Sid says "stop quiz" (Q4). Only treat a message as an answer when it plausibly answers (short, not a question, not a known intent) or within a bounded window after the question; otherwise dismiss or park the quiz and use the ordinary path; on storage failure dismiss the item and fall back to the ordinary reply; and let evidence age out of the active set so the cap cannot wedge the coach.

S1: grading makes correct answers look wrong. Exact lower-case string equality grades "Mitochondria." wrong (Q2). Owner-topic sources are just the request phrase, so model quotes are almost never in them, every item is uncertain-support, every answer is recorded uncertain, and summariseTopic counts uncertain as weak, so three correct answers make a strong high-confidence weak area (Q5). Normalise punctuation, articles and whitespace; record a non-exact answer as uncertain rather than wrong unless it clearly differs, or ask Sid to self-grade; do not count uncertain-support results toward weak-area judgement or check-ins; and tell Sid owner-topic quizzes are not source-checked.

S2: parseOwnerStudyObservation turns ordinary speech into study evidence and consumes the turn: "Getting up early is hard.", "My recovery is hard", "Walking after surgery feels hard", "Your last reply was wrong", "I'm not sure about going to the party" and "I found parking easy" all parse, and resolveCourse falls back to the only course (Q3). Require an explicit match to an existing course card, fact or known topic before recording; otherwise fall through to the ordinary path; add a table test of non-school sentences that must reach the fallback model.

S3: the narrowed BRIGHTSPACE_CHECK_COMPLETIONS now misses false claims main caught: "I've checked your D2L and nothing new is due.", "We've refreshed Brightspace for you.", "I looked at D2L and there's nothing due.", "D2L was just synced." and "I've just refreshed your Brightspace calendar." Keep main's recall, cover contractions, "looked at" and passive "was", and handle false positives with narrow lookaheads; table-test both sides.

N1: claimDigestCheckIn marks last_prompted_on during assembly, so a failed digest send (or a manual daily digest) spends the check-in; claim after delivery or record it. N2: "that mark was entered wrong" corrects only the evidence row, not the underlying school_course_facts row, and the mark heuristic can pick a non-mark fact; say so or route the correction. N3: creating practice silently dismisses an open quiz; mention it. Fix N items if small, otherwise record them in KNOWN_ISSUES. Also address every high or medium finding in reviewer-tools/pr53-adversarial.md, or record why not in your ready entry. In particular: "Not sure." with a period, "ok" and "check D2L now" are graded as quiz answers and a multi-line message fails; "The due date is wrong", "That plan is wrong", "I finished the lab, that was easy", "I found photosynthesis not hard" and "I got nothing wrong" become evidence and never reach the catch-up adapter; check-in confidence counts easy points and due-work facts become check-ins; generated question and answer text skips the secret and external-action guards and is shown under a Source line; a deploy before 0023 is applied adds a permanent digest gap (treat a missing table as no check-in); flashcard replies can exceed 4,096 characters.

Pull first. If main has moved, merge origin/main and resolve docs-only conflicts by keeping every entry from both sides; if PR #52 has merged with its own forwarded-message flag, reuse isDirectText instead of keeping two. Push fixes, copy the reviewer probe file into apps/cloud-gateway/test/school/ locally and run it (all five probes must now fail; do not commit it), add named tests for Q1-Q5 and the S2/S3 tables, rerun whole-trigger removal evidence for any changed 0023 trigger, run the focused school, digest and Telegram tests and the full suite once, and post in docs/AGENT_LOG.md when ready for Claude max re-review. Standing rules: read files in slices but search the whole file before editing and review the full diff before posting ready; triggers use only SELECT RAISE ... WHERE, never CASE; keep REPLACE/IGNORE insert guards on every unique key; while iterating run only related tests with failures-only output; never merge, deploy, apply migrations, touch secrets, submit, upload or contact anyone. Remove your extra worktree only after pushing.
