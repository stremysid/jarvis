You are an adversarial second reviewer for the Jarvis project (repo ksid1229-ops/jarvis; owner "Sid"). Find real defects in PR #82 before it merges. Treat the builder's claims as unproven.

PR #82 (branch `codex/r2-memory-topic-filing`, head `0394455`; implementation `00978ae`; base `origin/main`) makes automatic memory file items into a tree of areas ("like a brain": School > Chemistry > Unit 2):
- the single existing DeepSeek extraction call now returns, per proposal, a 1–4-component topic path and a filing confidence (`automatic-distillation.ts`, `provider-types.ts`);
- deterministic filing in `memory-repository.ts`: resolve against active sibling names after NFC/case folding, then the newest sibling alias with a bounded merge redirect; create only the missing tail with model-inference topic events; caps of depth 4 below Memory, 40 active children per topic and 6 new topics per hourly workflow; proposed/uncertain facts, confidence < 0.6, cap refusals and filing failures go to Inbox / Needs filing; a filing failure still commits the item;
- an hourly Inbox re-file step (≤ 10 items, exact normalized path match only, no model call);
- a raised measured-D1 statement allowance in `job-table.ts`.
Topic storage, events, aliases, placements and their triggers come from migration 0016 (applied in production, read-only). The design is `docs/plan/2026-09-14-r2-memory-design.md` §6.

WHAT MATTERS MOST
1. Memory is never lost or corrupted. Any path where a valid extracted memory is dropped, rolled back, or left unplaced because filing failed; where a batch partially commits (topic created but item not placed, or placed twice); where a replayed/retried hourly run duplicates topics, placements or events, or advances the distillation cursor past unfiled items; or where a 0016 trigger rejects the new writes in production-shaped data (existing root/inbox renamed, merged topics, archived topics, many siblings).
2. Tree integrity. Duplicate sibling topics (same normalized name) created by concurrency, by case/Unicode variants (NFC vs NFD, Turkish dotted i, full-width letters, trailing spaces, zero-width characters, emoji), or by two proposals in the same run; filing under an archived or merged topic; alias precedence wrong (older alias wins, alias shadows a current name, redirect loop or chain longer than the bound); depth/child/six-per-run caps off by one, counted per item instead of per workflow, or bypassable by the re-file step or by retries.
3. Untrusted model output. The topic path is model text built from Sid's messages, which can include forwarded or quoted text. Hunt for path components that are empty, over-long, control characters, path separators (">", "/"), prompt-injection strings, or names like "Inbox"/"Memory" that could collide with or rename the canonical root/inbox, and for any way filing changes lifecycle, uncertainty, authority or promotion, or lets an uncertain item reach an active area.
4. Re-file step. Moves only Inbox items whose complete path now matches exactly; never moves proposed/uncertain items, forgotten items, or items Sid placed manually; bounded to 10; idempotent; no model call; no cost ledger entry.
5. Cost and budget. Still exactly one provider call per bounded step and the same cap; the schema/prompt change doesn't raise worst-case reserved tokens beyond what the ledger reserves; the raised D1 allowance is actually measured against the worst case and not just widened.
6. Scope. No migration; voice/**, calls/**, school/**, university/** unchanged. Identify guards whose deletion no named test would catch.

WHERE / HOW
- Work from your session's repository worktree. Save the diff first: `git fetch origin && git diff origin/main...origin/codex/r2-memory-topic-filing -- . ':!docs/AGENT_LOG.md' > <scratchpad>/pr82/source.diff`. Read files with `git show 0394455:<path>`; base with `git show origin/main:<path>`. Builder's claims: top PR #82 entry of `docs/AGENT_LOG.md` on the branch.
- To execute, create your own checkout: `git worktree add <scratchpad>/pr82/agent/tree 0394455`, then in it `pnpm.cmd install --frozen-lockfile`, write adversarial tests as `apps/cloud-gateway/test/memory/adversarial-pr82.test.ts` using the existing test helpers (see `test/memory/automatic-distillation.test.ts` and `memory-repository.test.ts` for fixtures), and run only that file from `apps/cloud-gateway` with `npx.cmd vitest run test/memory/adversarial-pr82.test.ts`. Tests must assert CORRECT behaviour (a failing test = a proven defect). Do NOT run tests in `C:\Users\Sid\jarvis-pr39` or `C:\Users\Sid\jarvis-pr40` (the main reviewer uses them). Do not modify the PR branch, push, merge, deploy or apply anything. Never call the real DeepSeek API. Leave your worktree in place (the reviewer re-runs your tests).

OUTPUT: write `<scratchpad>/pr82-adversarial.md` and return it. One-line verdict and counts; findings High → Low, each with **Where** (file:line at the head), **Proven** (test name and result, or "by reading"), **Effect for Sid** in plain words, **Fix**; then short "checked and sound" and "unverified" lists. No padding; an unsubstantiated Medium is worse than none.

<scratchpad> is C:\Users\Sid\AppData\Local\Temp\claude\C--javis--claude-worktrees-jarvis-code-review-0b1695\f3f2b419-52e6-4c42-b275-f2c7a15d27ac\scratchpad
