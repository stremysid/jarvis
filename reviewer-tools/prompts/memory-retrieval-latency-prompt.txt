You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) in a fresh session, doing a production latency hotfix. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch codex/telegram-memory-retrieval-latency from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Production evidence (Sid's live test on deploy 7f112db5, main 7532b41, 2026-09-17 01:05 UTC): three Telegram turns logged contextRetrievalMs 498, 476 and 800; the third logged telegram_memory_retrieval_memory_timeout {baseMs: 450, memoryMs: 800}, so Jarvis answered "What's my fav subject" with "You haven't told me" although D1 holds the (uncertain) item "My favorite subject is math." Memory that times out is memory Sid doesn't have. Production D1 is small (about 185 events, 2 memory items), so this is round trips, not data size: each D1 call from the Worker costs roughly 15–25 ms.

Root causes to confirm by reading apps/cloud-gateway/src/memory/telegram-memory-retriever.ts (retrieve, retrieveMemory, readCandidateContexts, readCandidates, withoutForgottenTurns, retrieveBase), memory/literal-history.ts searchLiteral, memory-repository.ts readCurrentItem/readItemVisibility, and the shared D1ContextRetriever: (1) retrieveMemory awaits the base recent-context promise before starting literal history search, while the 800 ms memory timer started with base, so a 450 ms base leaves memory 350 ms; (2) candidate items are re-read with separate readCurrentItem and readItemVisibility calls, each several single-statement round trips; (3) literal search and base retrieval issue many sequential single statements.

Build:
1. Start literal history search immediately, in parallel with base retrieval; apply the recent-context de-duplication (recentEventIds, excerpt containment, recentContextCoversQuery) when merging results, not before searching.
2. Collapse candidate reads, visibility checks, literal-search reads and base-context reads into batched D1 calls (database.batch or single multi-row statements) with identical results, filters and forget/suppression semantics; declare the new round-trip ceilings.
3. Add telemetry to telegram_turn_outcome or the retrieval log: memory sub-timings (candidates, history, merge) and D1 round-trip count, integers only, no text.
4. Keep every existing rule byte-for-byte in behaviour: owner-control skip, suppression anti-joins, uncertain labelling, byte budgets, 2,500 ms base and 800 ms memory bounds and their fallback codes. D1ContextRetriever may be changed only to reduce round trips with identical output; voice tests must pass unchanged.

Tests: a counting D1 proxy with injected latency of 25 ms per round trip on a production-shaped fixture (about 185 events including recent turns, 2 uncertain proposed items and 2 active items, 8 literal-history hits) must show base retrieval ≤ 250 ms, memory retrieval ≤ 350 ms and the memory item "My favorite subject is math." returned for the query "What's my fav subject"; before/after round-trip counts in the ready entry; a test that a slow base (injected 700 ms) no longer causes a memory timeout; all existing retriever, forget/lift, suppression and telegram-memory integration tests unchanged and passing. Name the mutation you ran to prove the latency test fails on main's serialized path.

No migration. Never call real DeepSeek or Telegram. Do not touch voice/** (except none), calls/**, school/**, university/**, backup/**, meaning-search.ts. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push, open a draft PR against main titled "Telegram: memory recall no longer times out", post a ready entry at the top of docs/AGENT_LOG.md for Claude max review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
