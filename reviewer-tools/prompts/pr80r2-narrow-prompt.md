You are a narrow second reviewer for the Jarvis project (repo ksid1229-ops/jarvis; owner "Sid"). PR #80 (nightly verified backup of Jarvis's D1 data to R2, migration 0031 unapplied) was sent back at `cb8941d`; round 2 is head `07dd1f5` (implementation `a0bcead`, after a main merge). Verify the fixes and find regressions they introduced. Treat the builder's claims as unproven.

Read first: Claude's review entry "PR #80 max review at cb8941d" and the builder's newest entry at the top of `docs/AGENT_LOG.md` on `origin/codex/r2-memory-nightly-backup`; the first-round report `<scratchpad>/pr80-adversarial.md` and its test `<scratchpad>/pr80/agent/tree/apps/cloud-gateway/test/backup/adversarial-pr80.test.ts`. The relevant diff is saved at `<scratchpad>/pr80r2/source.diff` (backup source, 0031, backup and migration tests, KNOWN_ISSUES). Read files with `git show 07dd1f5:<path>`.

CHECK ONLY THESE
1. The builder says ADV3b and ADV6 "contain literal diagnostic sentinels" and ADV4 "assumes the pre-fix pointer-ordering bug", so those three still fail by design. Decide whether each claim is true or whether the defect it proved (ADV3b/S1 staleness or throughput, ADV4 dangling latest pointer after a D1 failure, ADV6 cleanup error reported as backup failure) still exists at the new head. Rewrite each as a correct assertion against the new design and run it.
2. Integer ordinals for WITHOUT ROWID tables (`memory_backup_row_ordinals`): correctness (a row inserted, updated or deleted between ordinal assignment and the cut; reused primary keys after delete; ordinals across tables), and cost (rows and D1 writes added per night for Sid's real data size; whether ordinals are assigned once or rewritten nightly; growth without cleanup).
3. Restore completeness: does a verified set really restore every backed-up table into a fresh D1 with foreign keys on, including tables with triggers that refuse direct inserts (append-only ledgers, guarded state), and does the restore test exercise a production-shaped schema rather than a hand-picked subset? Is the classification test enforced against every table created by all migrations (not a list the test itself defines)?
4. Budgets: 16 steps per invocation against Workers CPU, subrequest and D1 per-invocation query limits for a first full backup of all 104 tables; the three-retry transient policy cannot duplicate objects or receipts; the 36-hour staleness alert fires once and cannot spam.
5. Retention and pointer safety: cleanup never deletes the set `latest.json` names, including when `latest.json` is unreadable, stale, or written by a concurrent run; the order objects → manifest → D1 status holds on truncated listings.
6. Security scope: the backed-up set now includes authentication material (`owner_passphrase_verifiers`, `device_keys`, `bootstrap_tokens`, `request_nonces`, `identity_challenges`, `voice_owner_identity`, step-up tables). Say whether any of these hold secrets or replayable tokens whose copy in R2 creates a new risk, and whether excluding ephemeral ones is safer without breaking restore.
7. The 15 guards in 0031 are remote-D1-safe (`SELECT RAISE(...) WHERE`, never `SELECT CASE ... RAISE`) and the INTEGER key changes keep every CHECK comparing integers.

HOW
- Do NOT use `C:\Users\Sid\jarvis-pr39` or `C:\Users\Sid\jarvis-pr40` (the main reviewer uses them). To execute, create a short-path worktree `git worktree add C:\Users\Sid\jarvis-pr80r2-adv 07dd1f5` (long scratchpad paths break the Workers pool), `pnpm.cmd install --frozen-lockfile`, put tests in `apps/cloud-gateway/test/backup/adversarial-pr80r2.test.ts`, and run from that worktree root: `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/backup/adversarial-pr80r2.test.ts`. Tests assert CORRECT behaviour (a failing test = a proven defect). Do not push, merge, deploy, apply migrations, create cloud resources or call real services. Leave the worktree in place.

OUTPUT: write `<scratchpad>/pr80r2-narrow.md` and return it. One-line verdict and counts; findings High → Low, each with **Where** (file:line at 07dd1f5), **Proven** (test name and result, or "by reading"), **Effect for Sid** in plain words, **Fix**; then short "checked and sound" and "unverified" lists. No padding; an unsubstantiated Medium is worse than none.

<scratchpad> is C:\Users\Sid\AppData\Local\Temp\claude\C--javis--claude-worktrees-jarvis-code-review-0b1695\f3f2b419-52e6-4c42-b275-f2c7a15d27ac\scratchpad
