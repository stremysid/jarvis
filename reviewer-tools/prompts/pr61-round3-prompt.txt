Next task in this session: round 3 of PR #61 (branch codex/r5-school-grades-step5). Claude re-reviewed your round-2 head 4d99d15: changes requested (small). git fetch origin and read only Claude's newest entry at the top of docs/AGENT_LOG.md on that branch; the second reviewer's report is reviewer-tools/pr61b-adversarial.md on branch claude/reviewer-tools if you need the reproduction. You already know this code from round 2.

S1: in readDigestSnapshot's missing-work query add `AND basis.last_seen_at >= d.due_at` so a deadline moved later than the read can never print "no submission"; add a repository test that moves the due date later but still in the past after the read and asserts no line. S2: add a test where Classroom is read at T1 and the scan completes at T2, asserting the digest line's "as of" shows T1 (reverting to sync.last_success_at must fail it). N1: add "+N more" to the capped missing-work list or record the cap in KNOWN_ISSUES.md. N2: count undated-coursework rejections separately from genuinely rejected submissions in the poll detail, and record in KNOWN_ISSUES.md that a submission Classroom recreates under a new id is ignored.

Work in your existing worktree for that branch if present, otherwise create one next to the shared folder; never switch branches in the shared folder. Merge origin/main first, keeping every AGENT_LOG entry. Run the affected test files, lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a short ready entry at the top of docs/AGENT_LOG.md for Claude re-review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, request OAuth consent, spend, sign up, submit or contact anyone.
