You are the calling build chat for Jarvis (repo ksid1229-ops/jarvis), doing round 2 of PR #67 (branch codex/notification-delivery-hardening, fair resumable guest-grant notice drain, migration 0028) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: the newest Claude entry for PR #67 at the top of docs/AGENT_LOG.md on that branch, reviewer-tools/pr67/mut67.json on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:reviewer-tools/pr67/mut67.json — it lists every guard the reviewer removed), and the files those name.

Claude's max review at 6b4e2b7: changes requested (small). Gates are clean (3,715/3,715) and the starvation fix is real; nine of fourteen removed guards leave every test passing. S1: pin the run lease — a test that takes the lease mid-batch and asserts the run stops before its next notify, and an overlapping-run test for already_running. L1: a direct-UPDATE negative test for each of the four unpinned 0028 trigger clauses (lease-expiry bound on running→failed lease_expired; cursor must name an existing notice; claim keeps the cursor frozen; running→running updated_at monotonic). L2: pin the active-claim selection filter, the undeliverable-key skip and monotonicIso, or state precisely why one is behaviour-equivalent. N1: measure ten successful deliveries against the 95-statement budget. N2: remove D1GuestGrantNoticeSink.drain(), which still implements the starving oldest-ten batch, and move its test onto the drainer. N3: reword KNOWN_ISSUES.md so the poison-notice policy is the reviewer's recorded decision, not a wait on Sid: never terminalize a notice, keep retrying once per rotation, and a later slice surfaces any notice undelivered for more than 24 hours as one digest line. Migration 0028 stays with this PR.

Expect the reviewer to remove each guard again and require a named test to fail. Keep every trigger in remote-D1 form, WHEN ... BEGIN SELECT RAISE(ABORT, ...) END. Do not change voice/** behaviour beyond removing the unused drain method, calls/**, D1ContextRetriever or voice/production-runtime.ts. A lone owner-passphrase-routes failure is a known word-collision test defect being fixed separately.

Merge origin/main first; if docs/AGENT_LOG.md conflicts, keep every entry from both sides, newest first. Run lint, typecheck and the full suite once (rerun only unrelated files that time out under load). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review, then stop — do not wait for or poll for a review. Standing rules: never merge, deploy, apply migrations, touch secrets, send any real message, spend or contact anyone. Remove your extra worktree only after pushing.
