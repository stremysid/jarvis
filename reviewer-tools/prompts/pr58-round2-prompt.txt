You are continuing the docs-only migration scratch-proof runbook for Jarvis (repo ksid1229-ops/jarvis), PR #58 on branch codex/migration-scratch-proof-runbook. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for codex/migration-scratch-proof-runbook (latest head, which includes Claude's review entry), and work only there. This is a single-session task: use ordinary file edits, do not use orchestration or task-delegation tooling, and do not spawn sub-tasks.

Read only the newest Claude entry for PR #58 in docs/AGENT_LOG.md and the runbook itself.

Claude's review at aac4d96: changes requested (small). The document is right and carefully built; the numbers and error strings were independently verified, including 200 CREATE TRIGGER declarations with 197 unique names and the incomplete input: SQLITE_ERROR [7500] failure being one this repo really hit. Two changes:

S1. Step 3 compares every migration at or after 0016_ against a hard-coded nine-name list and throws "Repository candidates do not match the reviewed nine in order." PR #52 has 0024 open now and the R2 distillation slice will take 0026, so that check will almost certainly fire by the time Sid runs this, and the runbook never tells him what it means. Add one sentence at that point: it means the reviewed set changed since the runbook was written, it is not a database failure, and the fix is to update the list and have the new set reviewed before proceeding. Do the same for the 197 trigger-inventory count in step 4, which also moves with every new migration.

S2. The proof's blind spot is the one thing production has and scratch does not: rows. A new scratch database starts empty and applies 0001 through 0025. Production applies 0016 onward onto a live database already at 0015 with real data. A migration that is fine against empty tables and fails against existing rows — a NOT NULL column with no default, a new guard whose condition is false for a row already there, a unique index over data that already violates it — passes this proof and fails on his real database. Add a step between the current 3 and 4 that seeds the scratch database with a minimal production-shaped row set before applying 0016 onward, then applies and re-runs the receipts check: one human principal, one device, one active Telegram channel identity, and one conversation event, all with obviously fake identifiers and no real phone number, token or account id. Then state plainly in step 1 that the proof covers the additive migrations against a production-shaped database and still does not cover production's data volume. If you judge the seeding impractical, say why in the entry and instead promote the limitation into its own numbered step so it reads as a known gap rather than a parenthetical.

Two one-liners, your call: say that a mismatch in the CASE/RAISE wording check in step 6 means ask for a review rather than the database being broken, since it matches Wrangler's exact message; and tell him to delete an older scratch database first if wrangler d1 create fails at the account's database limit.

Keep everything that is already good: Windows 11 and PowerShell 7 only, from the repository root, pnpm.cmd, the double confirmation on the scratch name, the case-sensitive comparisons, extracting trigger names from the files rather than a second hand list, the exit-code check on every command, and step 8 pointing at deploy.md instead of duplicating the production procedure.

Run lint and typecheck; the full suite is not needed for a docs-only change. Push and post a ready entry in docs/AGENT_LOG.md for Claude review. Standing rules: never run a migration, create or delete a real database, deploy, merge, touch secrets, spend or contact anyone. Remove your extra worktree only after pushing.
