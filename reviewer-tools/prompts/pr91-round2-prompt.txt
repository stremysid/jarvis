You are a Jarvis build chat (repo ksid1229-ops/jarvis) doing round 2 of PR #91 (branch codex/d2l-notification-email, "School: D2L deadlines and grades from notification email") in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-d2lr2) for that branch at its latest head (which includes Claude's review entry), and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's newest entry at the top of docs/AGENT_LOG.md on that branch ("PR #91 max review at 4c7046f: changes requested (3 High)"); reviewer-tools/pr91-adversarial.md and the executable test reviewer-tools/pr91/adversarial-pr91.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); your own src/school/d2l-email-handler.ts, d2l-email-parser.ts, src/digest/digest-composer.ts, src/jobs/digest-job.ts, and the related tests.

Fix exactly what the entry lists:
- **B1 (High)** — positive authenticity evidence required before anything is ingested. A message may create a deadline or grade ONLY when one of these holds: a DKIM signature whose `d=` is a pinned domain; an `Authentication-Results` added by Cloudflare's own receiving MTA showing `dkim=pass` for a pinned `header.d`; or a valid ARC chain from Sid's Microsoft 365 tenant whose original authentication passed. Missing evidence is quarantine, never trust. `From:` is a routing hint only, never proof. Today `dkimDomains` is computed and never compared to the pin, and results can only lower trust on a literal fail — that is the defect.
- **B2 (High)** — never relay a link that is not on a pinned D2L host, and never from a message that failed the authenticity gate. Otherwise tell Sid a verification mail was refused, without the link. Remove the body-scan fallback that grabs the first https:// URL.
- **B3 (High)** — date-only due dates must parse. The no-time branch sets `millisecond: 999` while the offset probe drops milliseconds, so every candidate misses by 999 ms. Fix the convention, make `dueTimeSupplied: false` reachable, and make the "check your setup" notice fire only on authenticity failures, never on parse failures.
- **S1** an email-sourced grade must not print as `[verified: D2L email]` unless it passed authentication. **S2** cap what is retained for a refused message and give quarantine a documented expiry — the raw ~700 KB receipt is currently written before the quarantine decision and cannot be deleted. **S3** add push-source staleness ("nothing received in N days") since `digest-job.ts:107` returns before every staleness check.
- **N1** wire `GOOGLE_CLASSROOM_EMAIL_FROM_DOMAINS` or drop it (its only use is `void config.googleClassroomDomains;`). **N2** fix the stale `classroom-poll-job.test.ts` assertion that still expects `Brightspace: not set up`. **N3** determine and STATE in your entry whether Microsoft 365 forwarding rewrites `From:`; if it does, forwarded mail will quarantine under the new rule and the ARC path is the primary evidence — say which path Sid's real mail will take.

Copy the reviewer's adversarial-pr91.test.ts into apps/cloud-gateway/test/school/ temporarily (run from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/school/adversarial-pr91.test.ts`): all 13 must pass. Delete the copy before committing and keep your own equivalent named tests. Keep migration 0033 as it is unless a fix genuinely needs a schema change (it is already reviewed as sound; additive and remote-D1-safe only, whole-trigger removal tests for anything new). Merge origin/main first (main is 498e14a). Never send or receive real email, never call a real provider. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push and post a ready entry at the top of docs/AGENT_LOG.md for Claude max re-review. Then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
