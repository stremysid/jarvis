You are a Jarvis build chat (repo ksid1229-ops/jarvis) resuming an interrupted migration-repair task in a fresh session. The PC shut down mid-run. Your previous session left a git worktree at C:\Users\Sid\OneDrive\Documents\ChatGPT\jarvis-migration-repair-remote-d1 on branch codex/remote-d1-replay-repair-0001-0002-0006 (based on origin/main e808093) with UNCOMMITTED partial edits to DECISIONS.md, 0001_foundation.sql, 0002_foundation_hardening.sql, 0006_voice_access.sql and remote-d1-migration-syntax.test.ts. Work only in that worktree; do not switch branches in the shared folder. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

First: git fetch origin; confirm origin/main is still e808093 (merge it if not); run git diff and check every existing edit against the task below before building on it. Nothing from that session was pushed.

Read only: the newest Claude entry titled "PR #60 premise disproved" in docs/AGENT_LOG.md, the 2026-09-13 20:35 entry in the same file describing the original remote-D1 CASE/RAISE incident and the rewrite that fixed 0014 and 0015, migrations 0001_foundation.sql, 0002_foundation_hardening.sql and 0006_voice_access.sql, and apps/cloud-gateway/test/persistence/remote-d1-migration-syntax.test.ts.

Proven this morning against real remote D1: `SELECT CASE WHEN ... THEN RAISE(...) END;` inside a trigger body is rejected with `incomplete input: SQLITE_ERROR [7500]` even when the entire trigger is sent as one wrangler d1 execute --command. It is not a splitter problem. Consequence: 0001, 0002 and 0006 cannot be replayed onto a fresh remote D1, so this repository cannot currently rebuild its own database from scratch. Production is unaffected because those three are already applied and are never re-run.

Task (unchanged from the original brief): make every migration replayable on remote D1 by rewriting each `SELECT CASE WHEN <condition> THEN RAISE(ABORT, '<code>') END;` guard in those three files into the equivalent `SELECT RAISE(ABORT, '<code>') WHERE <condition>;`, exactly the rewrite this repo already applied to 0014's three guards and 0015's eight outbound_attempts_admission guards on 2026-09-13. Do not change any predicate, any error code, any trigger name, or anything else in those files. If a guard's condition cannot be expressed in the WHERE form without changing behaviour, leave it, and say precisely which one and why in your ready entry rather than forcing it.

These three files are already applied in production, which makes this unusual and worth doing carefully:
- Add a clear comment at the top of each rewritten file stating that production was migrated with the earlier CASE-wrapped text, that this rewrite is semantically identical, and that it exists so a fresh database can be rebuilt. Do not imply production was re-migrated.
- Record the same fact in KNOWN_ISSUES.md or DECISIONS.md, whichever the repo uses for a change of this shape, including the date and that the reviewer took this decision rather than the owner because the alternative was a database that can never be rebuilt.
- Prove the rewrite is behaviour-preserving: for every trigger you touch, the existing tests must still pass, and the whole-trigger-removal mutation for that trigger must still be killed by a named test. Run that mutation evidence and put the counts in your ready entry.

Then widen remote-d1-migration-syntax.test.ts so it asserts NO migration in the repository contains the rejected statement form — today it only checks the files it enumerates, and the whole point is that nothing anywhere can reintroduce it. Keep the existing explicit allowance for plain `CASE ... END` value expressions inside triggers, which are proven fine on remote D1, and keep its comment explaining why.

Do not touch any migration other than those three, do not renumber anything, and do not change voice/**, calls/** or any runtime code. Run lint, typecheck and the full suite once. Open a draft PR and post a ready entry in docs/AGENT_LOG.md for Claude max review, then stop. Standing rules: never merge, deploy, apply migrations, create or delete a database, touch secrets, spend or contact anyone.
