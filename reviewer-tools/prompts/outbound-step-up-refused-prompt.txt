You are the calling build chat for Jarvis (repo ksid1229-ops/jarvis), starting a new PR in a fresh session. PR #54 is merged; main is 282f066. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch off origin/main, and do all work, tests and pushes only in that worktree.

Sid has now made the decision that KNOWN_ISSUES.md records as waiting on him. Quoting the file: "Sid has not required a paid live scenario for an answered outbound call that fails step-up, such as voicemail or another person answering... Adding an `outbound-step-up-refused` release scenario means another paid call and waits for Sid's explicit decision." Asked directly on 2026-09-16, Sid said yes: add it. He accepted the extra paid call, roughly a cent.

Build that scenario into the live-evidence contract. Today VOICE_SMOKE_SCENARIOS in tests/acceptance/live/voice-smoke.ts holds six: inbound, unauthorized-caller, outbound-answer, outbound-no-answer, owner-step-up-refused, failure-callbacks. Add `outbound-step-up-refused` as the seventh, and update every place that counts six records: the driver, validateEvidence, auditVoiceEvidence, the release gate and docs/runbooks/voice-smoke.md.

What the scenario must prove, from the owner's point of view: Jarvis rings Sid's phone, something answers that is not Sid saying the passphrase — his voicemail greeting, or another person — and Jarvis ends the call having granted no owner authority and disclosed nothing. In evidence terms that is an outbound call that was answered, where the step-up was prompted and failed rather than never started, ownerAuthorityGranted is false, no authenticated turn, no model request, no personal-context read, and the refusal is observable in stored rows the way PR #54's owner-step-up-refused record already is. Do not reuse `not_started`: that outcome is for an outbound call nobody answered, and PR #54 added a guard that an answered outbound call may not claim owner authority with zero prompts. Decide the right outcome value from the existing OwnerStepUpOutcome union and say why in your ready entry.

Also fold in the two follow-ups from my PR #54 round-3 review, which are calling-chat work and belong in this PR:

1. The two remaining mutation survivors are mutually redundant, not behaviour-equivalent. The audit's `scenario === "inbound" && ownerStepUpOutcome !== "verified"` clause and the `direction !== "outbound"` half of the not_started branch in voice-smoke.ts each survive deletion alone, and I proved that deleting BOTH together still leaves all 68 tests passing. Add one test that pins each: validateEvidence must refuse an inbound record whose ownerStepUpOutcome is "not_started", and auditVoiceEvidence must refuse a set whose inbound record is not "verified". With the seventh scenario landing, re-check that the pair is still needed and still pinned.

2. Move reviewer-tools/pr54/round3/ (mutations.mjs, run-mutations.mjs, run54c.txt) to tests/acceptance/live/evidence-mutations/ and update any path inside them. My asking you to commit that spec is what created a top-level reviewer-tools/ directory in main, which collides by name with the reviewer's own claude/reviewer-tools branch and confuses later sessions. Keep the files, just move them.

Do not bundle anything else. In particular do NOT start on the separate KNOWN_ISSUES item about the evidence store retaining only a passing record with no ledger of failed paid attempts; that needs its own reviewed design.

Record Sid's decision where the repo records owner decisions: update the KNOWN_ISSUES paragraph so it no longer says the scenario waits on him, and add a DECISIONS.md entry dated 2026-09-16 saying Sid approved the extra paid outbound-step-up-refused scenario. Update NEXT_STEPS.md and docs/HANDOFF.md where they state the six-record contract.

Run lint, typecheck, pnpm test, pnpm test:voice-smoke, pnpm test:voice-access and pnpm typecheck:voice-access. Expect the reviewer to delete each new guard clause in turn and require a named test to fail, so give every rule its own load-bearing test rather than relying on a sibling guard. Open a draft PR and post a ready entry in docs/AGENT_LOG.md for Claude max re-review; R1 is the v1.0 release gate so this gets a max review. Standing rules: never place a call, open inbound calling, run the live smoke, merge, deploy, apply migrations, touch secrets or spend. Remove your extra worktree only after pushing.
