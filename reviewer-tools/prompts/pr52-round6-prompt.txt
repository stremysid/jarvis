You are the school build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #52 (branch codex/r5-application-track-slice1, the university application checklist, migration 0024) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder for codex/r5-application-track-slice1 (latest head, which includes Claude's round-5 entry), and do all work, tests and pushes only in that worktree. This is a single-session task: use ordinary file edits and test runs, do not use orchestration or task-delegation tooling, and do not spawn sub-tasks.

To save tokens, read only: the newest Claude entry for PR #52 in docs/AGENT_LOG.md on that branch, the report reviewer-tools/pr52e-adversarial.md on branch claude/reviewer-tools, and the files those name.

Claude's round-5 max re-review at 762e54b: changes requested, 2 High, 2 Medium, 4 Low. H1(b), M1 and Lows 2-4 from round 4 are genuinely fixed and proven load-bearing, the gates are clean (3,639/3,639, 9/9 trigger removals killed with BASE surviving, all four round-1 probes still fail), and the 0024 unverified-INSERT fix is proven against the old head.

Read the opening section of that entry first. The important part is not the list: this is the third round running where the fix for the previous round's Highs introduced new Highs, always in the same two components. Round 3 broke the reply guard toward over-refusal, round 4 toward under-refusal, round 5 does both at once. Two structural changes are asked for, instead of another enumerated phrasing:

1. Evidence binding, in apps/cloud-gateway/src/university/university-tracker-model.ts. Make adjacency the rule: an "it"/"that" clause binds only when it is the IMMEDIATELY NEXT clause after the naming clause in the same sentence. Then "The Western essay is next, and I submitted it." still works and "The Western essay is next, the Common App is done and I submitted it." cannot, without the code needing to know whether the Common App is tracked. Today carriesTarget at :476-490 only resets at a clause naming a tracked item, so a clause about anything untracked leaves it on — proven end to end through the real repository and 0024: that second sentence writes submitted_by_sid on the Western essay and drops it from the digest. Nine variants across submit, ready, drafting, retire, reactivate and un-submit do the same. This is round-4's own disclosed input with a comma instead of a period. Note also that Claude's mutation pass found the reset itself unpinned: deleting carriesTarget = false leaves every test passing, so whatever you keep must have its own test.

2. The ordinary-reply guard, in apps/cloud-gateway/src/school/school-catchup-model.ts. Invert it. When a reply contains a first-person claim verb from the action set at all, require it to match an ALLOWED shape and replace it otherwise, instead of enumerating harmful phrasings. Over-refusal then fails safe and is fixable by widening the allow-list against a corpus, where each missed phrasing today is a silent hole. Two proven failures to fix either way: hasFirstPersonExternalContact at :276-286 splits on [,;.] and requires the verb and target in the same piece, so "I've sent your essay, as promised, to Ms. Chen.", "I've forwarded your reference form, finally, to Ms. Chen.", "I've shared your draft, this morning, with your teacher." and "I've sent your transcript request; it went to the guidance office." all escape now and were blocked at 5f150b1; and the negative lookaheads at :52 are the three literal strings from the round-3 report, so 16 plausible benign replies are newly refused, including the denial "I've requested nothing from the school on your behalf." — the reply saying Jarvis did not act is replaced by one implying it might have. Also close the eight residual cross-clause misses listed as round-5 L4, which share the shape "pronoun object, third party in the next clause"; a sentence-level rule pairing a first-person contact verb with a third-party noun anywhere later in the same sentence would close those and the four regressions together.

M2, which is smaller but permanent for the owner: clauseGroups at :150-166 splits sentences on [.;!?\r\n] BEFORE masking the protected phrases, so a label containing a period is cut in half and, with the whole-message fallback correctly removed, nothing rescues it. "I submitted the St. Michael's reference." — a real UofT college name — is refused in both a plain and a connective-named program. Mask the protected phrases before the sentence split.

Lows, your call to fix or record with the reason: JOINT_OWNER_SUBMISSION still misses "My parents and I" (plural), "My sister and I", the one-l "counselor" spelling and "My mom and I have submitted" (no have allowed); the round-4 Low-2 fix now costs the whole turn because the repository throws and the adapter discards the entire university plan, which round-3 L7 called the worse outcome — mapping the duplicate to the retired itemId and re-running supportsStatus with existingStatus "not_needed_by_sid" was the other option offered; and your Low-1 deferral is recorded only in AGENT_LOG.md while KNOWN_ISSUES.md still says "six deferred limits", so add the seventh bullet.

Every new rule needs its own load-bearing test: expect the reviewer to delete each one in turn and require a named test to fail.

Run lint, typecheck and the full suite once (if unrelated tests time out under load, rerun just those files). Merge origin/main first — it is now 93e3d0d and has moved twice since this branch last merged — keeping every entry from both sides in any docs conflict. Push and post a ready entry in docs/AGENT_LOG.md for Claude max re-review. Standing rules: never merge, deploy, apply migrations, touch secrets, or change voice/** or calls/**. Remove your extra worktree only after pushing.
