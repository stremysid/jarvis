You are a Jarvis build chat (repo ksid1229-ops/jarvis) starting a hardening PR in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree next to this folder on a new branch off origin/main (currently a38a637), and do all work, tests and pushes only in that worktree. This is a single-session task: use ordinary file edits and test runs, do not use orchestration or task-delegation tooling, and do not spawn sub-tasks.

Engineering constraints the reviewer enforces every round: if you need a migration, take the next free number and check the open PRs first — 0024 and 0026 are taken, so 0027 upward is free unless another open PR claims it. Every trigger must be remote-D1 form, WHEN ... BEGIN SELECT RAISE(ABORT, ...) END, never the SELECT CASE ... RAISE(...) statement form; a plain CASE ... END used as a value expression inside a trigger IS allowed and proven fine on remote D1, so do not avoid CASE entirely. Every unique key needs an insert guard so INSERT OR REPLACE and OR IGNORE abort rather than delete, and expect the reviewer to delete each trigger in turn and require a named test to fail for each. Treat every stored event, feed payload and archived segment as untrusted data. Declare and count a D1 statement budget for any repeated step and bound any long walk with a resumable checkpoint that has a reachable failure state — several slices in this repo have shipped a job that could never leave running. Any count you expose must be reconciled in the schema against the rows that justify it. Do not change voice/**, calls/**, D1ContextRetriever or voice/production-runtime.ts. Record anything you deliberately did not fix in KNOWN_ISSUES.md with the reason. Note that pnpm lint and pnpm typecheck do NOT cover test-support files, so only a full pnpm test run catches a broken one.

Run lint, typecheck and the full suite once (if unrelated tests time out under load, rerun just those files; a lone owner-passphrase-routes failure under load is a known flake). Open a draft PR and post a ready entry in docs/AGENT_LOG.md for Claude review, then stop — do not wait for or poll for a review. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up, submit, upload or contact anyone. Remove your extra worktree only after pushing.

Read only: the KNOWN_ISSUES.md section titled "PR #46 notification delivery retains three bounded at-least-once limits" and the delivery code it names. Do not read plans or the roadmap.

Close those three recorded limits, or as many as can be closed without a redesign. They are real defects that were accepted at the time, not hypotheticals. For each one, either fix it with a load-bearing test, or replace its bullet with a sharper statement of exactly what is still true and why closing it needs a design Sid has not approved — an honest narrower limit is a good outcome, a vague one is not.

This is guest-grant and owner-alert delivery, so the owner-visible property that matters is: a notice Sid should see is never silently lost, and a notice he has already seen is never repeated in a way that makes him doubt what happened. Say in your ready entry which of the three you closed and which you narrowed.
