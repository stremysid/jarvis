You are a narrow second reviewer for the Jarvis project (repo ksid1229-ops/jarvis; owner "Sid"). PR #80 (nightly verified backup of Jarvis's D1 data to R2, migration 0031 unapplied) was sent back at `07dd1f5`; round 3 is head `01292f2` (implementation `54a7b8b`, after a main merge). The main reviewer is re-running gates, 0031 whole-trigger removal and the round-2 narrow tests (GROWTH, DELETE, GUARD, RESTORE-A, RESTORE-B). Your job: verify the new restore path really restores Sid's data and find regressions. Treat claims as unproven.

Read first: Claude's entry "PR #80 max re-review at 07dd1f5" and the builder's round-3 entry at the top of `docs/AGENT_LOG.md` on `origin/codex/r2-memory-nightly-backup`; `<scratchpad>/pr80r2-narrow.md`. The backup-only diff is at `<scratchpad>/pr80r3/source.diff`; the new code is `apps/cloud-gateway/src/backup/memory-backup-restore.ts` and runbook `docs/runbooks/memory-backup-restore.md`. Read with `git show 01292f2:<path>`.

CHECK ONLY THESE
1. Restore fidelity on production-shaped data. Build a realistic source D1 (all migrations) with rows past their initial state in EVERY table that has an insert guard, plus: several hours of distillation (finished runs, cost ledger, item versions/transitions/placements, forgotten and lifted items, suppressions), literal-history chunks and coverage, school and university saves, conversation turns/deliveries/idempotency, archive segments. Run the real backup to a verified set, restore with the real restore path into a fresh D1, then compare every backed-up table row-for-row and every rebuilt derived table (memory_item_state, placement state, FTS contents, memory_cursors) with the source. Then run the hourly distillation and literal-history jobs once against the restored D1 with a fake provider: they must not re-pay (no provider call for already-receipted events), duplicate items, or fail triggers.
2. Trigger recreation: after restore every trigger defined by migrations 0001–0031 exists with identical SQL, and inserting a guarded bad row fails exactly as on a normally migrated D1.
3. The runbook is executable by Sid on Windows (PowerShell, `node node_modules/wrangler/bin/wrangler.js`, never `wrangler d1 export` against production, scratch D1 first) and cannot touch production D1 without an explicit, separate confirm step.
4. Growth and deletes: receipts excluded, `scheduled_runs` bounded to 48 hours, short-lived tables excluded, exported ≤ expected recorded without failing; the classification test enumerates migration files and runtime fails closed with an alert on an unclassified table.
5. Paging by ordinal range no longer scans whole WITHOUT ROWID tables (query plan), and the invocation stays under Workers Paid's 1,000 D1 queries per invocation.

HOW
- Do NOT use `C:\Users\Sid\jarvis-pr39` or `C:\Users\Sid\jarvis-pr40`. Reuse the short-path worktree `C:\Users\Sid\jarvis-pr80r2-adv`: `git -C C:\Users\Sid\jarvis-pr80r2-adv checkout --detach 01292f2` (keep its untracked test), `pnpm.cmd install --frozen-lockfile`, add tests in `apps/cloud-gateway/test/backup/adversarial-pr80r3.test.ts`, run from that worktree root: `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/backup/adversarial-pr80r3.test.ts`. Tests assert CORRECT behaviour (a failing test = a proven defect). Do not push, merge, deploy, apply migrations, create cloud resources or call real services. Leave the worktree in place.

OUTPUT: write `<scratchpad>/pr80r3-narrow.md` and return it. One-line verdict and counts; findings High → Low, each with **Where** (file:line at 01292f2), **Proven** (test name and result, or "by reading"), **Effect for Sid** in plain words, **Fix**; then short "checked and sound" and "unverified" lists. No padding; an unsubstantiated Medium is worse than none.

<scratchpad> is C:\Users\Sid\AppData\Local\Temp\claude\C--javis--claude-worktrees-jarvis-code-review-0b1695\f3f2b419-52e6-4c42-b275-f2c7a15d27ac\scratchpad
