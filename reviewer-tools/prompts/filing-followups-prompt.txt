You are the memory build chat for Jarvis (repo ksid1229-ops/jarvis) in a fresh session, doing the follow-ups to merged PR #82 (automatic topic filing). Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-filing-fu) on a new branch codex/r2-memory-filing-followups from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

Read only: Claude's entry "PR #82 max re-review at e4fb760: cleared with follow-ups" in docs/AGENT_LOG.md on main; reviewer-tools/pr82r2-narrow.md and the executable test reviewer-tools/pr82r2/adversarial-pr82r2.test.ts on branch claude/reviewer-tools (git show origin/claude/reviewer-tools:<path>); apps/cloud-gateway/src/memory/automatic-distillation.ts, memory-repository.ts (automatic filing, re-file, name folding, alias lookup), jobs/job-table.ts, and the related tests.

Fix F1–F4 from that entry. F1: a wrapping re-file cursor on (updated_at, item_id) so unmovable Inbox rows can never starve later ones; and give the single extraction prompt the current top two levels of the topic tree (names only, bounded bytes) so the model files into existing areas instead of inventing new top-level names — no extra model call. F2: charge prepareAutomaticCommit per write attempt, stop re-file at refiled + failed >= 10, and pin both with counting-proxy tests. F3: run separator and Unicode Cf checks on the folded form; fold names as NFKC, then strip \p{Default_Ignorable_Code_Point}, then lowercase; apply the same fold to alias lookup under a parent; existing stored rows keep working without a migration (if impossible, stop and report). F4: named tests pinning the reachable surviving guards listed in the entry (one batch for areas plus item, equivalentAutomaticFilingReason replay, oldest-first NFKC tie-break, SQL decision filter in re-file, exact name before fold, memory_corrupt rethrow in prepare, the job's re-file reservation and canRefile).

Copy the reviewer's adversarial-pr82r2.test.ts into apps/cloud-gateway/test/memory/ temporarily (run from the worktree root with `npx.cmd vitest --config vitest.workspace.ts run apps/cloud-gateway/test/memory/adversarial-pr82r2.test.ts`): its failing N1, N2, N5, C2, D5, D4 must PASS after your fix and its passing tests stay passing; delete the copy before committing (keep your own equivalent named tests). No migration. Never call the real DeepSeek API. Do not touch telegram-memory-retriever.ts, literal-history.ts, the batched item reader (another builder is changing retrieval), voice/**, calls/**, school/**, university/**, backup/**. Run lint, typecheck and the full suite once (rerun only unrelated files that time out). Push, open a draft PR against main titled "R2: topic filing follow-ups (no starvation, existing-area hints, look-alike names)", post a ready entry at the top of docs/AGENT_LOG.md for Claude max review, then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
