You are the calling build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #54 (branch codex/r1-step-up-release-evidence, the R1 live-evidence contract) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, then create your own git worktree next to this folder for codex/r1-step-up-release-evidence (latest head, which includes Claude's max review entry), and do all work, tests and pushes only in that worktree.

Read first: the newest Claude entry for PR #54 in docs/AGENT_LOG.md on that branch, the adversarial report reviewer-tools/pr54-adversarial.md and its probe reviewer-tools/pr54/adversarial-probe.mjs, and the mutation results reviewer-tools/pr54/run54.txt (all on branch claude/reviewer-tools), plus docs/superpowers/specs/2026-09-14-owner-call-passphrase-design.md.

Claude max-reviewed PR #54 at bf16699: changes requested. Lint, typecheck, typecheck:voice-access, 3,280/3,280 tests and 874 voice-access tests pass; the six-record arithmetic, commit and correlation binding, exact-key redaction and fail-closed parsing are sound.

B1: the gate can certify v1.0 without a live inbound passphrase verification. validateOwnerStepUp accepts waived_passed_a for inbound, and auditVoiceEvidence never compares ownerCallerIdPolicy across records, so a set with a waived inbound record under waive_on_passed_a plus an owner-step-up-refused record under passphrase_always passes (proven by the adversarial probe). Sid's decision is that the waiver is built but ships switched off and initial deployment uses passphrase_always. Require ownerCallerIdPolicy "passphrase_always" on every owner-path record and ownerStepUpOutcome "verified" on inbound in the release audit; keep the per-record waiver validator only for a future separate optional waiver record that can never replace verified inbound; add audit tests for the mixed set, a waived inbound set and a policy disagreement.

S1: fixedRefusalSentToProvider, cleanEndFrameSent and ownerAlertCount cannot be derived from stored per-call state: #deliverOwnerStepUpRejection swallows a sendNeutralText failure, falls back from relay.end to close(1008) while recordRejectionDelivered writes the same row, and owner alerts are keyed by owner/class/direction with no session id and coalesced for 15 minutes. Replace these with observable fields (for example rejectionDeliveryRowCount 1 and ownerAlertDisposition "sent" | "coalesced", requiring sent), state in the runbook that the refusal scenario must start at least 15 minutes after any earlier owner rejection, and record in KNOWN_ISSUES the runtime follow-up to persist per-session refusal_sent, end mode and alert disposition. Do not add a migration in this PR.

Owner decision, do not implement: there is no live evidence for an answered outbound call that fails step-up (voicemail or another person). Record it in KNOWN_ISSUES as an owner decision tied to the existing outbound voicemail gap; adding an outbound-step-up-refused scenario means another paid live call and waits for Sid.

Low items, fix if small or record: L1 refusal counts pinned to 3/3 reject a real refusal with one "say only your passphrase" re-prompt and there is no rejection reason, so add ownerStepUpRepromptCount (0-2, prompts === 3 + reprompts) and ownerStepUpRejectionReason "attempts_exhausted"; L2 enforce attempts <= prompts <= attempts + 2 for verified; L3 require six distinct correlation ids and disjoint eventIds in the audit; L4 bound the refusal duration and reject a future startedAt; L5 make outbound records mirror the D1 binding (passphrase_always / not_applicable) and give outbound-no-answer a not_started outcome rather than refused; L6 record the missing failed-attempt ledger in KNOWN_ISSUES; L7 add the #46 step-up unit, migration and route tests to scripts/voice-release-gate.mjs and its test; L8 restore the schema 1.1 and guest_pin rejection assertions and the explicit per-scenario loop as a backstop; L9 update the release-manifest plan's voice keys or note that aggregation uses VOICE_SMOKE_SCENARIOS. F1: Claude's mutation pass (reviewer-tools/pr54/mut54.json, run54.txt) killed 22 of 29; V05 and V13 are equivalent, but five real survivors need named negative tests: V11 refusal record with terminalState other than rejected; V12 refusal record with authenticatedTurns > 0; V22 outbound-no-answer with non-zero step-up prompts or attempts; V03 outbound record with callerIdAttestation other than absent (adjust if L5 changes the values); V25 a schemaVersion 1.2 record carrying the 1.3 fields. Rerun reviewer-tools/pr54/mut54.json against your head (update anchors that your changes move) and report the result in your ready entry.

Pull first. If main has moved, merge origin/main and resolve docs-only conflicts by keeping every entry from both sides. Push fixes, run pnpm test:voice-smoke, pnpm test:voice-access, pnpm typecheck:voice-access, lint, typecheck and the full suite once, and post in docs/AGENT_LOG.md when ready for Claude Opus 5 max re-review. Standing rules: read files in slices but search the whole file before editing and review the full diff before posting ready; while iterating run only related tests with failures-only output; never place calls, open inbound calling, run live smoke, merge, deploy, apply migrations or touch secrets. Remove your extra worktree only after pushing.
