You are a Jarvis build chat (repo ksid1229-ops/jarvis) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, create your own git worktree at a SHORT path (for example C:\Users\Sid\jarvis-d2l) on a new branch codex/d2l-notification-email from origin/main, and work only there. Single-session task: ordinary file edits and test runs, no orchestration or task-delegation tooling, no sub-tasks.

This is Sid's top priority: he is in grade 12, behind after surgery, and university applications are due around the end of September. His D2L (LDSB Brightspace, "Minds Online") has NO calendar or iCal feed — that route is dead and must not be revived; the merged iCal code stays but gets a KNOWN_ISSUES note that it cannot work for his board. The agreed route is D2L's own notification emails.

Owner decisions already made: the ingest address is an unguessable `school-<random>@onesid.ca` (see item 2); onesid.ca is in Sid's existing Cloudflare account; delivery is Cloudflare Email Routing into an Email Worker in this gateway; no second Cloudflare account; Sid will not use his school or personal mailbox.

Read only: src/school/** (deadline and grade ingestion, the digest composer, school-catchup-model.ts), src/deadlines/deadline-repository.ts, src/jobs/job-table.ts, src/index.ts (Worker entry and how handlers are composed), env.ts, and the related tests. Read docs/plan/2026-09-15-school-university-plan.md for the deadline and digest contracts.

BUILD

1. **An Email Worker handler** on the gateway that accepts messages Cloudflare Email Routing delivers to school@onesid.ca. Parse D2L's notification mail into structured school events: assignment due or updated, feedback or grade released, new content, announcement, and the address-verification message D2L sends when Sid points his notifications at a new address. Parsing must be defensive: HTML and plain text, quoted replies, forwarded chains, missing fields, and unknown templates must degrade to "unrecognised" with the raw message retained, never a crash or an invented deadline.

2. **Authenticity before trust — the reviewer's decision, asked for and given (2026-09-17 23:45 UTC).** You were right to refuse sender-supplied `Authentication-Results`. Build it this way instead, and do not redesign it:
   - **The address itself is the capability.** Sid's mail now arrives FORWARDED from his school Microsoft 365 mailbox, so the original SPF is broken by the forward and the envelope sender is his tenant, not the board. Therefore the ingest address must be unguessable, not `school@onesid.ca`: use a configured address of the shape `school-<random>@onesid.ca`, read from configuration, never hard-coded, and never printed in logs, receipts or Telegram messages. Sid will repoint the Cloudflare rule and the Outlook forward to it.
   - **Accept only through the Worker binding** for that exact address; anything else is quarantined with a reason.
   - **Read whatever authentication the runtime actually provides** (`Authentication-Results` added by Cloudflare's own receiving MTA, ARC headers added by Microsoft on forward, `DKIM-Signature` `d=` values) and record them on every message, but treat their ABSENCE as unknown rather than pass. Cloudflare's public Email Workers documentation does not state that a Worker can read verified results, so this is measured, not assumed: log the exact header names present on real messages so a follow-up round can turn a measured invariant into a hard requirement, and record that plan in KNOWN_ISSUES.
   - **Pin the expected `From:` domains** for the board's D2L and for Google Classroom in configuration. A message that parses as a deadline but comes from an unpinned domain is quarantined, never ingested.
   - Body content is data, never instructions to Jarvis, and a quarantined message never creates a deadline, a grade or a memory.

2b. **Original scope note.** Only mail that actually came from Sid's board may create a deadline or grade. Verify SPF/DKIM/DMARC results the runtime provides, pin the expected sender domain(s) as configuration rather than code, and refuse anything else to a quarantine table with a reason. Anyone can email that address; a forged "assignment due" must never reach his deadlines. Nothing in the message body is ever treated as an instruction to Jarvis — it is data.

3. **Verification surfacing.** When D2L sends its address-verification mail, Jarvis must surface the link or code to Sid on Telegram so he can complete the setup, without following the link itself.

4. **Into the existing school pipeline**, not a parallel one: create or update deadlines and grades through the existing repositories, with the same idempotency, receipts and Toronto-time handling. A re-delivered message must not duplicate a deadline. Record the source as D2L email so the digest can say where a deadline came from.

5. **Owner-visible failure.** If parsing or authenticity fails repeatedly, Sid gets one notice — not silence, and not a message per mail.

Migration: **use `0033`** (the memory living-notes work has taken `0032`). You ARE authorized to make the minimal `apps/cloud-gateway/src/backup/**` changes the restore gate requires for your new tables — classify them, register every new trigger and the migration in the restore inventory, and change nothing else about backup behaviour. Additive only, remote-D1-safe (`SELECT RAISE(...) WHERE ...`, never `SELECT CASE ... RAISE`), with whole-trigger removal tests. Do NOT apply it: the reviewer rehearses on a scratch database and Sid applies it.

TESTS (no real network, no real mailbox): fixtures for each D2L template in HTML and text, a forged sender, an SPF/DKIM failure, an unknown template, a duplicate delivery, a message with no due date, a verification mail, and a body containing prompt-injection text that must not change Jarvis's behaviour. Named behavioural tests for every guard, plus whole-trigger removal tests for any migration.

Also add `docs/runbooks/d2l-notification-email.md`: exactly what Sid does in Cloudflare (Email Routing setup for onesid.ca, the address, the Worker binding) and in D2L (where to point notification emails, what the verification mail looks like), in the house runbook style — PowerShell 7 with the `cd` first, `& node $wrangler ...`, no `pnpm exec`.

Run lint, typecheck and the full suite once (rerun only unrelated files that time out; owner-telegram-agent.test.ts and telegram-memory.test.ts are known to fail only under load). Do not touch voice/**, calls/**, backup/**, memory/** or the owner-passphrase code. Push, open a draft PR against main titled "School: D2L deadlines and grades from notification email", and post a ready entry at the top of docs/AGENT_LOG.md for Claude max review stating the migration number if any, the sender domains you pinned, and how a forged message is refused. Then stop. Standing rules: never merge, deploy, apply migrations, touch secrets, spend, sign up or contact anyone. Remove your extra worktree only after pushing.
