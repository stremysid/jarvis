You are the university application build chat for Jarvis (repo ksid1229-ops/jarvis), continuing PR #52 (branch codex/r5-application-track-slice1) in a fresh session. Other Codex sessions use this shared folder on other branches, so do not switch branches here: git fetch origin, then create your own git worktree next to this folder for codex/r5-application-track-slice1 (latest head, which includes Claude's max review entry), and do all work, tests and pushes only in that worktree.

Read first: the newest Claude entry for PR #52 in docs/AGENT_LOG.md on that branch, the adversarial report reviewer-tools/pr52-adversarial.md and the probes reviewer-tools/pr52/zz-reviewer-pr52-probes.test.ts (both on branch claude/reviewer-tools).

Claude max-reviewed PR #52 at 3388c39: changes requested. Lint and typecheck pass, the 3 full-suite failures were voice timeouts under load (56/56 on rerun), and all 7 whole-trigger removals in 0024 are killed. Migration 0024 is not applied, so fix the schema in place in 0024; do not add a new migration.

B1: submitted_by_sid is not tied to its item and can never be undone. OWNER_SUBMISSION matches anywhere in the whole message, so "I submitted my Waterloo AIF. I haven't started the Western essay yet." marks the Western essay submitted (P1), and a retracted report (P3) or a quoted question after a colon (P4) also pass; submitted_terminal plus reject_delete then make the mark permanent and the digest hides the item forever. Tie the positive report to the item: the report clause must name that item (label, or kind plus university or program), at most one submitted_by_sid update per turn, and refuse when the message also carries negation, retraction ("actually", "didn't go through"), question or conditional markers. Add an owner correction path out of submitted_by_sid from a later owner turn with whole-message evidence naming the item; at the schema level, for example, allow leaving submitted_by_sid only when source_turn_id changes, and keep submitted_at consistent with the status CHECK.

S1: an item can never be retired (delete forbidden, label immutable, no "not doing this" status), so a mistaken, duplicate or skipped item stays in the digest and holds a cap slot forever. Add an owner-reported non-digest status in 0024 (for example not_needed_by_sid) with exact owner evidence, excluded from the digest and able to return to an active status.

S2: non-submitted status evidence accepts any substring, so "I haven't started the Western essay yet" supports drafting via "started the Western essay" (P2). Require the evidence to be the whole sentence or clause and refuse clauses with negation, conditional or question markers, or use the whole-message rule for every status change.

S3: guardedOrdinaryReply and fallbackWithSaveFailure now route every ordinary reply through safeReply, whose safeModelText throws on over-long or non-NFC text, so a long ordinary answer on any fallback path ends the turn with no reply; apply the claim patterns without that gate (normalise and bound instead of throwing) and add a test. S4: FALSE_EXTERNAL_COMPLETIONS still passes "I've sent your reference request to Ms. Chen.", "I've now submitted your Waterloo AIF.", "Done, your Waterloo AIF is submitted.", "...has been sent to the guidance office" and "gone ahead and submitted"; cover passive is/was/has been submitted/uploaded/sent, sent ... to, now, gone ahead and, with a table test, and add negative tests for benign "I've spent some time" style replies. S5: MAX_STRUCTURED_PROMPT_BYTES is 48,000 and submitted (and after S1 retired) items stay in universityStateJson forever, so full caps can push every turn onto the ordinary path and lock both trackers; keep submitted and retired items out of prompt state or summarise them, and add a test that full caps still fit. M4: an existing item's verified date can be cleared by a null-date update with any excerpt; require whole-message correction evidence. Labels are free model text: refuse labels carrying dates or verified/unverified wording, or require an owner-message excerpt. Forwarded Telegram messages are not marked, so a forwarded teacher line "I have uploaded your transcript" reads as Sid; refuse submitted_by_sid on forwarded or quoted turns if ingress can carry that flag cheaply, otherwise record it in KNOWN_ISSUES as a cross-feature follow-up. N1: applyOwnerPlan counts only items under active programs but cap_insert counts every row, so a save near the cap fails as a generic save failure; count what the trigger counts. N2: safeOrdinaryReply now applies the full guard to every ordinary fallback, and the new spent/spending and reached out verbs replace harmless replies; pin with a test or narrow. N3: evidenceSupportsDate accepts 03/04/2027 for both March 4 and April 3; refuse or keep unverified an all-numeric date with both parts at most 12 unless the evidence is ISO or uses a month name. Fix N items if small, otherwise record them in KNOWN_ISSUES. Also address every high or medium finding in reviewer-tools/pr52-adversarial.md, or record why not in your ready entry.

Main has moved to 1cae97b (#50 merged). Pull first, then merge origin/main into the branch; resolve any docs-only conflicts (KNOWN_ISSUES.md, NEXT_STEPS.md, docs/AGENT_LOG.md) by keeping every entry from both sides. Then push fixes, copy the reviewer probe file into apps/cloud-gateway/test/university/ locally and run it (all four probes must now fail; do not commit it), add named tests for P1-P4, the correction path and the new status, rerun whole-trigger removal evidence for every 0024 trigger including any new or changed one, run the focused university and school tests and the full suite once, and post in docs/AGENT_LOG.md when ready for Claude max re-review. Standing rules: read files in slices but search the whole file before editing and review the full diff before posting ready; triggers use only SELECT RAISE ... WHERE, never CASE; keep the REPLACE/IGNORE insert guard on both keys; while iterating run only related tests with failures-only output; never merge, deploy, apply migrations, touch secrets, submit, upload or contact anyone. Remove your extra worktree only after pushing.
