# Reviewer handoff — 2026-09-17 17:10 UTC

This supersedes every earlier handoff (`HANDOFF-2026-09-16c.md` and older). Claude Opus 5 wrote it at Sid's request, at the end of a very long session. Everything a successor needs is in three places: this file, `C:\Users\Sid\.claude\projects\C--javis\memory\`, and branch `claude/reviewer-tools`. **Nothing important lives only in a scratchpad.** Where this file conflicts with an older handoff or with the reviewer manual, this file wins.

---

## 0. Read these, in this order, before doing anything

1. Every file listed in `C:\Users\Sid\.claude\projects\C--javis\memory\MEMORY.md`. The newest and most important are:
   - `checkpoint-2026-09-17-night.md`;
   - `jarvis-vision.md`: Jarvis is a Claude/GPT-class personal agent with full memory and real tools;
   - `sid-ai-inference-not-hardcoded.md`: the AI decides; code only enforces permissions and receipts; no regex routers;
   - `sid-memory-requirements.md`, items 11–13: the final memory design, and Luna as the brain;
   - `sid-standing-decisions.md`: outward actions always ask; proactive messages 7am–2am as an AI-editable preference; sensitive memories kept private; **the reviewer deploys**;
   - `jarvis-personality-pushback.md`: Sid's standing instructions for Jarvis;
   - `reviewer-may-merge.md`: merge and deploy authority, with guardrails.
2. This file.
3. The top 15 entries of `docs/AGENT_LOG.md` on `origin/main` and on each open PR branch.
4. `reviewer-tools/memory-system-research-2026-09-17.md` on this branch. It is the research synthesis, cross-checked against Sid's own deep research from two Gemini models and Claude Opus.
5. `reviewer-tools/REVIEWER-MANUAL.md`, for the loop and formats. Sid has since delegated merging AND non-migration deploys to the reviewer.

Repository `ksid1229-ops/jarvis`; local folder `C:\javis`; `gh` is authenticated. Status page: https://claude.ai/artifact/XjrTCjXg4o6rBEzi7t89f5. The last source copy is `reviewer-tools/status-page-v167.html`. Republish to that URL with `Artifact` `url`, after an `action: "read"` first.

---

## 1. State at handoff

- **Production:** gateway version `4900dffa`, deployed by the reviewer. It equals **main `7a84220`**. D1 is at migration `0030`, with 0031 not applied.
- **Live and verified (2026-09-17 05:17 UTC):** Sid asked "What's my fav subject". Jarvis answered from memory in 4.35 s total, with context 452 ms, memory candidates 261 ms, 4 D1 round trips and no timeout.
- **Merged this session:**
  - #75: fewer D1 round trips.
  - #82 and #88: automatic topic filing and its follow-ups.
  - #85: memory retrieval latency.
  - #87: literal history actually reaches Telegram, archive-safe and slow-base-safe.
- **Sid's PC was off overnight, so nothing ran.** Codex builders run on his PC via `codex exec`.

### Open PRs and the exact next action

| PR | State | Next action |
|---|---|---|
| [#80](https://github.com/ksid1229-ops/jarvis/pull/80) nightly backup to R2, with a resumable restore | **CLEARED** at `d62b23f` (entry `5d9eff9`). The merge is **held** because it adds the `BACKUP` R2 binding `jarvis-memory-backup` plus migration `0031`; merging first would break every deploy. | Sid's one-time setup (§2), then merge with GATE and deploy. |
| [#83](https://github.com/ksid1229-ops/jarvis/pull/83) meaning search (Vectorize bge-m3), plus the #87 follow-ups | Round 2 at `787a1b5`. Gates green (4,944). `adversarial-pr83` 5/5 and `adversarial-pr87` 16/16 pass. The narrow review agent was **killed at shutdown before reporting**. | Relaunch an Opus agent with `reviewer-tools/prompts/pr83r2-narrow-prompt.md` (worktree `C:\Users\Sid\jarvis-pr83-adv`). If it clears, merge only after the Vectorize setup (§2), because of the binding. |
| [#86](https://github.com/ksid1229-ops/jarvis/pull/86) one AI agent plus tools replacing code routers | Round 2 re-review: **changes requested** at `3c25b32` (entry `f434e9d`). Findings: B1 "Done" sent when nothing saved; B2 unsignalled receipts; B3 contradictory remember grounding (fall back to uncertain inferred); B4 deadline vs waitUntil; N1–N7. Round 3 **never started**. | Launch the builder: copy `reviewer-tools/prompts/pr86-round3-prompt.txt` to `$RELAY_SCRATCH/relay/`, then `bash queued-relay.sh pr86-round3`. |
| [#84](https://github.com/ksid1229-ops/jarvis/pull/84) regex claim guard | Superseded by #86. | Leave it open as the record. Don't merge. |

### Queue after that, in order

1. **Luna brain plus standing instructions and preferences.** The prompt is `reviewer-tools/prompts/luna-brain-prompt.txt`. It is updated with the $15 AI-editable cap (a raise needs a confirm tap), 7am–2am proactive hours, and outward actions always asking. Launch after #86 merges.
2. **Paid real-model eval**, Luna vs DeepSeek, covering the #86 corpus plus sycophancy and guessing cases. Worker secrets can't be read back, so Sid sets `OPENAI_API_KEY` and `DEEPSEEK_API_KEY` in **his** PowerShell env and runs the eval script, or pastes the keys only into his own terminal. Give a one-line cost heads-up (under $1).
3. **Profile + living topic notes + nightly consolidation.** The prompt is not written yet. Design is in `sid-memory-requirements.md` items 11–12:
   - an always-in-prompt profile rebuilt in the background;
   - one living Markdown note per topic area, maintained by DeepSeek nightly;
   - raw history and facts stay as the evidence and index layer;
   - supersede contradicted facts, expire time-bound ones;
   - a stable cached prefix (≥4,096 tokens if the brain is Haiku; the Luna cache minimum is 1,024).
4. **D2L notification emails** at `school@onesid.ca`, through Cloudflare Email Routing to an Email Worker. The domain onesid.ca is in the stremywholesale Cloudflare account.
5. **Investigate** the production confusion on "what did I say three messages ago" (events 205–209, before the latest deploys).
6. **Twilio calling setup**, after memory and the AI rebuild land (Sid's decision).

---

## 2. Sid's one-time setup session (first thing when he's at the PC)

He asked for this to be batched into one session of about 10 minutes. All commands run in PowerShell 7, starting with `cd C:\javis`, using `& node node_modules/wrangler/bin/wrangler.js` (not `pnpm.cmd exec`).

1. **R2 bucket for #80:**
   `& node node_modules/wrangler/bin/wrangler.js r2 bucket create jarvis-memory-backup`
2. **Vectorize for #83** (only once #83 clears; the metadata index must exist before any insert):
   `& node node_modules/wrangler/bin/wrangler.js vectorize create jarvis-memory-bge-m3 --dimensions=1024 --metric=cosine`
   `& node node_modules/wrangler/bin/wrangler.js vectorize create-metadata-index jarvis-memory-bge-m3 --property-name=principal --type=string`
   Re-check the exact property names in #83's final ready entry first.
3. **Migration 0031:**
   - The reviewer first rehearses on a scratch D1 using the proven runbook (`docs/runbooks/` scratch rehearsal, the #71 procedure).
   - Sid then takes a Time Travel bookmark and applies 0031 to production.
   - Verify the triggers read-only afterwards (0031 has 15 guards).
   - Remote-D1 hazards: never `SELECT CASE … RAISE`; never `wrangler d1 export` on production.
4. **Merge and deploy:** merge #80, then #83, each with GATE. The reviewer deploys, then watches the logs. That covers the backup cron at 19:30 local, the hourly memory job, and a Telegram turn.

---

## 3. How the loop runs (reference; the scripts are in this branch)

- **Scratchpad setup:** copy `reviewer-tools/{queued-relay.sh,merge-cleared.sh,agentlog-top.mjs,agentlog-merge.mjs,gen-trig.mjs,mutrun.mjs}` into the new session's scratchpad, and `export RELAY_SCRATCH=<scratchpad>`.
- **`merge-cleared.sh`:** it `cd`s into `C:/javis/.claude/worktrees/handoff-documentation-c01991`. Keep that worktree, or edit the path.
- **Builders:** `bash $RELAY_SCRATCH/queued-relay.sh <name>` runs `codex exec` with `$RELAY_SCRATCH/relay/<name>-prompt.txt`. MAX=3 codex.exe includes the desktop app-server, so **2 builders at a time**. Always use a fresh session per task or round. Tell builders to use SHORT worktree paths like `C:\Users\Sid\jarvis-prNN`, because long paths break the Workers test pool.
- **Gates:**
  - Run in `C:\Users\Sid\jarvis-pr39` or `jarvis-pr40`: `pnpm.cmd install --frozen-lockfile && pnpm.cmd lint && pnpm.cmd typecheck && pnpm.cmd test`.
  - Rerun single files with `npx.cmd vitest --config vitest.workspace.ts run <file>`.
  - Load timeouts under builder load are common (voice `call-session-do`, `telegram-memory`); rerun those alone.
- **Reviews:**
  - First max reviews get an Opus adversarial agent. Re-reviews get a narrow brief (`reviewer-tools/prompts/*-narrow-prompt.md` templates).
  - Agents must use short-path worktrees (`C:\Users\Sid\jarvis-prNN-adv`); these exist and hold the adversarial test files.
  - Always re-run the agent's test file yourself before posting.
  - Post evidence to reviewer-tools (`prNN/`, `session-2026-09-16/entries/`, `prompts/`), then post the entry on the PR branch with `agentlog-top.mjs`, after checking that the head matches.
- **Merge:** `GATE=C:/Users/Sid/jarvis-pr39 bash merge-cleared.sh <pr> <branch> <sha> <entry.md>`.
- **Deploy** (the reviewer's job for non-migration merges): `Set-Location C:\javis; git pull --ff-only; pnpm.cmd install --frozen-lockfile; ./scripts/deploy.ps1 -Publish -Confirm:$false`. Then tail logs with `node C:/javis/node_modules/wrangler/bin/wrangler.js tail jarvis-cloud-gateway --format json` from `C:\javis\apps\cloud-gateway`.
- **Read-only production queries:** `node C:/javis/node_modules/wrangler/bin/wrangler.js d1 execute jarvis --remote --json --command "SELECT …"`.
- **Sid's phone:** use `PushNotification` at decision points and deploy-ready moments; it's his standing preference.
- **Claude usage:** Sid is on Max 5x and bought $5 of extra credits. Each Opus review agent costs about 250–400k tokens. If the usage limit hits, hold agents until the reset.

---

## 4. Lessons from this session

- **Sid's direction is binding:** Jarvis runs through the AI (tools plus inference), never through phrase lists or regex routers. Code enforces authority, receipts, reversibility and budgets. Honesty checks may use deterministic guards as a backstop, as #86 round 2 reinstated `guardReplyClaims`, but intent detection must not.
- **Builder claims were wrong or incomplete several times:** "20/20 pass" hid load-sensitive tests, a "fixed" cursor lived only in memory, and a merged-main "full suite" was never run. Always re-run gates and the adversarial suites yourself.
- **Soft-assertion diagnostics** (`expect.soft` sentinel strings like "D5 measured=…") show as failures but aren't defects. Read the actual bound.
- **Deploy prerequisites:** any PR adding a wrangler binding (R2 bucket, Vectorize) makes main undeployable until the resource exists. Hold the merge, or batch the setup with Sid.

— Claude Opus 5
