# Reviewer handoff — 2026-09-15 ~07:30 UTC (supersedes HANDOFF-2026-09-15.md for state)

Read `REVIEWER-MANUAL.md` first, then this file. The memory files in
`C:\Users\Sid\.claude\projects\C--javis\memory\` (start with MEMORY.md) carry
Sid's standing instructions. Several changed today. Read them all.

## Rule changes from Sid today (these override the manual)

- **The reviewer may merge PRs it has cleared** (`reviewer-may-merge`). Merge
  only at the exact reviewed head plus the reviewer's own AGENT_LOG commit, and
  use `gh pr ready`, then `gh pr merge N --merge --match-head-commit`. On an
  AGENT_LOG-only conflict, merge main into the branch with `agentlog-union.mjs`
  and keep both sides. Verify main afterwards. Still never deploy, apply
  migrations, touch secrets or open calling.
- **Relay by computer control** (`overnight-relay-and-standing-rules`). The
  ChatGPT app is granted. The chats are on monitor **G27QC A**: school
  top-right, memory bottom-left, calling bottom-right. The pencil icon at a
  window's top-left opens a new chat, and "Do anything" is the input. Paste
  only after posting the AGENT_LOG entry, only into an idle chat, and never
  approve prompts or change settings. Sid disabled the Windows clipboard/emoji
  service, TextInputHost, which had been blocking clicks.
- **Standing credit rules** go in every Codex task: one chat per PR including
  its fix rounds, file slices, focused tests while iterating, full suite once,
  and run reviewer checklists before "ready".
- **Models:** keep Sol xhigh for every chat. Suggest Astra xhigh only for
  something truly critical, and keep credit use low (`builder-models`).
- **School is top priority** after the foundations (`sid-school-priority`):
  grade 12, behind, applications open about the end of September. Feature
  wishlist and no-commands memory requirements are in their memory files.
- **Agents:** allowed, but keep usage moderate. Sid hit his 5-hour limit today.

## State

- **main `dfb8ca8`**: #39 (0016), #40 (0018, passphrase PR 2), #41 (school
  plan) and #43 (Classroom ingestion) are merged. After the merges: lint and
  typecheck clean, 3,007/3,007 tests.
- **Production is unchanged.** 0016–0018 are unapplied, and 0019 is still in
  a PR. Every apply waits for the Sid-attended scratch remote-D1 proof. The
  draft kit is `remote-d1-0016/`; it is unreviewed and needs updating for
  0017–0019.
- **Open and in progress:**
  - **#42** (memory chat, migration 0019): changes requested at `5b516d1`.
    B1: ten ingress-guard clauses have no isolating test. S1: whole-trigger
    removal fails the test file in setup. F2 is fixed. The fixes were pasted
    and are running. On re-review, rerun `mut42.json` (clause removals, every
    clause must be killed by its own test) and `mut42-f2.json`.
  - **Calling chat, new chat:** passphrase PR 3 (Telegram
    `/disable-owner-step-up`, guest-grant notices, 750 ms voice retrieval
    timeout) plus #40 follow-ups F1–F5 from the round-4 entry. No PR yet.
    Review at max. Rerun the contract-gap ports (`pr40-contract-gaps/port623-*`)
    and the serialized voice gate.
  - **School chat, new chat:** R5 slice 1, the catch-up conversation on the
    live bot. No PR yet. Review at xhigh. #43's follow-ups F1 (digest
    staleness) and F2 (wrap the Classroom bootstrap) are planned for the later
    Brightspace feed PR.
  - **Check the base branch:** both new chats opened in a folder that was on
    the old `codex/r5-classroom-hourly-ingestion` branch. Their prompt says to
    branch off main. When they push, confirm each PR's diff contains only its
    own work.
- **Migration numbers:** 0016–0018 merged, 0019 is #42, next free is 0020.
- **Test copies:** `jarvis-deploy` at #42 `5b516d1`, `jarvis-pr40` at
  `623c64a`, `jarvis-pr39` at `5729b09`, `jarvis-pr33-tests` at main
  `dfb8ca8`. All clean.
- **Watcher:** restart it on PRs 42 and up (manual §6 command, change
  `select(.number>=42)`).
- **Status page:** https://claude.ai/artifact/XjrTCjXg4o6rBEzi7t89f5. The
  source in this folder is stale, so read the live page with
  Artifact `read` before editing.
