# Reviewer handoff — 2026-09-15 ~17:30 UTC (current; supersedes HANDOFF-2026-09-15-b.md)

The previous reviewer session hit its context limit. **Read in this order:**
1. Every memory file listed in MEMORY.md.
2. `REVIEWER-MANUAL.md`.
3. `HANDOFF-2026-09-15-b.md` (rules), then this file. Where they differ, this file wins.

Status page: https://claude.ai/artifact/XjrTCjXg4o6rBEzi7t89f5. Read it with Artifact `read`, edit it, and republish it with `url`.

**Sid context:**
- Windows Update restarted the PC at 12:26 UTC. Nothing was lost.
- About 17:20 UTC Sid had to call an ambulance. Don't press him with anything non-urgent.

## 1. State

**main = `1130694`.** It is verified: lint and typecheck pass, and 3,112/3,112 tests pass.

Merged today by the reviewer, under Sid's delegation:

| PR | Merge |
|---|---|
| #40 | `dfb8ca8` |
| #41 | `d42dce7` |
| #43 | `1fe35ee` |
| #42 | `f0bfbe9` |
| #44 | `3e28bda` |
| #45 | `e0b5072` |
| #48 | `1130694` |

**Migrations:** main owns `0016`–`0020` and `0022`; none are applied. `0021` belongs to #46. **The next free number is 0023.**

### #46: the calling chat, bottom-right window "Build passphrase PR 3". Max re-review in progress.

- **Heads:** round-2 fixes at `91fe8be`, with main merged at **`16bf448`** (MERGEABLE). The merge added only main's files, plus `0022` lines in `test/persistence/migration.ts` and `remote-d1-migration-syntax.test.ts`, plus `universityRepository` in `index.ts`. #46's source is unchanged from `91fe8be`.
- **The verification chain `run46b.sh` was still running in `C:\Users\Sid\jarvis-pr40` at handoff**, against `91fe8be`. It runs the gates, the voice gate, the re-ported `port46b` gaps, the reverify3 probes and the 0021 trigger removals.
  - Its outputs went to the old session scratchpad, which may be gone.
  - **First run `git -C C:/Users/Sid/jarvis-pr40 status`**, then `git checkout -- .` if the tree is dirty.
  - **Rerun on `16bf448`:** full gates, then `test:voice-access` serialized, then `pr46/gaps46b/port46b-*.diff`, then the reverify3 probes (use `-t reverify3`, never a pipe or spaces with `npx.cmd`), then `mut46b-triggers.json` with its branch set to `16bf448`.
- **Adversarial round 2 is done** (`pr46b-adversarial.md`). The reviewer verified both findings against the code:
  - **M1 confirmed, which becomes S1 (a regression):** `call-session-do.ts:1214-1227` swallows `alert()` failure but still writes `owner_call_step_up_rejection_deliveries`. No job drains step-up alerts, so a Telegram blip loses the alert forever.
  - **M2 confirmed, which becomes F1:** with `waive_on_passed_a` set, a disabled head gives a 1011 close. That policy is default-off.
  - **L1–L4:** duplicate guest notices; a notice that can never be delivered blocking newer ones; the marker-present socket not closed; more near-miss disable forms reaching the model.
- **Grep before handoff:** no test names the `0021` messages `*_immutable`, `*_delete_forbidden` (both rejection tables) or `guest_grant_notice_transition_invalid` / `_delete_forbidden`. Expect whole-trigger survivors, and add them as an S finding.
- **Draft verdict:** `pr46/entry46b-draft.md`, "changes requested (small)". Before posting, change the head to `16bf448`, delete the "no longer merges cleanly" and merge-main parts (the builder did that), fill the placeholders and add the trigger survivors.
- **Rollout note:** this code needs `0021` applied before deploy.

### #47: the memory chat, top-right window "Build R2 memory repository". Cleared, waiting on its main merge.

- **Cleared with follow-ups F1–F4 at `074312c`**; the entry is `b5f0d9f`. Evidence is in `pr47/` and `mut47.json`. Checks: 3,094/3,094 tests, Q1 now fails, and M1–M4 were killed while M5 (the channel check) survived.
- **The relay was sent at about 17:25 UTC.** The memory chat will merge main, because `NEXT_STEPS.md` conflicted with #48.
- **When it posts:**
  - check that `git diff 074312c <newhead> -- apps` shows only main's changes;
  - check that the only non-main changes are `NEXT_STEPS.md` and AGENT_LOG;
  - run the suite, merge with `--match-head-commit`, and verify main.
- **The follow-ups go into the next memory PR (owner-controls service):**
  - F1: bind archived receipts to their subject before slice 2.
  - F2: bootstrap the root and inbox by stable id.
  - F3: test-only seams.
  - F4: a channel-mismatch test.

### School chat: bottom-left window "Add D2L deadline feed support". Building.

- **Task:** D2L Brightspace calendar-feed deadlines, plus #43 F1 (digest staleness gap) and F2 (wrap the Classroom bootstrap). The feed URL is a secret Sid adds later.
- Its folder showed the old branch `codex/r5-university-tracker`. **When it pushes, check that the diff against main contains only its own work.** Migration 0023 or later.

## 2. Relay (changes from HANDOFF-b)

- **Layout:** the chats are on monitor **"EK271U E"** (`switch_display("EK271U E")`). In the 1456×819 frame the inputs are:
  - top-right memory (1080,344);
  - bottom-left school (360,766);
  - bottom-right calling (1080,766).
  - The new-chat pencil for the bottom-left window is at about (17,428).
- **Verify before send:**
  1. `write_clipboard` the exact text.
  2. Click the input and press `ctrl+v`.
  3. **Zoom and read the input box.**
  4. Only then press `Return`.
  5. Zoom again to confirm the bubble and the stop square. One chat at a time. A stale paste once reached the school chat; see memory `relay-verify-paste-before-send`.
- **Focus:** computer-use refuses clicks whenever another app (Opera, TextInputHost, the desktop) has focus, on any monitor.
  - Ask Sid to click a ChatGPT window and say "go".
  - If the desktop is frontmost, `open_application("ChatGPT")` brings it forward.
- **Clipboard history:** Sid turned it off, but TextInputHost still appears sometimes.

## 3. Tools and copies

- **Tools:** use `git show origin/claude/reviewer-tools:reviewer-tools/<file>` for `mutrun.mjs`, `gen-trig.mjs`, `agentlog-insert.mjs` and `agentlog-union.mjs`. The old scratchpad paths are gone.
- **Test copies:** `jarvis-deploy` is at `1130694` and clean. `jarvis-pr40` is busy or dirty (see #46). `jarvis-pr39` is at `5729b09` and `jarvis-pr33-tests` at `dfb8ca8`.
- **Run one heavy suite at a time.** Builder chats also run suites, which causes 5 s load flakes; rerun any failing file alone before calling it real.
- The watcher polls PRs 46 and up. Restart it after each trigger.
