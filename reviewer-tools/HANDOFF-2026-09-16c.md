# Reviewer handoff — 2026-09-16 18:15 UTC

Supersedes every earlier handoff (`HANDOFF-2026-09-16b.md` and older). Written by Claude Opus 5 at Sid's request. Everything a successor needs is in this file, in `C:\Users\Sid\.claude\projects\C--javis\memory\`, and in branch `claude/reviewer-tools`. **Nothing important lives only in a scratchpad.** If something here conflicts with an older handoff or with the reviewer manual, this file wins.

---

## 0. Read in this order before doing anything

1. Every file listed in `C:\Users\Sid\.claude\projects\C--javis\memory\MEMORY.md`. They hold Sid's standing rules: short replies, PR links, shell plus `cd` for commands, outcomes not internals, merge delegation, and school priority.
2. This file.
3. The top 12 entries of `docs/AGENT_LOG.md` on `origin/main`: `git fetch origin && git show origin/main:docs/AGENT_LOG.md | head -300`.
4. `git show origin/claude/reviewer-tools:reviewer-tools/REVIEWER-MANUAL.md`, for the loop and formats. Where the manual says "Sid merges", Sid has since delegated merging to the reviewer (memory `reviewer-may-merge.md`).

Repository: `ksid1229-ops/jarvis`. The local folder is named `javis` (`C:\javis`). `gh` is installed and authenticated.

---

## 1. State at handoff

**`origin/main` = `ce2b1ee`**. No production deploy and no production migration happened this session. The gateway is still version `28109492`, and production D1 is still at `0015`.

### Merged this session (each at the exact cleared head; main verified equal to the merged head)

| PR | what it gives Sid | merge commit |
|---|---|---|
| [#68](https://github.com/ksid1229-ops/jarvis/pull/68) | test pin: disabled account can't record old-chat search hits | `57a9ad0` |
| [#63](https://github.com/ksid1229-ops/jarvis/pull/63) | roadmap: proactive study coach (R5A) + one Jarvis-run calendar (R6) | `5358b14` |
| [#69](https://github.com/ksid1229-ops/jarvis/pull/69) | fixes the passphrase-route test flake: a random word collision, not load | `dee991f` |
| [#70](https://github.com/ksid1229-ops/jarvis/pull/70) | 0001/0002/0006 guards rewritten so a fresh D1 can replay every migration | `010f93b` |
| [#67](https://github.com/ksid1229-ops/jarvis/pull/67) | guest-PIN notices never starve (migration 0028) | `fd65944` |
| [#60](https://github.com/ksid1229-ops/jarvis/pull/60) | scratch-D1 rehearsal runbook on the real production path | `9ded693` |
| [#61](https://github.com/ksid1229-ops/jarvis/pull/61) | Classroom grades + derived missing work (migration 0027) | `2d098d0` |
| [#59](https://github.com/ksid1229-ops/jarvis/pull/59) | automatic memory distillation (migration 0026) | `4e21369` |

**Unapplied production migration candidates on main: `0016`–`0028`, all 13 contiguous.** `0029` arrives with PR #64.

### Open PRs, exact state, and the exact next action

**PR [#71](https://github.com/ksid1229-ops/jarvis/pull/71): fix the rehearsal runbook for Windows.** Branch `codex/fix-scratch-runbook-windows`.
- Round 1 (`7bb4162`): run as written on real D1, five of six fixes worked; the id regex did not match Wrangler's JSON. Changes requested (entry `57cc479`).
- Round 2 (`6a7f2a3`): regex fixed, a test reads the pattern from the markdown against captured Wrangler output, and step 9's y/n sentence was added. Script tests pass 18/18.
- **STATUS: see §9, updated at the moment of handoff.** If §9 says cleared and merged, nothing is left. If it says not yet verified, run the runbook as written per §5 against a new scratch name. If every step prints its OK line and ends with `SCRATCH DELETE OK`, post a clearance entry and merge with `merge-cleared.sh` using `GATE` (§4).

**PR [#62](https://github.com/ksid1229-ops/jarvis/pull/62): memory in Telegram chats, plus the `directOwnerText` marker.** Branch `codex/r2-memory-runtime-slice-4`, head `ee261a6`.
- First max review (memory, security-relevant). Gates at `ee261a6` passed in `C:\Users\Sid\jarvis-pr39`: lint 0, typecheck 0, `pnpm test` **174 files / 3,880 tests**.
- The adversarial second reviewer was launched and **had not reported at handoff; assume its output is lost.**
- **Next:**
  1. Launch one Opus agent with `reviewer-tools/prompts/agents/pr62-adversarial.md`, replacing `<scratchpad>` with your scratchpad path and `<head>` with `ee261a6`.
  2. Meanwhile write your own guard mutations with `mutrun.mjs` (§4). Minimum set: the `directOwnerText` true condition, each borrowed-text key in `BORROWED_TEXT_KEYS` / `isDirectText`, the owner-principal check in the memory controls, the suppression filter in the retriever, the D1 statement counter, and the receipt-token index. Each must be killed by a named test, with BASE surviving.
  3. Verify any agent High yourself before posting.
  4. Post the verdict (§3).
- #62 was rebased onto final #59 by a merge commit (`56bd556`), so the diff to review is `origin/main...ee261a6`.

**PR [#64](https://github.com/ksid1229-ops/jarvis/pull/64): full university application workflow (migration 0029).** Branch `codex/r5-university-application-step6`, head `ebabd18`, implementation `2330218`.
- Round 1 (`3b267b9`): changes requested, 2 High, 7 Medium, 5 Low. Entry `da8f66e` on the branch; report `reviewer-tools/pr64-adversarial.md`.
- Round 2, from me: gates at `ebabd18` are green (lint 0, typecheck 0, **177 files / 3,998 tests**). **0029 whole-trigger removal: 11/11 killed by named tests, BASE survived** (`reviewer-tools/pr64/round2/`).
- The narrow second reviewer was launched and **had not reported at handoff; assume lost.**
- **Next:**
  1. Launch one Opus agent with `reviewer-tools/prompts/agents/pr64-round2-narrow.md`.
  2. When it returns, verify its Highs yourself by re-running its named probe script.
  3. Post the verdict. If it clears, merge with `GATE` and `ALLOW_CONTEXT_ONLY=1` if needed (§4).
- **Watch one design point:** round 2 now refuses `I received a Western offer` unless the program is named. Decide whether that over-refuses and whether Sid is told nothing was recorded. If Jarvis is silent, that is a Medium for Sid ("no homework", but he must not lose an offer silently).

### Things that are NOT open work

Do not reopen or relaunch any of these: #52–#58 and the #59/#60/#61/#63/#67/#68/#69/#70 rounds. Their evidence is all on reviewer-tools.

---

## 2. What Sid is waiting on (bring only these to him)

1. **Switching the database changes on for real, then deploying.** This is Sid-attended and the next big step.
   - The reviewer must **never** apply a migration or deploy (memory `reviewer-may-merge.md`).
   - When Sid says he's ready, walk him through `docs/runbooks/deploy.md` section "R0 item 5: migrate, then deploy" one step at a time, PowerShell 7, `cd C:\javis`, with the `$wrangler`/`$gateway` variables from the top of that file:
     1. Record the D1 recovery point.
     2. `migrations list` for production `jarvis`. It must show exactly `0016`–`0028`, plus `0029` if #64 merged first; stop on anything else.
     3. His confirmation.
     4. `migrations apply`.
     5. List again.
     6. `./scripts/deploy.ps1 -Publish`.
   - **If #64 merges before the apply, first run the scratch rehearsal once more (§5)** so `0029` is proven on remote D1 too.
   - Calling stays off after this deploy, because Twilio is not configured.
2. **Calling go-live**, after (1), Sid-attended. Runbooks: `docs/runbooks/device-key-replacement.md`, `owner-phone-enrollment.md`, `owner-passphrase.md`, `voice-smoke.md`. It needs Twilio setup, which Sid does.
3. **Delete the old probe database `jarvis-probe-caseraise`** (Sid's; this session did not create it). PowerShell 7: `cd C:\javis` then `npx.cmd wrangler d1 delete jarvis-probe-caseraise`.
4. **Later, not now:** the v1.6 calendar-view decision and the optional UptimeRobot monitor.

Sid's two questions this session are answered. Do not ask them again:
- **Builder sessions:** "whatever saves the most tokens", so a fresh session every task (§6).
- **Rehearsal:** "if that's the best idea then sure", so the reviewer may run the **scratch** rehearsal itself, on throwaway databases only.

---

## 3. How to review and post (unchanged loop, exact mechanics)

1. **Gates at the exact head**, in a gate checkout (§4): `pnpm.cmd lint`, `pnpm.cmd typecheck`, `pnpm.cmd test`. Rerun any lone failing file alone before calling it unrelated.
   - Known non-issues: `call-session-do.test.ts` and `voice-owner-call-step-up.test.ts` 5-second timeouts under load; both pass alone.
2. **Migrations:** `node gen-trig.mjs <sql> <out.json> <sha> <checkout> <repo-relative sql> <test,files>`, then `node mutrun.mjs <out.json>`, then `node killcheck.mjs <run.txt>`. Every trigger must be a **named** kill, with BASE surviving.
3. **Code guards:** hand-write `{id,file,from,to,tests}` mutations. `from` must match exactly once; mutrun normalizes CRLF. **Test paths must exist**: a wrong path silently runs the wrong files, and everything "survives".
4. **Second reviewer:** only on the first max review of migration, security or R1 PRs. Re-reviews get a narrow brief. Docs-only PRs get none. **Verify every agent High yourself** before posting.
5. **Verdict entry:** write a markdown file.
   - Heading: `## YYYY-MM-DD HH:MM UTC — Claude Opus 5, PR #N … : cleared | changes requested`. Take the time from `date -u`; never estimate.
   - Put it first on the **PR branch**: make a temporary worktree of the branch, run `node agentlog-top.mjs docs/AGENT_LOG.md entry.md`, commit (message ends with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`), push, then remove the worktree. Cross-cutting notes go on `main` the same way.
6. **Fix round:** write a prompt file and launch a fresh builder (§6).
7. **Tell Sid** in one short bold line with PR links, then update the status page (§7).

---

## 4. Tools (all on `claude/reviewer-tools` under `reviewer-tools/`)

Copy them into your scratchpad first:

```bash
S="<your scratchpad>"; cd "<your worktree>"; git fetch origin
for f in mutrun.mjs gen-trig.mjs killcheck.mjs agentlog-top.mjs agentlog-merge.mjs merge-cleared.sh queued-relay.sh; do git show origin/claude/reviewer-tools:reviewer-tools/$f | tr -d '\r' > "$S/$f"; done
mkdir -p "$S/relay" "$S/work"; export RELAY_SCRATCH="$S"
```

- **Gate checkouts** are `C:\Users\Sid\jarvis-pr39` and `C:\Users\Sid\jarvis-pr40`. Both are worktrees of `C:\javis` with `node_modules`. Check them out with `git checkout --detach <sha>`.
  - `mutrun.mjs` requires a clean tree and returns it to main afterwards.
  - At handoff: pr39 is at `ee261a6` and pr40 is at `6a7f2a3`, both clean.
  - **Run one mutation run at a time, and never in a checkout another run is using.**
- **`merge-cleared.sh <pr> <branch> <reviewed-sha> <entry.md>`** (needs `RELAY_SCRATCH`):
  - posts the clearance entry on the branch;
  - merges `origin/main` into it, resolving AGENT_LOG-only conflicts by prepending branch entries via `agentlog-merge.mjs`;
  - requires the non-log diff to equal the reviewed diff (zero-context patch-id);
  - pushes, marks ready, merges with `--merge --match-head-commit` (retries 4 times), and checks the main tree equals the merged head.
  - Prefix `GATE=/c/Users/Sid/jarvis-pr39` (or pr40) for **every code PR**: it runs the full suite on the merged tree first and refuses on failure.
  - Prefix `ALLOW_CONTEXT_ONLY=1` only after you have reviewed a builder's own merge-main commit. It accepts when every file's `--numstat` equals the cleared diff.
  - A **non-log conflict aborts** (rc 2). Then launch a fresh builder with a merge-only prompt, copying `reviewer-tools/prompts/pr59-mergemain-prompt.txt`.
  - Always check the script's rc before any follow-up.
- **`queued-relay.sh <name> [<name>…]`** launches builders; see §6.

---

## 5. The scratch-D1 rehearsal (the reviewer may run it; throwaway databases only)

**Proven today on real remote D1**, final full run at main `4e21369`:
- baseline `0001`–`0015` replayed 15/15;
- seed OK;
- candidates `0016`–`0028` applied 13/13 over pre-existing rows;
- triggers 231/231;
- unique guards 4/4;
- CASE/RAISE rejected;
- scratch deleted.

Logs are in `reviewer-tools/rehearsal-2026-09-16/` (account id redacted). The record entry is `ce2b1ee` on main. Scratch names used: `jarvis-scratch-rehearsal-0916a`…`0916h`. **All deleted; always confirm with `node node_modules/wrangler/bin/wrangler.js d1 list` from a checkout.**

**How to run it exactly as written:**
1. In a gate checkout at the commit to rehearse, run `node reviewer-tools/rehearsal-2026-09-16/build-driver.mjs <checkout> jarvis-scratch-rehearsal-<new unique suffix> <scratchpad>/driver.ps1`. It turns the runbook's PowerShell blocks into one script. The only deviations: `Read-Host` answered automatically, `cd` replaced, and `-y` on the final delete.
2. Run `pwsh -NoProfile -File <scratchpad>/driver.ps1 *>> <log>` from the checkout, in the background. It takes about 5–7 minutes.
3. Success is these lines in order: `SCRATCH CREATE OK`, `SCRATCH BASELINE OK: 15/15`, `SEED CHECK OK`, `SCRATCH MIGRATIONS OK: N/N`, `TRIGGER CHECK OK: X/X`, four `UNIQUE GUARD OK`, `UNIQUE ROW OK`, `CASE RAISE CHECK OK`, `SCRATCH DELETE OK`, `SCRATCH CONFIG DELETE OK`, then exit 0.
4. **On any failure the scratch database is left behind.** Delete it: from the checkout, `node node_modules/wrangler/bin/wrangler.js d1 delete <that exact scratch name> -y`, and remove its `%TEMP%\jarvis-<name>-*.toml` config. Record the defect and start again with a **new** name.
5. Never pass a production name. The runbook refuses any name without `scratch`; keep it that way.

**Rule learned the hard way:** call Wrangler as `node node_modules/wrangler/bin/wrangler.js …` (PowerShell: `& node $wrangler …`), never `pnpm.cmd exec wrangler`. `pnpm.cmd` drops empty arguments and mangles newlines and quotes.

---

## 6. Builders (Codex CLI on Sid's PC)

- **Launching:** write the task to `$RELAY_SCRATCH/relay/<name>-prompt.txt`, then run in the background: `bash "$RELAY_SCRATCH/queued-relay.sh" <name>`.
  - Add `CODEX_ARGS='-c model_reasoning_effort="high"'` for docs, test-only or merge-only rounds.
  - Code, migration and security rounds use the default (GPT-5.6 Sol xhigh).
  - It runs `codex exec -` from `C:\Users\Sid\OneDrive\Documents\ChatGPT\jarvis`, allows at most 3 `codex.exe` at once, and retries capacity errors. The log is `relay/<name>.log`, the output `relay/<name>-attempt1.log` (tail it for the builder's summary and "tokens used").
- **Always a fresh session.** Never `codex exec resume` / `lane-relay.sh`. Measured today: resumed tasks cost 299k–1,073k tokens; fresh ones 43k–90k (small) or 180k–230k (code).
- **Prompt shape:** copy one from `reviewer-tools/prompts/`, for example `pr61-round3-prompt.txt` or `pr71-round2-prompt.txt`. Every prompt:
  - starts "You are a Jarvis build chat … in a fresh session";
  - tells it to create its own worktree next to the shared folder;
  - lists **only** the files to read;
  - states the exact fixes;
  - requires lint, typecheck and the full suite once;
  - requires a ready entry at the top of `docs/AGENT_LOG.md`;
  - ends with the standing rules: never merge, deploy, apply migrations, touch secrets, spend or contact anyone.
- **Builders die when Sid's PC turns off overnight.** Next morning, check for partial worktrees under `C:\Users\Sid\OneDrive\Documents\ChatGPT\` (`git worktree list` from that `jarvis` folder) and write a resume prompt that points at the leftover worktree.
- **After a builder finishes:** read its ready entry, then review. You get a task notification when the background command exits; no watcher is needed.

---

## 7. Talking to Sid and the status page

- **Replies:** very short, bold first line, every PR as a link `[#N](https://github.com/ksid1229-ops/jarvis/pull/N)`, no internals, no narration.
- **Commands:** name the shell and give `cd` first.
- **Visuals:** Sid cannot see inline widgets (`show_widget`). **Visuals go on the status page artifact** https://claude.ai/artifact/XjrTCjXg4o6rBEzi7t89f5.
- **Updating the page from a new conversation:**
  1. Read it with the Artifact tool, `action: "read"`, that `url`.
  2. Edit the HTML.
  3. Publish with the same `url`.
  - The latest source is also at `reviewer-tools/session-2026-09-16/status-board-artifact.html`.
  - Keep the version ladder at the top (segments: live, built, review, builder working, your step, not started), the "Needs you" box, and the PR ledger.
  - Keep the "PC build plan (paused)" section unchanged (memory `sid-pc-upgrade-talk.md`), and never put it in the repo.
- **Version ladder numbers at handoff:**
  - R0 7/7 live;
  - v1.0 code 4/4, Sid's go-live steps 0/4;
  - v1.1 memory 6/11 built (#62 with builder/reviewer);
  - v1.2 school 6/7 built (#64);
  - study coach 2/5;
  - v1.3–v1.8 not started (v1.6 has its plan merged).

---

## 8. Lessons from this session (do not relearn them)

- **Run a procedure before clearing it.** #60 passed four reading reviews and still had six real defects.
- **Merging a higher-numbered migration first creates a gap.** Rehearsal tooling now reports gaps instead of stopping.
- **Merging onto a moved main can break the migration lists in `remote-d1-migration-syntax.test.ts`.** Always merge with `GATE`.
- **Windows checkouts are CRLF** (`core.autocrlf=true`). Test regexes must accept `\r?\n`.
- **Check `rc` before bookkeeping after a pipe.**
- **Don't trust a quiet watcher.** Builder launches notify on exit; that is enough.
- **Some early AGENT_LOG timestamps this session were estimates ahead of UTC.** Use `date -u`.

---

## 9. PR #71 verification at handoff time

(filled in below at the moment of handoff)

— Claude Opus 5
