# Reviewer handoff, 2026-09-15 (the previous session's context was full)

## Roles and working rules
- **Roles.** Claude Opus 5 reviews and Codex chats build. Sid merges and runs every production action.
- **Reviewing a PR.** Run the local tests plus valid mutations, and use one Opus adversarial agent for big PRs. Post the verdict to `docs/AGENT_LOG.md` on the PR branch without asking.
- **Talking to Sid.** Short replies with a bold answer first and clickable PR links. Give him paste-ready Codex messages, and keep one status page.
  - Status page: https://claude.ai/code/artifact/f8eddfb6-11ab-4e9e-99d7-906d993eb888. Its source is `reviewer-tools/jarvis-status-board.html`; republish it with that URL.
  - Memory Plan page: https://claude.ai/code/artifact/5ef1176e-b047-4365-ad6b-bd49f8bc0526.

## State
- **Main:** `2619f02`.
- **Merged tonight**, each identical to the reviewed tree: #35 (`fb7c864`), #36 (`fcd55ef`), #37 (`2619f02`) and #38 (`b8b47bd`).
- **Production is unchanged:** gateway `28109492`, D1 migrations through `0015`.

## Open PRs
- **#39, memory schema `0016` (memory chat): changes requested, round 3,** reviewed at `c34ab17`.
  - S1: the implicit rowid lets `INSERT OR REPLACE` delete guarded rows on 17 TEXT-PK tables. `pr39-nf1-probe.test.ts` proves it. Fix: `WITHOUT ROWID` plus a generic REPLACE sweep test.
  - S2: a topic-sibling REPLACE.
  - S3: rules can future-date an expiry.
  - S4: the ledger accepts a future `occurred_at`.
  - Already verified: 75 of 75 trigger removals are genuine kills. `pr39-h2-probe.test.ts` must keep failing.
  - Before any production apply, Sid must attend a scratch remote-D1 proof (`jarvis-scratch-0016-proof`). The reviewer prepares the PowerShell steps once the SQL clears.
  - The N6 ingress allowlist becomes a separate `0019` PR after #39.
- **#40, call step-up `0018` (calling chat): changes requested.**
  - B1: REPLACE on the bindings, windows and repeat-check tables. A bindings REPLACE can mint owner authority through the waiver branch.
  - B2: the alarm key is deleted before handling.
  - B3: the waiver ignores a disabled verifier.
  - S1: repeat suppression misses a split final.
  - S2: voice-gate tests time out at 5 s.
  - The trigger-removal run is incomplete. Rerun `mut40b-triggers-q1/q2.json`, gateway tests only, on the fix head.
- **Migration numbers:** `0016` is #39, `0017` is merged, `0018` is #40, and `0019` is reserved for the N6 allowlist. Before accepting any number, check the migration files and AGENT_LOG on every open PR branch.

## Tools and test copies
- **Tools** (all in `reviewer-tools`):
  - `mutrun.mjs`, `agentlog-insert.mjs`, `agentlog-union.mjs`;
  - `killcheck.mjs`: count a kill only when a test fails, never when all tests were skipped, and hand-check the "other" kills;
  - `gen39b.mjs` and `gen-trig.mjs`: generate clean trigger-block removal specs;
  - the probes.
- **Test copies:** `C:\Users\Sid\jarvis-deploy` (main), and `C:\Users\Sid\jarvis-pr40`, a git worktree of jarvis-deploy with `node_modules` installed.

## Lessons
- Keep each background mutation call to about 20 runs. A roughly 60-minute call was killed partway.
- Run heavy suites one at a time. Concurrent runs produce 5–15 s timeouts that look like kills.
- The archival tail-read and voice guest-PIN log tests are load flakes. Confirm any failure in isolation.

## Sid
- No third build chat for now. Flaky-test cleanup goes to the calling chat after passphrase PR 3.
- Extra Opus agents are fine at medium effort when genuinely useful.
- Decisions pending from Sid: the R1 caller-ID spoofing and outbound voicemail gaps (see memory), and attending the scratch remote-D1 proof after #39 clears.

## First steps for the next reviewer
1. Read this file and the memory checkpoint.
2. Run `gh pr list`.
3. Check the #39 and #40 heads and their newest AGENT_LOG entries.
4. Re-review whichever PR has been resubmitted.
