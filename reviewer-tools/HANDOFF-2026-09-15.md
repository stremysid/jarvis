# Reviewer handoff — 2026-09-15, about 03:30 UTC

**Read `REVIEWER-MANUAL.md` first.** It explains how this role works: who's who, the loop, the message formats, and the review method. This file covers only the state at handoff. Every SHA below was checked with `git` or `gh` when this was written. Where something is unknown, it's listed under "Known unknowns"; don't fill those gaps by guessing.

---

## 1. Where things stand

- **`main` is `2619f02`** (the #37 merge).
- **Merged on Sep 13–15.** Each was verified identical to the tree the reviewer cleared:
  - [#29](https://github.com/ksid1229-ops/jarvis/pull/29) enrollment options: `f2424f5`
  - [#30](https://github.com/ksid1229-ops/jarvis/pull/30) device-key replacement runbook: `ca51a9b`
  - [#31](https://github.com/ksid1229-ops/jarvis/pull/31) owner phone enrollment: `8ae758a`
  - [#32](https://github.com/ksid1229-ops/jarvis/pull/32) retire legacy PIN: `8150e36`
  - [#33](https://github.com/ksid1229-ops/jarvis/pull/33) owner passphrase design (docs): `726b78b`
  - [#34](https://github.com/ksid1229-ops/jarvis/pull/34) enrollment follow-ups: `52f0881`
  - [#35](https://github.com/ksid1229-ops/jarvis/pull/35) memory pure logic: `fb7c864`
  - [#36](https://github.com/ksid1229-ops/jarvis/pull/36) R2 memory design docs: `fcd55ef`
  - [#38](https://github.com/ksid1229-ops/jarvis/pull/38) memory follow-ups F1–F4: `b8b47bd`
  - [#37](https://github.com/ksid1229-ops/jarvis/pull/37) passphrase PR 1 (verifier, migration 0017, CLI): `2619f02`
- **Production is unchanged.**
  - Gateway deployment `28109492` runs `fd39301`. D1 has migrations through `0015`.
  - `0017` is merged but not applied.
  - Nothing is live: calling is off, Twilio isn't configured, and the owner phone isn't enrolled.
- **The R1 rollout comes later, and Sid attends every step:** settings, deploy, the #30 key replacement, then #31 enrollment.
  - Inbound calling stays closed until the passphrase runtime ships (#40 plus PR 3).
  - Sid alone applies migrations, and only after review. `0016` also needs the scratch proof first (§2).
- **Migration numbers:**
  - `0016`: #39
  - `0017`: merged
  - `0018`: #40
  - `0019`: reserved for the memory chat's events-ingress allowlist PR after #39
  - Next free: `0020`. Always check every open PR branch before accepting a number.
- **Status page:** #39 and #40 both show "Needs fixes". Page: https://claude.ai/artifact/XjrTCjXg4o6rBEzi7t89f5

---

## 2. [#39](https://github.com/ksid1229-ops/jarvis/pull/39): memory schema `0016` (memory chat)

- **Branch:** `codex/r2-memory-schema-0016`.
  - The head is `c34ab17`, the reviewer's round-3 entry.
  - The last reviewed code is `8b62e80`.
  - The migration is `apps/cloud-gateway/src/persistence/migrations/0016_cloud_memory.sql`.
- **Rounds so far:**
  1. Round 1 at `4f2c1c0`: changes requested, B1–B7 (entry `0d3c03a`).
  2. Round 2 at `eb70b70`: changes requested, R1–R5 (entry `2d21c7b`).
  3. Migration-collision note: `4cbcd82`.
  4. Round 3 at `8b62e80`: changes requested (entry `c34ab17`).
- **Round-3 blockers** (full text in the entry and in `notes-2026-09-15/rev39c.md`):
  - **S1 (NF1, High):** 17 STRICT tables with TEXT primary keys keep a hidden `rowid`, so `INSERT OR REPLACE … (rowid, …)` deletes guarded rows. Runtime-proven by `pr39-nf1-probe.test.ts`. Fix: `WITHOUT ROWID`, plus one generic REPLACE sweep test over every `0016` table.
  - **S2 (NF2):** a topic rename, move or merge done by REPLACE deletes a same-named empty sibling. Fix: a sibling-name collision check in those branches.
  - **S3 (NF3):** rules can future-date an expiry to escape the owner lock. Fix: bound `occurred_at` (≤ now + 5 min, ≥ the current state's `updated_at`), and require `valid_to <= now`.
  - **S4 (L4):** ledger time is bounded only from below. Fix: bound `memory_runs.started_at` and the ledger `occurred_at` to near now.
- **Already proven at `8b62e80`:**
  - `pnpm test` passed 2,886 of 2,886. pytest: 878 passed, 32 skipped. Ruff and mypy are clean.
  - 75 of 75 trigger removals were genuine kills.
  - The H2 fix holds.
- **Status:** Sid sent the round-3 paste. The memory chat hasn't pushed fixes yet.

**Re-review, when the chat posts "ready":**
1. Run the full TypeScript and Python checks (manual §4).
2. Copy `pr39-h2-probe.test.ts` and `pr39-nf1-probe.test.ts` into `apps/cloud-gateway/test/persistence/` of the test copy. **Both must FAIL.**
3. Confirm the generic REPLACE sweep test exists and covers every table: an explicit rowid, the natural key, and `UPDATE OR REPLACE` of each key column and rowid.
4. Run trigger removals with `gen-trig.mjs` on the new `0016_cloud_memory.sql`. The test file is `apps/cloud-gateway/test/persistence/cloud-memory-migration.test.ts`. Use chunks of about 19, then `killcheck.mjs`.
5. Optionally, one Opus re-verify agent on S1–S4 and any new holes (prior reports: `pr39-reverify.md`, `pr39-reverify2.md`).
6. **If cleared:** the verdict goes to AGENT_LOG, and Sid is asked to merge. Merging applies nothing.
7. **Before any production apply,** Sid attends a **scratch remote-D1 proof**. The reviewer prepares exact PowerShell 7 steps from `notes-2026-09-15/remote39.md` and the Codex entry at "2026-09-15 01:31 UTC" on the #39 branch:
   - a new database `jarvis-scratch-0016-proof`, through a separate Wrangler config with no production binding;
   - apply 0001–0015, then 0016, with `--remote`;
   - capture the schema inventory and `PRAGMA recursive_triggers`;
   - run the owner-command, REPLACE, stale-replay, deep move/merge and cycle probes;
   - keep redacted receipts, then delete the database;
   - **never** run `wrangler d1 export`.
8. **After #39,** the memory chat opens the `0019` PR for N6:
   - an allowlist of `event_type`/`source` values at ingress;
   - binding owner-command operands (state, version, merge target, destination topic, suppress range), not just the target id.

   See N6 in `pr39-reverify.md`. The R2 item after that isn't decided in any note. Read `NEXT_STEPS.md` and `docs/plan/2026-09-14-r2-memory-design.md`, and have the memory chat propose it in AGENT_LOG.

---

## 3. [#40](https://github.com/ksid1229-ops/jarvis/pull/40): owner call step-up `0018` (calling chat)

- **Branch:** `codex/r1-owner-passphrase-step-up`.
  - The head is `587d884`, the reviewer's correction entry.
  - The reviewed code is `6b63d08`.
  - The migration is `apps/cloud-gateway/src/persistence/migrations/0018_owner_call_step_up.sql` (9 tables, 30 triggers).
- **Reviewer entries:**
  - `8b56b13`: max review, changes requested.
  - `587d884`: correction. The trigger-coverage paragraph was template text; **no valid trigger-removal results exist for 6b63d08.**
- **Blockers** (full text in `adv40.md`; the report is `pr40-adversarial.md`):
  - **B1:** `INSERT OR REPLACE` on `owner_call_step_up_bindings` (insert guard at SQL lines 104–141), `owner_call_step_up_windows` (155–175) and `owner_call_step_up_repeat_checks` (410–429). A binding REPLACE to `waived_passed_a` mints owner authority through the waiver branch of `call_session_authorities_require_current_lineage` (495–498). Fix: existing-key and rowid rejection or `WITHOUT ROWID`; read-then-insert in `bind()`/`begin()`/`expire()`; a REPLACE sweep test.
  - **B2:** `CallSessionDO.alarm()` deletes `OWNER_STEP_UP_ALARM_KEY` (`call-session-do.ts` 1634) before `handleOwnerStepUpAlarm`, so a throw loses the 60 s hang-up. Fix: delete after success, idempotent handling, and a throwing-first-attempt test.
  - **B3:** the waiver branch ignores a disabled or unconfigured passphrase head (`0018` 495–498, `call-session-do.ts` 919–931, the waiver path in `voice-access-repository.ts`). Fix: require an active head with a current verifier.
  - **S1:** a split-final repeat after "Verified." can bypass repeat suppression (`owner-call-step-up.ts` 249–251).
  - **S2:** the voice gate is timing-sensitive. On an idle machine it passed 806 of 811: 4 × "Test timed out in 5000ms" plus one cascade. The same 4 files passed 70 of 70 alone.
  - **Lows:** listed at the end of `adv40.md`.
- **Tests at `6b63d08`:**
  - Typecheck, voice typecheck and lint pass.
  - Codex claimed 2,752 of 2,752 and 811 of 811. The reviewer got 806 of 811 on the gate (S2).
- **Status:** Sid confirmed he **sent** the paste below to the calling chat. Waiting on its fixes.

The paste Sid sent, for reference:
```text
PR #40 review is in docs/AGENT_LOG.md on your branch (reviewer commit 8b56b13): changes requested. Fix B1 (INSERT/UPDATE OR REPLACE on the 0018 bindings, windows and repeat_checks tables; a bindings REPLACE can mint owner authority through the waiver branch. Use WITHOUT ROWID plus existing-key/rowid insert guards, pin key columns on updates, and add one REPLACE sweep test), B2 (alarm() deletes the alarm key before handling it, so a throw loses the timeout), B3 (the Passed-A waiver ignores a disabled verifier), S1 (split-final repeat leak) and S2 (voice-gate tests hitting the 5 s timeout). Pull first, then push fixes and request re-review.
```

**Re-review, when the chat posts "ready":**
1. In `C:\Users\Sid\jarvis-pr40` on an idle machine, run `pnpm.cmd lint`, `typecheck`, `test`, `typecheck:voice-access` and `test:voice-access`. S2 is fixed only when the gate reaches 811 of 811.
2. **Trigger coverage** (never measured yet):
   - Run `git show origin/codex/r1-owner-passphrase-step-up:apps/cloud-gateway/src/persistence/migrations/0018_owner_call_step_up.sql > 0018.sql`.
   - Then `node gen-trig.mjs 0018.sql mut40c.json <fix sha> C:/Users/Sid/jarvis-pr40 apps/cloud-gateway/src/persistence/migrations/0018_owner_call_step_up.sql apps/cloud-gateway/test/persistence/owner-call-step-up-migration.test.ts,apps/cloud-gateway/test/voice/call-session-do.test.ts`.
   - Split it into chunks of about 15 and run them one at a time. Then `killcheck.mjs`; 0018 trigger names rarely match test titles, so hand-check "other" kills.
   - Add the fake voice test files only if their BASE passes without timeouts.
3. **Write probes** that assert the bugs; each must FAIL on the fix:
   - REPLACE switching a `required` binding to `waived_passed_a`;
   - REPLACE extending a window deadline;
   - REPLACE resetting a repeat check;
   - a first alarm attempt that throws, where the call must still end;
   - the waiver with the head disabled, which must mint no authority;
   - a split-final repeat that must never reach model input or the transcript, unless the limit is documented and tested.
4. **Apply each #33 contract gap patch** from `claude/r1-call-safety-research:docs/reviews/2026-09-14-pr33-tests/*.patch` to the fix head. The tests must fail for every patch. There is no record that the reviewer ran these against #40; Codex claims its contract rejects the thin stub.
5. Check that each low in `adv40.md` is fixed or deliberately deferred, and say which.
6. **If cleared:** Sid merges. The calling chat's next task is **passphrase PR 3**: `/disable-owner-step-up --confirm` through Telegram (re-enable only by a new signed-CLI generate), guest-grant notices, and the 750 ms voice retrieval timeout. After PR 3, the calling chat also fixes the flaky tests (§4).

---

## 4. Sid

- **Waiting on him now:** nothing. Both chats have their fix messages.
- **Later, and only he can do these:**
  - merging #39 and #40;
  - attending the scratch remote-D1 proof;
  - the R1 rollout, and applying `0016`–`0018` in production;
  - the UptimeRobot external monitor (`NEXT_STEPS.md`, R0 item 7, still owner-blocked).
- **Already decided — don't ask again:**
  - **Spoken owner passphrase** on every inbound and outbound owner call, with the Passed-A waiver built but off (`DECISIONS.md`).
  - **Memory model:** DeepSeek with a $5/month cap. The setting is provider-agnostic, with a warning before credit runs out. When credit runs out he switches to Claude or GPT and sets the new cap himself. Reprocessing has its own one-time approved budget.
  - **No third build chat.** Flaky-test fixes go to the calling chat after PR 3. The known flakes are archival tail read, fake voice guest-PIN logs and guest activation.
  - **GitHub one-way export of memory** (Obsidian-style): he said "sure". It's built after core memory, excludes health, money, passwords and other people's details, and he does the GitHub access click later.
  - **The first official call is an onboarding call** (memory `jarvis-onboarding-call`).
- He's on medication for wisdom-teeth pain. Carry on as normal and don't mention it.

---

## 5. Places

- **Tools, specs, probes and reports:** branch `claude/reviewer-tools`, folder `reviewer-tools/` (`README.md` lists everything). The full chronological history of the last two reviewer sessions is `notes-2026-09-15/reviewer-log-2026-09-14.md`.
- **Test copies** (worktrees of `C:\javis`):
  - `C:\Users\Sid\jarvis-deploy`: detached on `2619f02`, clean.
  - `C:\Users\Sid\jarvis-pr40`: detached on `6b63d08`, clean.
  - Throwaway, removable with `git -C C:/javis worktree remove <path>`: `C:\Users\Sid\jarvis-pr31-adv2`, `C:\Users\Sid\jarvis-pr33-tests`.
- **The last reviewer's own worktree:** `C:\javis\.claude\worktrees\handoff-documentation-c01991`. Nothing there needs keeping.
- **Pages:**
  - Status: https://claude.ai/artifact/XjrTCjXg4o6rBEzi7t89f5 (also https://claude.ai/code/artifact/f8eddfb6-11ab-4e9e-99d7-906d993eb888)
  - Memory Plan: https://claude.ai/artifact/CiyukSCFtUpHe9J4Uc3PZo
- **Memory:** `C:\Users\Sid\.claude\projects\C--javis\memory\`. Start with `jarvis-reviewer-role.md`, then read every file listed in `MEMORY.md`.
- **Repo docs:**
  - `CLAUDE.md`, `docs/BUILDING.md`, `docs/HANDOFF.md`, `NEXT_STEPS.md`, `KNOWN_ISSUES.md`, `DECISIONS.md`
  - R2 design: `docs/plan/2026-09-14-r2-memory-design.md`
  - Passphrase design: `docs/superpowers/specs/2026-09-14-owner-call-passphrase-design.md`
  - Caller-ID and voicemail research: `docs/research/2026-09-14-callerid-spoofing-options.md`, `docs/research/2026-09-14-outbound-voicemail-options.md`

---

## 6. Known unknowns (don't guess; find out or ask)

- **#39 merge versus scratch proof:** no note settles whether the merge waits for the proof. The recommendation above (merge applies nothing; the proof comes before any production apply) is the reviewer's, not Sid's decision.
- **The next R2 item after `0019`** is undecided.
- **Calling unknowns** (from the caller-ID memory): the Twilio number's country, Sid's carrier, and how well speech-to-text catches the phrase at highway speed.
- **"eff" and "foe"** in Sid's topic-tree example: meaning unknown.
- **The rebuilt Sep 14 log:** `reviewer-log-2026-09-14.md` was rebuilt from the session transcript, and one small edit failed to replay, so the log may be missing one minor update.
