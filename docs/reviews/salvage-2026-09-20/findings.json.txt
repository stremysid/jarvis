workflow "jarvis-audit-w2-voice-channels" completed (9 agents).
Return value:
{
  "sha": "ca88bf49d42455dab25e5de8de794dfe20ff1f54",
  "batches": 9,
  "failed_batches": [],
  "files": [
    {
      "path": "apps/cloud-gateway/src/voice/call-session-do.ts",
      "lines_in_file": 2187,
      "lines_read": "1-2187 (all; read in three passes 1-1245, 1246-1945, 1946-2187)",
      "verdict": "issues",
      "findings": [
        {
          "title": "A second final transcript while a turn is still in flight throws turn_in_progress, and the Durable Object answers by closing the relay - the live call is dropped",
          "symbol": "CallSessionCore.#handlePrompt (apps/cloud-gateway/src/voice/call-session-do.ts:1420 as of ca88bf4), closed at CallSession.webSocketMessage (apps/cloud-gateway/src/voice/call-session-do.ts:2081-2085 as of ca88bf4)",
          "line_range": "1410-1435, 1436-1499, 2075-2086",
          "severity": "time",
          "what_is_wrong": "The guard that protects one turn from another rejects with a thrown Error instead of a returnable status, so the DO's single catch-all converts an expected overlap into a 1011 socket close that ends the call.",
          "mechanism": "The TwiML advertises overlap: interruptible=\"any\" reportInputDuringAgentSpeech=\"any\" (voice/twiml.ts:93). #handlePrompt admits the first final transcript and reserves ownership by setting #activeTurnAbort (call-session-do.ts:1442), clearing it only in the finally at :1497 after #conversation.handleTurn settles. A second final transcript arriving before that finally - including one sent straight after an interrupt, because the interrupt path aborts the turn but never awaits its settlement (:806-825, :1620-1625) - reaches `if (this.#activeTurnAbort !== null) throw new Error(\"turn_in_progress\")` (:1420). handleRelayEvent propagates the throw to CallSession.webSocketMessage, whose catch-all (:2081-2085) calls closeSocket(socket, 1011, \"relay processing failed\") for every error it has not already marked policy-closed. Nothing at that boundary distinguishes turn_in_progress from a fatal fault. Durable Object events interleave at await points, which this file's own comment (:1440-1441, \"a competing prompt\") and its concurrency tests assume.",
          "what_it_breaks": "The ConversationRelay session ends in the middle of an answer: the sentence being spoken is cut off, the caller's second question is discarded without ever reaching the model, and the call has to be dialled again. It fires whenever Sid or a guest asks a follow-up before Jarvis finishes answering - the exact behaviour the TwiML is configured to allow - and it looks to the caller like a dropped line rather than a policy refusal.",
          "evidence": "read",
          "fix": "Separate expected session conditions from faults at the DO boundary: give the Core a typed condition (e.g. throw a SessionBusyError) and in webSocketMessage drop the overlapping prompt and keep the socket, closing only on unexpected errors; alternatively have #handlePrompt return a status that handleRelayEvent ignores. Add a test that drives two prompt frames through CallSession.webSocketMessage (not CallSessionCore.handleRelayEvent) with a blocked capacity guard and asserts the socket stays open. No migration.",
          "falsifier": "If the Durable Object runtime serialises webSocketMessage handlers so a second frame cannot run while the first handleTurn is awaited, #activeTurnAbort is always null at :1420 and the path is unreachable; the observation that would show it is a test dispatching two prompt frames through the DO with the first turn deliberately blocked: if the second is queued until the first settles instead of throwing, the finding is wrong."
        },
        {
          "title": "The third wrong guest PIN durably rejects the call but never tells the caller or ends the relay - the leg stays open and silent",
          "symbol": "CallSessionCore.#authenticateGuest (apps/cloud-gateway/src/voice/call-session-do.ts:1556-1559 as of ca88bf4) with CallSessionCore.#transition (apps/cloud-gateway/src/voice/call-session-do.ts:1698-1714 as of ca88bf4)",
          "line_range": "1537-1578, 1698-1714, 2125-2127",
          "severity": "spend",
          "what_is_wrong": "A terminal guest rejection runs no delivery: no speech, no end frame, no close, so the caller is left on an open, silent call until they happen to speak again (which then closes it) or hang up.",
          "mechanism": "When the proof is null, #authenticateGuest maps the attempt through evaluatePinAttempt with failedAttempts = attemptOrdinal - 1 (:1557); inbound-auth.ts:261-279 returns terminateCall once the third failure is counted. It then calls #transition(\"rejected\", observedAt) (:1558) and returns. #transition writes D1, bumps #lifecycleGeneration and clears in-memory state, but never touches #relay - there is no sendNeutralText, no relay.end and no relay.close. The core stays in #cores with phase \"rejected\"; the socket is only closed later, by the next inbound frame, when #resolveCore returns mismatch because canResumeRejectedOwnerStepUp is false for interaction kind guest_pin (:2125-2127, :2183) and webSocketMessage closes it with 1008 (:2067-2070). A caller who simply waits hears nothing. Contrast the owner path, which speaks OWNER_STEP_UP_REJECTED and sends the end frame (:1217-1226), and the enrollment path, which at least speaks (:1599).",
          "what_it_breaks": "A guest who mistypes the PIN three times gets total silence: they cannot tell rejection from a dead line, they keep talking into a call that will never answer, and Twilio bills the open leg until the caller or the carrier gives up. Sid cannot explain the dead call afterwards, and the recorded phase gives no hint that the caller was never told.",
          "evidence": "read",
          "fix": "Make the terminal path deliver: on the terminating guest decision, speak a fixed rejection line and relay.end/relay.close (as the owner rejection does) before or alongside #transition, or make #transition (or a subscriber to it) close the relay whenever the next phase is in TERMINAL_PHASES. Extend test/voice/call-session-do.test.ts:1389 (\"rejects the call after three complete bad guest candidates\") to assert the relay was ended or closed, not only instance.phase. No migration.",
          "falsifier": "If the Twilio ConversationRelay session closed itself when call_sessions.phase becomes rejected, or if a test shows a final text frame or a close after the third bad candidate, the finding is wrong; check any assertion on relay.finish/relay.close/closeSocket in that test - there is none today."
        },
        {
          "title": "Code, not Jarvis, silently discards the owner's spoken turns: for two seconds after the passphrase match every owner utterance is dropped, and for 1.5 s more any utterance built from passphrase-list words is swallowed",
          "symbol": "CallSessionCore.#guardOwnerRepeat (apps/cloud-gateway/src/voice/call-session-do.ts:1121-1127 as of ca88bf4), reached from CallSessionCore.#handlePrompt (apps/cloud-gateway/src/voice/call-session-do.ts:1412-1415 as of ca88bf4)",
          "line_range": "1121-1175, 1410-1415",
          "severity": "roadmap-violation",
          "what_is_wrong": "A content filter inside the call state machine decides which of the owner's words Jarvis is allowed to hear, and in its worst branch it drops the utterance outright (return null) without consulting the text at all and without replying to the caller.",
          "mechanism": "The D1 trigger owner_call_step_up_attempts_publish_match stamps owner_call_step_up_successes.verified_at at the moment the passphrase verifies (migrations/0018_owner_call_step_up.sql:260-266, from owner-call-step-up.ts:234-243). repeatStatus returns \"guard\" while now <= verified_at + 2000 ms (owner-call-step-up.ts:315-325). #handlePrompt pushes every owner utterance through #guardOwnerRepeat (:1412-1413); on \"guard\" it returns null for ANY text (:1124-1127, no content test), and #handlePrompt returns at :1415 - no model call, no spoken fallback, no persisted turn. Between 2000 ms and 3500 ms (\"fragment\") an utterance consisting only of 1-2 words from the passphrase word list is also held back and returns null (:1142-1154, owner-passphrase-verifier.ts:169-181). The window opens at the match, i.e. before \"Verified.\" is even spoken, so it covers exactly the moment the owner starts his follow-up. This is a third copy of the repeat-suppression rule alongside OwnerCallStepUpService.repeatStatus/verifyRepeat and the SQL 2-second guard at migrations/0018:433-448.",
          "what_it_breaks": "Sid says the passphrase, hears \"Verified.\", immediately says a short follow-up (\"Stop\", \"Wait\"), and Jarvis neither answers nor records anything - there is no transcript row to explain the silence, so it reads as Jarvis ignoring him. It also breaks the Core Rule directly: an if in the gateway decides what is relevant and what Jarvis is permitted to hear, rather than exposing the decision where Jarvis can make it.",
          "evidence": "read",
          "fix": "Move the decision out of the transport: either suppress only text that could be the passphrase (run ownerPassphraseFragmentWordCount first and apply the word-list test inside the \"guard\" window too), or pass the utterance to Jarvis with a passphrase-suppression tool and let it decide. Whatever is dropped must be answered with a neutral spoken line so the caller is never left in silence. Test change: a case asserting what the owner hears when a 3-word non-passphrase utterance lands 1 s after verification. No migration.",
          "falsifier": "If the first post-verification final transcript reliably arrives more than 3500 ms after verified_at (transcription plus reply latency always exceeding the window) and is never composed solely of 1-2 word-list words, the practical impact is nil. Measure the arrival time of the first final prompt after a matched passphrase in a staging call; a value above 3.5 s falsifies the break."
        },
        {
          "title": "The termination phase ladder in the Durable Object is unreachable from production and disagrees with the live socket-close mapping for the same event",
          "symbol": "CallSession.#terminalizeDurableSession (apps/cloud-gateway/src/voice/call-session-do.ts:1990-2031 as of ca88bf4) against CallSessionCore.handleSocketClose (apps/cloud-gateway/src/voice/call-session-do.ts:1635-1658 as of ca88bf4)",
          "line_range": "1990-2031, 1627-1665",
          "severity": "dead-code",
          "what_is_wrong": "Two copies of the rule \"what terminal phase does this ending produce\" exist, they assign different phases to the same real event, and only the narrower one ever runs because the sole production caller pre-filters to already-terminal sessions.",
          "mechanism": "The ladder maps created/connecting/pre_auth -> rejected and active/authenticated -> ending -> completed (:2005-2012), with an explicit failed request forced to failed (:2004-2006). Its only production caller is the callback recorder (production-routes.ts:79 -> http/voice-callback-recorder.ts:196), and finishSession throws callback_terminal_state_missing unless D1 already holds completed/failed/rejected/expired (voice-callback-recorder.ts:189-191); so the loop's first iteration always returns at :1998-2003. The ladder therefore runs only from tests. The live mapping is handleSocketClose (:1635-1658): a hangup while active or authenticated is written ending then completed, but a hangup while created, connecting or pre_auth - or while activationOnly is still pending - falls to the else at :1657 and is written failed, where the ladder would have written rejected. Nothing else in apps/cloud-gateway/src reads call_sessions.phase to consume the difference (grep: only countActiveSessions and the recorder).",
          "what_it_breaks": "The durable cause of an ended call depends on which rule is live: the socket path records a caller who hung up during authentication as \"failed\", i.e. a system failure, while the companion rule calls the same event \"rejected\". The dead ladder also means the DO's advertised ability to terminalize a session a provider says has ended cannot be reached by the callback path - the recorder refuses first - so the two modules must be kept in step by hand, and relaxing the recorder guard (the obvious fix for a callback that arrives while D1 still says active) silently flips the recorded cause.",
          "evidence": "read",
          "fix": "Keep one mapping. Move the phase selection for a provider callback into a single function that both handleSocketClose and #terminalizeDurableSession call (for example \"no provider session binding and not authenticated -> rejected, active/authenticated -> ending->completed, requested failed -> failed\"), and either wire the callback path to use #terminalizeDurableSession (drop the recorder's terminal-only guard and let the DO recover a non-terminal session) or delete the ladder. Test change: assert the phase written when the socket closes during pre_auth equals the phase the termination ladder would write. No migration.",
          "falsifier": "A production caller that invokes CallSession.terminate on a session that is not already terminal would make the ladder live and reduce this to a mapping question; grep for terminateSession/terminate call sites outside the recorder (only production-routes.ts:79 today) would show it."
        },
        {
          "title": "#lastSentAssistantEventId is written and cleared but never read - the DO advertises a delivery receipt it does not have",
          "symbol": "CallSessionCore.#lastSentAssistantEventId (apps/cloud-gateway/src/voice/call-session-do.ts:701 as of ca88bf4)",
          "line_range": "701, 1487-1495, 1620-1625, 1679-1685",
          "severity": "dead-code",
          "what_is_wrong": "The field that records which assistant event the relay last spoke is assigned, reset and never consulted, so the state it holds has no effect on any behaviour.",
          "mechanism": "It is declared at :701, written once at :1492 when the conversation service reports outcome \"voice_sent\", and nulled in #cancelCurrentOutput (:1623) and #terminateOnce (:1684). A search of the whole file and of C:/javis finds no read of it: no receipt comparison, no retraction, no logging, no durable write. The test whose name suggests it covers this behaviour - \"never promotes an already sent voice turn to delivered history on a later interrupt\" (test/voice/call-session-do.test.ts:1895) - asserts against the conversation tables' sent_assistant_event_id / delivered_assistant_event_id columns, not against this field.",
          "what_it_breaks": "Nothing executes differently today, but the field claims a capability the object does not implement: the next reader (or the next fix to the interrupted-turn path) will assume the DO knows which assistant event was last spoken to the caller, and any behaviour built on that assumption will be built on a value nothing maintains. It is also dead state kept alive by three writes and covered by no assertion.",
          "evidence": "read",
          "fix": "Either delete the field and its three assignments, or complete the intended receipt: on interruption, compare the last spoken assistant event against the conversation record and use it (e.g. to refuse a duplicate finish or to expose the spoken event for reconciliation) with a test that fails when the value is wrong. No migration.",
          "falsifier": "Any read of #lastSentAssistantEventId - grep for the identifier outside lines 701/1492/1623/1684, or a caller that consumes a getter for it - falsifies the finding."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/voice/call-state.ts",
      "lines_in_file": 37,
      "lines_read": "1-37 (all)",
      "verdict": "issues",
      "findings": [
        {
          "title": "canPersistTurn has no production caller and encodes a stale copy of the rule the conversation module actually applies",
          "symbol": "canPersistTurn (apps/cloud-gateway/src/voice/call-state.ts:31-37 as of ca88bf4)",
          "line_range": "31-37",
          "severity": "dead-code",
          "what_is_wrong": "An exported predicate that decides which turns are worth persisting is imported by nothing in production, and its rule (committed state plus a delivered boolean) is not the rule the live voice path uses.",
          "mechanism": "A repo-wide search for canPersistTurn matches only apps/cloud-gateway/src/voice/call-state.ts:31, its unit test (test/voice/call-state.test.ts:3,79) and the 2026-08-29 plan document (docs/superpowers/plans/2026-08-29-jarvis-calling.md:152-166). No file under apps/*/src imports it, and the barrel/exports do not re-export it. The live rule for a voice turn is enforced elsewhere: the conversation tables carry state plus sent_assistant_event_id / delivered_assistant_event_id (test/voice/call-session-do.test.ts:2162 asserts exactly those columns), i.e. delivery is proven by an event receipt, not by a boolean argument. So the predicate is a second, unenforced statement of \"what is worth remembering\".",
          "what_it_breaks": "A future caller that wires this predicate would apply a rule production does not: a turn the conversation module would keep or drop on receipt evidence would be judged on a caller-supplied boolean instead, and the unit test that passes today certifies a rule nothing runs. It is also a judgement the Core Rule reserves for Jarvis (what is worth remembering) sitting unused in a state-machine module.",
          "evidence": "read",
          "fix": "Delete canPersistTurn and its test case, or move it to the one place that writes turns (the conversation repository) and derive `delivered` from the recorded event ids so the predicate and the persisted rule are the same code. No migration.",
          "falsifier": "Any production import of canPersistTurn (grep over apps/*/src and the worker entry points) would show the rule is live and reduce this to a naming question."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/voice/call-session-terminator.ts",
      "lines_in_file": 15,
      "lines_read": "1-15 (all)",
      "verdict": "issues",
      "findings": [
        {
          "title": "DurableObjectCallSessionTerminator has no production caller - production inlines a second copy of its body, so the adapter the acceptance harness tests is not the code that runs",
          "symbol": "DurableObjectCallSessionTerminator.terminate (apps/cloud-gateway/src/voice/call-session-terminator.ts:11-14 as of ca88bf4)",
          "line_range": "1-15",
          "severity": "dead-code",
          "what_is_wrong": "The class designated as the worker-side termination adapter is orphaned; the live path re-implements the same three steps inline in production route construction.",
          "mechanism": "production-routes.ts:79 builds the callback recorder with `terminateSession: (input) => session(input.sessionId).terminate(input)`, where `session = (id) => env.CALL_SESSION.get(env.CALL_SESSION.idFromName(id))` (:61) - idFromName, get, terminate, in the same order as call-session-terminator.ts:12-13. A repo-wide grep for DurableObjectCallSessionTerminator finds only tests/acceptance/fake/voice-relay-system.ts:308, which constructs it with a fake namespace whose get() routes terminate through runInDurableObject instead of the production stub. The plan that specifies this adapter (docs/superpowers/plans/2026-08-30-jarvis-owner-guest-voice-access.md:912) and the repository's own review (docs/reviews/2026-09-18-redesign-and-optimisation.md:193,200, \"no caller, no plan\") both name it as the intended production seam.",
          "what_it_breaks": "The acceptance harness exercises a construction production never uses: hardening or a defect added to the class cannot reach or be caught in production, and the inline lambda at production-routes.ts:79 (which also builds the DO name from a caller-supplied sessionId) has no test of its own. The worker's real termination path is validated only indirectly, through the DO's own D1 re-validation.",
          "evidence": "read",
          "fix": "Pick one: delete call-session-terminator.ts (the review's suggestion) or construct the recorder at production-routes.ts:79 with `new DurableObjectCallSessionTerminator(env.CALL_SESSION)` so the tested class is the live path. No migration.",
          "falsifier": "A non-test reference to DurableObjectCallSessionTerminator - a production import, a factory, or a build-time injection - would show production does use it; grep of C:/javis for the identifier returns only tests/acceptance/fake/voice-relay-system.ts:308 plus the class file itself."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/voice/inbound-auth.ts",
      "lines_in_file": 279,
      "lines_read": "1-279 (complete)",
      "verdict": "clean",
      "findings": []
    },
    {
      "path": "apps/cloud-gateway/src/voice/inbound.ts",
      "lines_in_file": 452,
      "lines_read": "1-452 (complete)",
      "verdict": "clean",
      "findings": []
    },
    {
      "path": "apps/cloud-gateway/src/voice/twiml.ts",
      "lines_in_file": 94,
      "lines_read": "1-94 (complete)",
      "verdict": "clean",
      "findings": []
    },
    {
      "path": "apps/cloud-gateway/src/voice/outbound.ts",
      "lines_in_file": 514,
      "lines_read": "1-514 (complete)",
      "verdict": "issues",
      "findings": [
        {
          "title": "Jarvis cannot place a call: the only issuer of an outbound command is the owner's own Telegram message",
          "symbol": "dispatchOutboundCall (apps/cloud-gateway/src/voice/outbound.ts:315-330)",
          "line_range": "315-330",
          "severity": "roadmap-violation",
          "what_is_wrong": "dispatchOutboundCall is the single entry point to the outbound dispatcher, and its authorization model admits only human-originated commands, so the roadmap's `call_place(reason)` tool has no seam to plug into.",
          "mechanism": "1) packages/contracts/src/calls.ts:43 types OutboundCallCommand.issuedBy as \"telegram_call_command\" | \"local_cli\" - there is no model member. 2) PolicyEngine.hasTrustedOrigin (src/policy/policy-engine.ts:225-230) requires a stored authenticatedOrigin(commandId) whose issuedBy matches that union. 3) The only production implementation of that port is D1TelegramCallCommands.authenticatedOrigin (voice/production-routes.ts:106), which reconstructs an accepted Telegram update whose text parses as /call <reason> --confirm (channels/telegram/telegram-call-command.ts:100-145). 4) No code in apps/ ever constructs issuedBy \"local_cli\" - it exists only in tests. 5) A Jarvis-issued command therefore fails at step 2 with invalid_origin before the dispatcher runs; policy-engine.test.ts:157 encodes exactly that denial. 6) docs/QUEUE.md:30 scopes the planned fix to composing the tool-calling agent into CallSessionCore, which would let a call act but still not place one.",
          "what_it_breaks": "Roadmap Phase 5 tells code to build `call_place(reason)` and Phase 6 gives Jarvis \"whether to text or call\" (docs/plan/2026-09-19-jarvis-roadmap.md:261,274). Today every outbound call requires Sid to type /call <reason> --confirm in Telegram, so Jarvis can never initiate one - including the case the roadmap calls out (\"when calling you is better than texting\"). It fires the first time Jarvis decides a call is the right move: there is no tool to call and no issuer value that would be accepted.",
          "evidence": "read",
          "fix": "Add a Jarvis-side origin provider beside D1TelegramCallCommands that mints {principalId: owner, issuedBy: \"model\", commandHash} from a persisted Jarvis decision, add \"model\" to the issuedBy union (contracts/calls.ts:43) and to the hasTrustedOrigin allow-list (policy-engine.ts:225-230), and route it through the existing tier gate (autonomy/tool-gate.ts) so a call remains an approved action. An additive migration is needed only if the decision receipt gets its own table - reusing the existing events/idempotency receipt path needs none. Test change: policy-engine.test.ts:157 currently asserts that a model-issued command is denied.",
          "falsifier": "If a model-originated command path exists that I did not trace (e.g. a signed local-agent request that causes an authenticatedOrigin row to be written), or the roadmap's call_place is meant to be reachable only from the owner's own confirmed command, this is not a violation."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/voice/outbound-recipient-lookup.ts",
      "lines_in_file": 25,
      "lines_read": "1-25 (complete)",
      "verdict": "issues",
      "findings": [
        {
          "title": "The recipient resolver is a third, stricter copy of \"which voice destination may be called\", so a policy-authorized guest call is dialed and then refused",
          "symbol": "D1OutboundRecipientIdentityLookup.resolveActiveVerifiedVoiceIdentityId (apps/cloud-gateway/src/voice/outbound-recipient-lookup.ts:9-24)",
          "line_range": "9-24",
          "severity": "dead-code",
          "what_is_wrong": "This lookup requires the destination identity to be status='active' with verified_at set, while the two other copies of the same rule accept a pending guest identity backed by an active grant - so the claim path cannot resolve the guests those paths authorize.",
          "mechanism": "1) policy-engine.ts:262-269 accepts destination.status IN ('pending','active') plus an existing voice_access_grants row, and policy-engine.ts:128 hands that resolved E164 to the dispatcher as destinationE164. 2) outbound-call-dispatcher.ts:304,314 dials exactly that E164 (To=destinationE164) with Url=<origin>/voice/outbound/{attemptId}. 3) The guest answers; Twilio POSTs the signed TwiML webhook; claimOutboundTwiML reads To and calls this lookup (outbound.ts:391-397). 4) This SQL filters i.status='active' AND i.verified_at IS NOT NULL; a pending guest identity (channel_identities rows are inserted 'pending' at voice-access-repository.ts:1045-1047 and only promoted to active at :1547) matches nothing, the lookup returns null, and `!safeAtom(observedIdentity)` returns 403 forbidden. 5) No TwiML, no relay: the call rings and dies, while the attempt row stays provider_dispatch_state='dispatched'. call-repository.ts:672-686 shows the guest branch was intended to work for pending grants.",
          "what_it_breaks": "The guest branch of CallRepository.claimExpectedCall and of PolicyEngine.resolveVoiceAccessDestination is unreachable through the only production resolver: Jarvis can never complete a call to a guest who was invited but has not yet dialed in and activated (the moment a grant is 'pending'). It cannot fire today only because the sole production command hard-codes the owner identity (telegram-call-command.ts:143); the first guest-call feature built on the existing allow verdict will ring the guest and hang up with a 403 that nothing surfaces to Sid or to the model.",
          "evidence": "read",
          "fix": "Make this predicate match the other two copies: accept i.status IN ('pending','active') and, for a non-owner identity, require EXISTS (SELECT 1 FROM voice_access_grants g WHERE g.identity_id = i.identity_id AND g.principal_id = i.principal_id AND g.status IN ('pending','active')) - or delete this copy and resolve through VoiceAccessRepository.resolveIdentityCandidate, which already encodes the owner/guest distinction. No migration: no schema change. Test change: add a pending-grant guest row to test/voice/outbound-recipient-lookup.test.ts (it currently only covers active/verified and disabled).",
          "falsifier": "If calling guests outbound is not a supported capability - i.e. the guest branches at policy-engine.ts:262 and call-repository.ts:672 are themselves dead - then this is only dead code, not a divergent rule; or if channel_identities rows for guests are created 'active' with verified_at set before any grant is used, the lookup would accept them."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/voice/owner-access-intent.ts",
      "lines_in_file": 77,
      "lines_read": "1-77 (every line)",
      "verdict": "issues",
      "findings": [
        {
          "title": "Code, not Jarvis, decides what an access instruction means, and a miss becomes an ordinary chat turn",
          "symbol": "parseOwnerAccessIntent — apps/cloud-gateway/src/voice/owner-access-intent.ts:45 as of ca88bf4",
          "line_range": "45-77",
          "severity": "roadmap-violation",
          "what_is_wrong": "A hand-written regex grammar decides whether the owner's utterance is an access command and which operation, target and permissions it names — the roadmap gives interpretation of the owner's words to Jarvis (roadmap:32, and the model test at roadmap:41-46: a model can read bad spelling and infer intent) — and when the grammar misses, that same utterance is quietly handled as conversation.",
          "mechanism": "`#handlePrompt` is the only production entry (call-session-do.ts:1421-1426): it calls parseOwnerAccessIntent(promptText) and on a non-null draft begins the access flow; on null it falls through to `#conversation.handleTurn` (1466). The grammar accepts four fixed shapes — allow <E164> with <phrases>, replace permissions for, rotate pin for, revoke — plus the literal string 'list allowed callers', and requires the number to appear in the transcript as a literal +-prefixed E.164 (58-73). Any other phrasing ('let Dana call me', a number transcribed as words, a phrase such as 'texts' absent from PERMISSION_CAPABILITIES) returns null at line 76 with no state change and no feedback to the owner.",
          "what_it_breaks": "Sid's instruction is filed as a chat message. No proposal is created, no access change happens, and the model — which is never told the sentence was a command it cannot execute — is free to answer as if it complied; nothing tells Sid it was not understood. A guest keeps or loses access while Sid believes the opposite. Fires on the first phrasing outside the four shapes, which is most of them.",
          "evidence": "read",
          "fix": "Let Jarvis own the interpretation: expose the owner-access operations as tools (one per operation, capability phrases as tool parameters) and let the model call them, keeping the code-side validation and the confirm step. If the grammar must stay as a stopgap, a parse of an owner utterance that looks like an access instruction but fails must produce a spoken 'I did not understand an access command' instead of falling through, so the model can never claim an access change. No migration.",
          "falsifier": "A production caller of these commands other than call-session-do.ts:1422 — e.g. a Telegram command handler owning the same verbs — would weaken the silent-fallthrough claim; grep over the repo shows the only non-test call site is that line."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/voice/owner-access-service.ts",
      "lines_in_file": 871,
      "lines_read": "1-871 (every line)",
      "verdict": "issues",
      "findings": [
        {
          "title": "Revoking a caller burns the number: it can never be allowed again",
          "symbol": "OwnerAccessService.prepare — apps/cloud-gateway/src/voice/owner-access-service.ts:623 as of ca88bf4",
          "line_range": "619-638",
          "severity": "mislead",
          "what_is_wrong": "The target lookup returns the newest grant for a phone whatever its status, so once a grant is revoked every operation on that number is refused — add because a target exists, replace/rotate/revoke because the target is revoked — and the schema's UNIQUE(channel, provider_subject) (0001_foundation.sql:48) means no fresh identity can be created for it either.",
          "mechanism": "prepare calls getGuestGrantByProviderE164 (line 625), which is GRANT_SELECT ... WHERE identity.channel='voice' AND identity.provider_subject=? ORDER BY grant_row.grant_version DESC LIMIT 1 with no status filter (voice-access-repository.ts:768-778). revokeGrant keeps the row at status='revoked' with grant_version=expected+1 (1280-1284) and disables the identity row (1285-1288), so the revoked grant is still the newest match for that phone. Line 628 throws when a target exists (add); lines 632 and 636 throw when the target is revoked (replace_permissions, rotate_pin, revoke). listGuests filters status IN ('pending','active') (1325-1326), so 'list allowed callers' hides the row doing the blocking. test/security/owner-access-security.test.ts:239-284 pins the refusal, so it is deliberate but unrecoverable.",
          "what_it_breaks": "After Sid revokes a guest he cannot let that person back in from any Jarvis surface: every attempt ends in owner_access_target_unavailable, which no layer maps to speech, so the relay socket is closed 1011 and the call ends. The only recovery is hand-written SQL against production D1 with the event and grant rows in the way. Fires the first time he changes his mind.",
          "evidence": "read",
          "fix": "Decide the product behaviour, then make it work: either treat a revoked grant as absent for 'add' (new grant_id and identity while honouring the existing UNIQUE(channel, provider_subject) row) or reactivate the existing grant at a new version with a fresh PIN, and show revoked callers (masked) in 'list' so the block is visible. Any migration is additive only, and must not put a semicolon inside a `--` comment because the test splitter divides on it.",
          "falsifier": "If getGuestGrantByProviderE164 filtered grant_row.status <> 'revoked', or if a production path other than OwnerAccessService.prepare could call createGuestGrant for an existing phone, the door would not be shut."
        },
        {
          "title": "The phrase-to-capability table is code-owned meaning, and all but one of its phrases fail in production",
          "symbol": "PERMISSION_CAPABILITIES and OwnerAccessService.#snapshot — apps/cloud-gateway/src/voice/owner-access-service.ts:109 as of ca88bf4",
          "line_range": "109-127, 579-603",
          "severity": "roadmap-violation",
          "what_is_wrong": "Code, not Jarvis, maps the owner's spoken words to capabilities, and the table advertises sixteen capabilities the production registry cannot grant, so ordinary phrasing becomes an internal error rather than a tool call.",
          "mechanism": "#snapshot (585-595) special-cases the single phrase 'everything' and otherwise looks each phrase up in the frozen table at 109-127, throwing owner_access_permission_invalid for anything absent. The table has 17 keys over 16 capabilities (both 'destructive proposals' and 'destructive action proposals' map to destructive.propose) and includes 'access management'→access.manage. Production builds new CapabilityRegistry({ installed: ['conversation.basic','access.manage'] }) (production-runtime.ts:64), so registry.resolve throws capability_not_installed for the 14 phrases targeting uninstalled capabilities and capability_not_grantable for access.manage, which sits in OWNER_ONLY_CAPABILITIES (capability-registry.ts:36-42, 180). #snapshot's catch (600-602) flattens all of it into one message the owner never hears. Only 'conversation' resolves.",
          "what_it_breaks": "Sid can only grant 'conversation'; 'allow +1416... with calls', 'with file reading' or 'with access management' dies inside prepare. No prompt lists the valid phrases, so he cannot learn the vocabulary — the failure is discovered by the call dropping, and the model never gets a chance to map 'let him call me' onto calls.place.",
          "evidence": "read",
          "fix": "Move the interpretation to Jarvis: give the model the capability ids as a tool parameter and let it map the owner's words, keeping the table only as a validation set for what the model returns. At minimum remove 'access management' (it can never succeed) and make refusals a spoken outcome instead of a thrown error. No migration.",
          "falsifier": "If production constructed the registry with the full GUEST_CAPABILITY_IDS plus resource adapters, the table would mostly work and only the roadmap half of this would stand."
        },
        {
          "title": "Every owner-access refusal is an unhandled error that ends the call",
          "symbol": "OwnerAccessService throw sites vs CallSessionCore.webSocketMessage — apps/cloud-gateway/src/voice/owner-access-service.ts:147 as of ca88bf4",
          "line_range": "147-149, 466-471, 591-601, 628-637, 728-732, 852-854",
          "severity": "mislead",
          "what_is_wrong": "Nothing between the owner-access service and the relay turns an owner_access_* error into speech, so a refused, collided, expired or malformed access command closes the Durable Object's relay socket instead of telling the owner what happened.",
          "mechanism": "webSocketMessage (call-session-do.ts:2075-2085) awaits handleRelayEvent in a bare try whose only handler calls closeSocket(socket, 1011, 'relay processing failed'). #handlePrompt (1424, 1428, 1432) and #confirmOwnerAccess (1064-1078, which only clears state in finally) let every thrown error through. Throws: non-owner authority (571, 575), malformed input (147-149; capturePinSelection runs at 718 before execute's try at 720, so its TypeError is not even wrapped), a phrase that is unknown, uninstalled or ungrantable (591, 601), a target taken or revoked (628, 632, 636), an expired or already-consumed proposal (728, 732), any repository failure (852-854). Grepping 'owner_access_' in call-session-do.ts returns only interaction-kind names: no speech mapping exists.",
          "what_it_breaks": "An owner who pauses, repeats a proposal, names a capability that is not installed, or tries to re-add a revoked caller has the call terminated with no explanation, and cannot tell a refusal from a network fault. This is the failure mode that makes every other finding in this file worse than it reads.",
          "evidence": "read",
          "fix": "Give the DO a spoken mapping for the access-command error family — one fixed sentence per outcome, e.g. 'I could not change access: that caller cannot be added', 'that permission is not available here' — sent before the interaction is cleared, exactly as the unparseable-PIN path does at call-session-do.ts:1039, with the raw error text kept out of the speech. No migration.",
          "falsifier": "If ConversationRelay treated a 1011 close as recoverable for the owner, or if an outer adapter caught owner_access_* and rendered speech, the 'ends the call' claim would be wrong; the handler traced above is the only one on the relay message path."
        },
        {
          "title": "The 'production' access-document verifier has no production caller",
          "symbol": "createTargetGuestAccessDocumentVerifier and TargetGuestResourceScopeResolver — apps/cloud-gateway/src/voice/owner-access-service.ts:344 as of ca88bf4",
          "line_range": "252-342, 344-373",
          "severity": "dead-code",
          "what_is_wrong": "The function whose docstring calls it the 'Reconstructible production verifier binding one canonical document to the target guest's owned scopes' is constructed only in tests, and production never gives the repository a verifier or the service a scope resolver.",
          "mechanism": "production-runtime.ts:63 builds new VoiceAccessRepository(env.DB) with no hooks, so #accessDocumentVerifier stays undefined; #verifyAccessDocument (voice-access-repository.ts:731-753) then only re-hashes the row's own capabilityIds/resourceScopes and compares that with the row's own stored hash — true by construction — and throws voice_access_document_invalid for any grant carrying a resource scope or a resource capability (740-744). production-runtime.ts:80-86 passes no scopeResolver, so OwnerAccessService falls back to new TargetGuestResourceScopeResolver([]) (line 541), whose every owned list is empty. The only production registry installs conversation.basic and access.manage (production-runtime.ts:64). Grep over the repo finds createTargetGuestAccessDocumentVerifier only in tests and its own definition.",
          "what_it_breaks": "Calendar, file and PC capabilities can never be granted in production (registry.resolve throws capability_not_grantable, capability-registry.ts:184-185, and the empty resolver has no owned ids to select from), so the scoped-grant path the verifier exists to protect is unreachable; if such a grant ever reached D1 by any other route, every read of it fails closed, including the guest's own authentication. The docstring tells a maintainer the scoped path is wired when it is not.",
          "evidence": "read",
          "fix": "Either delete the resolver, the verifier and the scopeResolver dependency (production has no scoped capability and no resource adapters), or wire both: pass accessDocumentVerifier: createTargetGuestAccessDocumentVerifier(registry, resolver) to VoiceAccessRepository and the same resolver to OwnerAccessService in production-runtime.ts, with the configured calendar/file/PC ids. No migration.",
          "falsifier": "A production construction of VoiceAccessRepository with a hooks object, or an OwnerAccessService given a scopeResolver, would falsify this; grep over apps/cloud-gateway/src returns exactly one construction (production-runtime.ts:63) with no second argument, and one OwnerAccessService construction (line 80) with no scopeResolver."
        },
        {
          "title": "A 60-second timer, not Jarvis, decides how long the owner's pending decision lives",
          "symbol": "PreparedOwnerAccessProposal.expiresAt in OwnerAccessService.prepare — apps/cloud-gateway/src/voice/owner-access-service.ts:652 as of ca88bf4",
          "line_range": "643-653, 730-733",
          "severity": "roadmap-violation",
          "what_is_wrong": "The lifetime of a pending owner decision is a hard-coded 60 seconds chosen in code — the roadmap assigns 'how long a fact lasts' to Jarvis (roadmap:32, 70) — and when it lapses the owner gets no sentence, just a closed socket.",
          "mechanism": "prepare stamps expiresAt: new Date(nowEpoch + 60_000).toISOString() (652) and execute refuses anything at or past it (730-733), invalidating the proposal. The owner's path to confirmation is three relay round trips: say the command → hear 'Enter four digits ...' or 'Say confirm ...' (call-session-do.ts:1018-1028) → say or key the PIN → hear 'Say confirm to apply this access change, or cancel.' (1049) → say 'confirm'. Every step crosses Deepgram transcription, and #captureOwnerAccessPin re-prompts on speech it cannot parse (1038-1041). Passing the deadline throws owner_access_proposal_expired, which per the sibling finding ends the call rather than re-asking.",
          "what_it_breaks": "The owner loses the change and the call and must redial and re-speak the whole command. The bound is invisible and unconfigurable: no prompt says he is on a clock and no constant raises it. Fires whenever a confirmation takes more than a minute, which two voice round trips plus a PIN can.",
          "evidence": "read",
          "fix": "Either make the hold explicit and generous (extend the window to what the interaction justifies and say 'this expires in N seconds' in the prompt), or drop the wall-clock expiry and let the proposal live until the owner confirms, cancels, or the call itself ends — the call lifecycle is the natural bound and needs no invented timer. Keep the one-shot invalidation. No migration; the value is in-memory only.",
          "falsifier": "If the interaction were a single relay turn with no separate PIN/confirm round trips, or if the DO rebuilt the proposal on expiry instead of failing, 60 s would be harmless; the three-prompt sequence at call-session-do.ts:1018-1049 is what makes it reachable."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/voice/owner-call-step-up.ts",
      "lines_in_file": 557,
      "lines_read": "1-557 (every line)",
      "verdict": "issues",
      "findings": [
        {
          "title": "The owner's spoken passphrase reaches the model and the conversation store on its second repeat",
          "symbol": "OwnerCallStepUpService.repeatStatus and verifyRepeat — apps/cloud-gateway/src/voice/owner-call-step-up.ts:288 as of ca88bf4",
          "line_range": "288-333",
          "severity": "data-leak",
          "what_is_wrong": "The repeat filter whose whole purpose is to keep a repeated passphrase out of the conversation is one-shot per call: after the first check row exists repeatStatus returns 'spent', which the DO reads as 'stop filtering', so a later repeat of the same three words is sent to the model and stored as a conversation turn.",
          "mechanism": "#guardOwnerRepeat (call-session-do.ts:1121-1131) returns the text unchanged for any status other than guard/fragment/available. repeatStatus (315-333) returns guard for 2 s after verification, fragment until 3.5 s, and spent forever once a row exists in owner_call_step_up_repeat_checks; verifyRepeat writes that row on its first call (303-306) before it verifies, so even a non-matching candidate spends the claim. After that the utterance returns through #handlePrompt into #conversation.handleTurn (call-session-do.ts:1412-1414, 1466-1473). The only filter there is Redactor, which redacts credential-shaped field names (redaction.ts:9), not a passphrase. A repeat inside the 2 s guard is swallowed with no reply, which invites the owner to say it again.",
          "what_it_breaks": "canonicalizeOwnerPassphrase requires exactly three list words (owner-passphrase-verifier.ts:183-192) and ownerPassphraseFragmentWordCount accepts those same three words (169-181), so the passphrase is a 3-word voice secret and a repeated utterance is eligible. It is then sent to DeepSeek and written to the conversation tables, against the repo's never-list for credentials and PINs. Fires on the second deliberate repeat in a call, which the silent first suppression makes likely.",
          "evidence": "read",
          "fix": "Make suppression last for the whole call: once a repeat check exists, keep verifying passphrase-shaped utterances and discard matches rather than returning the text, or keep a per-session 'passphrase seen' flag that routes the utterance to a discard path — never to the conversation service. Add a test that a second repeat after a consumed check is not passed to handleTurn. No migration.",
          "falsifier": "If #guardOwnerRepeat re-verified while status is 'spent' (it does not: line 1128-1131 returns the text), or if the conversation service dropped owner turns equal to a recently verified passphrase, the leak claim is wrong."
        },
        {
          "title": "The repeat-check claim guard cannot fire: a duplicate insert throws a primary-key error that drops the call",
          "symbol": "OwnerCallStepUpService.verifyRepeat — apps/cloud-gateway/src/voice/owner-call-step-up.ts:303 as of ca88bf4",
          "line_range": "295-313",
          "severity": "dead-code",
          "what_is_wrong": "`claimed === null` is treated as 'another worker holds the claim', but owner_call_step_up_repeat_checks.session_id is a PRIMARY KEY, so a second insert raises a constraint error instead of returning no rows — the guard is unreachable and the loser's error propagates to the relay.",
          "mechanism": "verifyRepeat calls repeatStatus (295) — a D1 round trip, hence an await point — then runs INSERT ... RETURNING session_id (303-306). owner_call_step_up_repeat_checks.session_id is a PRIMARY KEY (0018_owner_call_step_up.sql:73), so a second insert raises 'UNIQUE constraint failed' rather than returning no row; the claimed === null fallback at 307 therefore never handles a conflict. Two owner prompts can be inside #guardOwnerRepeat at once: #handlePrompt checks #activeTurnAbort only afterwards (call-session-do.ts:1412-1420) and nothing guards the guard, so both can see 'available' and both insert. The loser's error leaves verifyRepeat and handleRelayEvent, and webSocketMessage closes the socket with 1011 (2081-2085). The concurrency trigger is inferred from those await points.",
          "what_it_breaks": "A duplicated or rapidly repeated owner utterance ends the call instead of being treated as a repeat, and because the claim is one-shot the losing verification never happened. Fires whenever two owner prompts overlap by one D1 round trip, which a second final transcript for the same speech makes normal on this channel.",
          "evidence": "read",
          "fix": "Make the claim idempotent — INSERT ... ON CONFLICT(session_id) DO NOTHING RETURNING session_id, so null really means someone else holds it — and treat a raised constraint error as 'continue' so a duplicate can never escape. Add a test that two concurrent verifyRepeat calls do not throw. No migration; the primary key already exists.",
          "falsifier": "If SQLite's INSERT ... RETURNING returned null instead of throwing on a primary-key conflict the guard would work as written; if the DO could not interleave two #guardOwnerRepeat calls the race would be unreachable, though the guard would still be dead code."
        },
        {
          "title": "A dead second copy of the guest PIN attempt budget",
          "symbol": "OwnerCallStepUpService.reserveGuestPinAttempt — apps/cloud-gateway/src/voice/owner-call-step-up.ts:391 as of ca88bf4",
          "line_range": "391-400",
          "severity": "dead-code",
          "what_is_wrong": "This method duplicates VoiceAccessRepository.reserveGuestPinAttempt statement for statement, but the DO calls the rep
… [truncated: 170128 more characters]
workflow "jarvis-audit-w1-memory-persistence" completed (17 agents).
Return value:
{
  "sha": "ca88bf49d42455dab25e5de8de794dfe20ff1f54",
  "batches": 17,
  "batches_returned": 17,
  "failed_batches": [],
  "files": [
    {
      "path": "apps/cloud-gateway/src/memory/memory-repository.ts",
      "lines_in_file": 4200,
      "lines_read": "1-2100 of 4200",
      "verdict": "issues",
      "findings": [
        {
          "title": "appendPin refuses on lifecycle state where its own comment promises a retrievability check, so a pin that can never appear is accepted and confirmed to Sid",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:1264 MemoryRepository.appendPin as of ca88bf4",
          "line_range": "1250-1284",
          "severity": "mislead",
          "what_is_wrong": "The comment at 1260-1263 states that pinning a non-retrievable memory is refused rather than silently ignored, but the guard at 1264 only tests `item.lifecycle.state !== \"active\"`, and `memory_retrievable_item_versions` (0016_cloud_memory.sql:942-977) proves an item can be active and still absent from every pinned read.",
          "mechanism": "1. Retrievability is defined by the view at 0016_cloud_memory.sql:942-977: state.lifecycle_state='active' AND the current version has at least one source AND no source of that version is covered by an active suppression AND the item's creation event is not covered by one. 2. An item can be active but fail the source clauses: two active memories can share a conversation turn as a source (the distiller commits one item per proposal, several proposals can cite the same event, automatic-distillation.ts:885-938), so forgetting one inserts suppressions keyed on the event/sequence range that also cover the other's copy of it. The codebase already knows this state exists: countSiblingItemsHiddenByForget (memory-repository.ts:1563-1641) counts exactly these items, and the lift receipt at memory-owner-controls.ts:1454 tells Sid 'Restored 1 memory ... but it is still hidden because another forgotten memory covers the same conversation turn'. 3. Only the forgotten item's lifecycle transitions, so the sibling stays lifecycle_state='active' and remains visible to readCurrentItem/readCurrentItemInternal; the existing caller redactUnretrievableItem (memory-owner-controls.ts:302-306) is applied to readCurrentItem output precisely because hidden-but-active is normal. 4. Sid now pins it: setPin (memory-owner-controls.ts:1470-1508) calls appendPin, readCurrentItemInternal returns the item, the check at 1264 passes, the INSERT at 1273-1281 appends a pin row, and the receipt at memory-owner-controls.ts:1513 says 'Pinned 1 memory; it goes in front of me in every conversation.' 5. Every consumer of pins reads memory_pinned_item_versions (0038_memory_lifetime_and_pins.sql:129-135), which INNER JOINs memory_retrievable_item_versions, so the row is filtered out and the memory never enters the core profile. 6. Nothing later reports the discrepancy: appendPin returns appended:true and the caller returns replayed:false.",
          "what_it_breaks": "Sid is told a fact is pinned and will be in front of Jarvis on every turn while it is in fact invisible. Fires whenever the pinned item is active but hidden - any source of its current version suppressed, or its creation event covered by an active suppression - which is the exact state the forget/lift paths already name in their receipts. The failure is silent and permanent until another suppression is lifted.",
          "evidence": "read",
          "fix": "In appendPin, replace the lifecycle test at 1264 with a retrievability test for the item's current version: either `const visibility = await this.readItemVisibility(principalId, itemId); if (!visibility.retrievable) refuse();` (readItemVisibility already exists at 1353 and is used by the same callers), or add an EXISTS against memory_retrievable_item_versions for state.current_version_id in the readCurrentItemInternal query. Test change: add a case that pins an item whose only source is suppressed and asserts MemoryRepositoryError('memory_refused'); no migration needed.",
          "falsifier": "Show that an item with lifecycle_state='active' can never be missing from memory_retrievable_item_versions - e.g. a trigger that transitions an item out of 'active' the moment a suppression covers its sources. I found no such trigger in 0016_cloud_memory.sql (the suppression triggers at 1589+ hide events, not state rows), and appendPin's own guard is the only check on this path."
        },
        {
          "title": "A batched retrieval read silently deletes archive-unavailable candidates, while the single-item read of the same memory fails loudly",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:1552-1559 MemoryRepository.readCurrentItemsWithVisibility as of ca88bf4",
          "line_range": "1424-1561",
          "severity": "mislead",
          "what_is_wrong": "When the archive circuit is open, the per-candidate catch at 1552-1557 converts the failure into `return null` and the filter at 1559 drops the candidate, so the call returns a normal-looking shorter list that neither caller can distinguish from 'that memory does not exist'.",
          "mechanism": "1. archiveUnavailable (509-511) matches errors whose message is exactly 'archive_circuit_open'; the archival layer throws that whenever archive_state.circuit_state <> 'closed' (archival-service.ts:155,169,182,188,210,235; archive-repository.ts:192,244; tiered-event-reader.ts:35). 2. That state is latched, not transient: archive-repository.ts:433-438 writes circuit_state='open' on a latched failure and no production code ever writes it back to 'closed' (the only resets are in tests, test/archive/archive-fixture.ts:23 and test/memory/telegram-memory.test.ts:489), so the condition persists until an operator edits the row. 3. For each requested item, the batched validation (validateCreation at 1481-1496 and the source validation at 1503-1519) verifies each source receipt through the injected ArchivedEventReader; an archived source needs an R2 read, which throws 'archive_circuit_open' while the circuit is open. 4. The catch at 1552-1557 sees that message and returns null for that item only; line 1559 filters nulls out. Other error classes still throw, so a corrupt row aborts the whole read while an unavailable archive quietly removes a row. 5. Both production callers index the result by item id and skip missing entries: telegram-memory-retriever.ts:1220-1224 (`const read = byItemId.get(candidate.itemId); if (read === undefined) continue;`) and telegram-memory-retriever.ts:1557-1558 (`if (read === undefined) return null;`). Neither compares the returned list against the requested ids, and no counter is returned. 6. The same memory read through readCurrentItem (1202-1208) has no such catch and surfaces memory_unavailable. The existing test at test/memory/memory-repository.test.ts:863-896 asserts the drop, so it is deliberate - but nothing upstream is told it happened.",
          "what_it_breaks": "While the archive circuit is open (a latched state that stays open until manually repaired), every memory whose evidence has been archived disappears from Jarvis's recall with no error, no log line and no counter, while memories with live evidence keep working. Jarvis answers as if those facts were never learned, and Sid cannot tell 'I do not know that' from 'the archive is down'. The single-item paths (explain, correct, pin) fail loudly for the same items, so the system gives two different answers about one memory.",
          "evidence": "read",
          "fix": "Make failure distinguishable from absence: return a result that names the unavailable candidates (e.g. `{ items, unavailableItemIds }`, or per-candidate `{ item: null, unavailable: true }`), and have the retriever surface 'some memories could not be checked' rather than continuing silently; alternatively let archiveUnavailable propagate so the batch path matches readCurrentItem. Test change: extend test/memory/memory-repository.test.ts:863 to assert the caller can observe the skipped item; no migration.",
          "falsifier": "A caller that compares the returned entries against the requested ids and reports the difference, or a repository path that signals unavailability separately - neither exists: grep for readCurrentItemsWithVisibility finds exactly two production call sites (telegram-memory-retriever.ts:1209 and 1544) and both skip missing entries silently."
        },
        {
          "title": "The duplicate-remember guard reads and normalises every active memory's full text on every remember, with no LIMIT and no supporting index",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:1218-1236 MemoryRepository.findActiveItemByNormalizedText as of ca88bf4",
          "line_range": "1211-1239",
          "severity": "spend",
          "what_is_wrong": "The guard SELECTs item_id and the full current text of every active item for the principal, with no LIMIT and no SQL-side comparison, then normalises each row in the Worker, so one remember command costs work proportional to the entire memory store.",
          "mechanism": "1. The query at 1218-1227 filters only item.principal_id and state.lifecycle_state='active'; there is no LIMIT and no ORDER BY-supported index. memory_items has PRIMARY KEY (item_id) only (0016_cloud_memory.sql:3-4) and the indexes created for memory are memory_item_versions_current_lookup, memory_item_sources_event_lookup, memory_event_suppressions_*, memory_topics_parent_lookup, memory_item_placement_topic_lookup, memory_history_coverage_range_lookup, memory_runs_job_started_lookup, memory_cost_ledger_month_lookup (0016:1045-1070) plus memory_item_pins_current (0038:137) - none on memory_items(principal_id, created_at), so SQLite scans memory_items and sorts by created_at. 2. `.all()` materialises every row into the Worker, each carrying up to 4096 bytes of text (safeRowText cap at 1232). 3. The loop at 1229-1236 then runs normalizedRememberText (418-426: NFC normalise, two regex replaces, toLocaleLowerCase('en-CA'), two more regex passes) on every row before comparing, and only the first match short-circuits. 4. The call site is the common path: memory-owner-controls.ts:659-661 runs it for every fresh remember that is not model-inferred, before the item is written. 5. The store only grows: the distiller commits items continuously (automatic-distillation.ts:929-938), memory_items is insert-only (immutability triggers 0016:1093-1103 forbid UPDATE and DELETE), and durable items never lapse, so nothing bounds the number of active rows.",
          "what_it_breaks": "Remember latency and D1 rows-read cost grow linearly and without bound with the memory store: with a few thousand active items each remember moves roughly (items x average text bytes) out of D1 - hundreds of KB to several MB - and burns Worker CPU on regex normalisation for a string equality test. It also makes every remember's latency depend on data written by an unrelated background job, so the command degrades over months of normal use.",
          "evidence": "read",
          "fix": "Push the comparison into SQL: store the normalised text (or a SHA-256 of it) on the version row, index (principal_id, normalized_text_hash), and query with `WHERE ... AND normalized_text_hash = ? LIMIT 1`; at minimum add a LIMIT after a SQL-side normalised match and an index on memory_items(principal_id, created_at) so the scan is index-driven. The stored-normalised-hash route needs an additive migration (nullable column + backfill + index); the index alone is additive and needs a migration too.",
          "falsifier": "If memory_items had an index on principal_id and the active set were bounded - e.g. an expiry job removing durable items, or a hard per-principal item cap - the scan would be a bounded cost rather than an unbounded one. Check that memory_items is insert-only (0016:1093-1103) and that no deletion path exists; if a pruning job exists outside this file, re-check this finding."
        },
        {
          "title": "refileAutomaticInboxItems decides in code which memories to move, how many, and in what order, and hard-codes a third copy of the filing-confidence threshold",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:1865-2008 MemoryRepository.refileAutomaticInboxItems as of ca88bf4",
          "line_range": "1865-2008",
          "severity": "roadmap-violation",
          "what_is_wrong": "A wake-up caller passes only a principalId and the repository alone decides which inbox items are worth moving (confidence >= 0.6, uncertain = 0, reason prefix), how many (10 per call), and which slice of them to look at (a wall-clock-hour-indexed rotation over at most 100 rows) - the 'what counts as relevant / whether to act / how many results' judgments the roadmap reserves for Jarvis (docs/plan/2026-09-19-jarvis-roadmap.md:19-34).",
          "mechanism": "1. Caller: AutomaticMemoryDistillationWorkflow.refileInboxItems (automatic-distillation.ts:707-715) passes only options.principalId, memoises one promise per workflow instance, and swallows every error into {examinedItemCount:0, refiledItemCount:0, failedItemCount:1}. Nothing in that call tells the repository which items matter or asks Jarvis anything. 2. Candidate selection is SQL in the repository: `event.confidence >= 0.6`, `version.uncertain = 0`, `state.lifecycle_state='active'` and a string-prefix match on the stored reason (1873-1894 and 1904-1938). The 0.6 literal appears three times in this file - 934 (automatic-filing validity in captureInput), 1891 and 1921 (refile candidacy) - and a fourth time as FILING_CONFIDENCE_THRESHOLD = 0.6 in automatic-distillation.ts:113, which is the value the model's filingConfidence is actually compared against at distillation time (automatic-distillation.ts:1266-1271). 3. Ordering and volume are also decided in code: `hourIndex = Math.floor(this.freshNow().valueOf() / 3_600_000)` picks a window offset (1901-1903), the window is capped at AUTOMATIC_INBOX_REFILE_CANDIDATE_LIMIT=100 (1931-1937) and at most AUTOMATIC_INBOX_REFILE_LIMIT=10 items are moved before the loop breaks (1943). 4. The model's topic decision for each item is already stored in the placement reason when the item was committed (automatic-distillation.ts:1364-1371, decision inbox_cap/inbox_filing_failure), so the code is replaying a stale decision made when the item was created, without giving the model a chance to revise it or Sid a chance to see it.",
          "what_it_breaks": "Sid's memories are re-filed into topic areas by a rule he can neither see nor veto, and only 10 per invocation are ever moved. Because the threshold exists as four independent literals, changing FILING_CONFIDENCE_THRESHOLD in the distiller (or retuning the model) leaves the refile gate at 0.6: items the model judged filing-worthy are then never refiled, or items it judged below threshold are. And because the decision 'is this worth acting on' lives in SQL, it cannot be changed by a prompt, which is the whole point of the roadmap rule.",
          "evidence": "read",
          "fix": "Turn refiling into a tool the model calls: a wake-up tells Jarvis 'N inbox items still have unresolved topic decisions' with the stored topicPath/confidence, and the model decides which to refile, in what order, and how many. If the rule must stay for now, at least export one shared constant for the confidence floor and import it in both files so the gate cannot drift from FILING_CONFIDENCE_THRESHOLD. No migration for the constant unification; a model-driven refile needs a tool definition and a test change.",
          "falsifier": "A production path that asks the model (or Sid) whether a given inbox item should be refiled before the INSERT at 1973-1989 - grep for refileAutomaticInboxItems finds only automatic-distillation.ts:708, which passes no item list and no threshold. If a model-driven refile tool exists elsewhere, this is enforcement of a decision already made, not a new decision."
        },
        {
          "title": "How long a fact lasts is decided by code when the caller omits lifetime, and the model is invited to omit it",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:889-896 captureInput as of ca88bf4",
          "line_range": "889-896",
          "severity": "roadmap-violation",
          "what_is_wrong": "`input.lifetime === undefined ? (validTo === null ? \"durable\" : \"temporary\") : ...` makes the repository decide durability - 'how long a fact lasts', explicitly listed as Jarvis's call in docs/plan/2026-09-19-jarvis-roadmap.md:32 - whenever the caller is silent; the same default is written independently in two other files.",
          "mechanism": "1. The production memory_remember tool schema makes lifetime optional (required list at memory-tools.ts:42 excludes it) and its description tells the model 'Leave it out and the fact is durable' (memory-tools.ts:50). 2. owner-telegram-agent.ts:1091 applies the same default (`args.lifetime === undefined ? \"durable\" : args.lifetime`) before calling the controls service; memory-owner-controls.ts:696 applies it a third time (`input.lifetime ?? \"durable\"`); the repository applies it a fourth time at 889-891. 3. Whatever comes out is then enforced as if it were a decision: the refusal at 896 requires durable <=> validTo === null, and the 0038 coupling trigger holds memory_items.lifetime to the version's valid_to (0038_memory_lifetime_and_pins.sql:30-54), so a fact the model failed to annotate is stored as permanent with no later point at which it can lapse. 4. The project states the opposite principle elsewhere in the same subsystem: automatic-distillation.ts:1318-1323 refuses to guess an expiry for extracted facts because 'answering it by guessing an expiry here would be code deciding what the roadmap gives to the model'.",
          "what_it_breaks": "A transient statement the model did not annotate - 'I am tired today', 'my exam is tomorrow' - is stored as a durable fact, retrieved forever and presented to the model as current, with nothing in the receipt telling Sid that the lifetime was assumed. Fires on any remember where the model omits lifetime and expiresAt, which the tool description explicitly permits.",
          "evidence": "read",
          "fix": "Make lifetime and expiresAt required in the memory_remember schema (memory-tools.ts:42) so the omission cannot occur, and delete the defaulting branches in captureInput (889-891) and the two callers so an absent lifetime is refused rather than assumed; keep at most one compatibility default for non-model callers if one is needed. Test change: a test that a CommitInitialMemoryInput without lifetime is refused. No migration.",
          "falsifier": "Evidence that the tool schema requires the model to state lifetime on every call (memory-tools.ts:50 says the opposite and describes the default), or a later code path that re-derives an expiry for durable items (none: memory_items is insert-only and nothing updates lifetime)."
        },
        {
          "title": "The duplicate guard decides in code that two statements are the same memory, merging the new wording silently and dropping it as a version",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:1233 findActiveItemByNormalizedText / normalizedRememberText at 418-426 as of ca88bf4",
          "line_range": "1211-1239",
          "severity": "roadmap-violation",
          "what_is_wrong": "Code, not Jarvis, decides whether a newly stated fact is 'the same memory' as an existing one by normalising case, apostrophes, zero-width characters and punctuation and comparing strings, then merges the new statement into the old item - a judgment a chat model performs trivially and which the roadmap gives to the model ('what's worth remembering').",
          "mechanism": "1. normalizedRememberText (418-426) maps seven apostrophe look-alikes to \"'\", strips zero-width characters, lowercases with 'en-CA' and collapses every run of non-letter/digit/apostrophe to a space; findActiveItemByNormalizedText compares that against every active item's stored text and returns the first match. 2. On a match, memory-owner-controls.ts:662-682 does not create a memory: it appends the new turn as an extra source to the old item (appendSourceToActiveItem) and returns replayed:true with the receipt 'That memory was already active, so I did not add a duplicate; I added Sid's new wording as evidence.' The stored version text therefore stays the first wording forever; the new sentence survives only as a source excerpt. 3. The decision is invisible to the model: the model proposed a new fact from the current message and is never shown the existing item it was folded into, so it cannot object or correct the wording. 4. Secondary effect of the mechanism: every restatement appends a source, and appendSourceToActiveItem refuses once the item has 8 sources (1315, mirroring the 8-source cap at 899), so the ninth distinct restatement of one sentence is a hard memory_refused instead of an idempotent success. 5. The same file's own evidence class for this decision is the model: the roadmap's test (docs/plan/2026-09-19-jarvis-roadmap.md:36-51) is 'if a chat model could do it, assuming every permission were granted, there is no need to code it'.",
          "what_it_breaks": "When the first wording of a memory is wrong or awkward, Sid can restate it as many times as he likes and the stored wording never changes; the retrieval text and every downstream prompt keep the old sentence, while the receipt implies his new words were recorded. He must know and use memory_correct to fix it. Fires on any restatement whose normalised form matches an existing active item.",
          "evidence": "read",
          "fix": "Take the decision out of code: expose the candidate memories to the model (a lookup tool or the retrieval context already carries item ids) and let it decide whether a new statement duplicates, extends or corrects an existing memory, exactly as memory_correct already invites. If a mechanical guard stays as a safety net, it should be a non-authoritative hint returned to the model, not a silent merge in the write path. Test change: assert that a restatement whose text differs only in punctuation surfaces the existing item to the caller instead of merging.",
          "falsifier": "A production path where the model sees the comparison before the merge (the call site at memory-owner-controls.ts:659-661 runs before any model turn on the new item and passes only principalId and text), or a statement in the docs that folding restatements into the original wording is the intended product decision."
        },
        {
          "title": "The provenance-flag guard in the owner-turn checks cannot fail: the only production builder writes constant false for all seven flags",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:2018-2029 MemoryRepository.validateOwnerTurn (and 2069-2078 readAcceptedOwnerTurn) as of ca88bf4",
          "line_range": "2010-2029",
          "severity": "dead-code",
          "what_is_wrong": "`flags.some(Boolean) -> refuse()` guards forwarded/quoted/pasted/attachment/model-generated/tool-generated/guest turns, but the only production constructor of MemoryOwnerTurnInput sets every one of those flags to literal false, so the guard cannot fire on any real request.",
          "mechanism": "1. Both production control entry points build their turn through readTelegramMemoryOwnerTurn: index.ts:299 (the owner agent's tool authority) and telegram-memory-controls.ts:365 (the memory-control model adapter), which calls the same function at owner-telegram-agent.ts:974-989. 2. That function (telegram-memory-controls.ts:236-303) verifies the request against the durable conversation turn and the committed envelope, requires payload.directOwnerText === true (line 285), and then returns a frozen object with forwarded/quoted/pasted/hasAttachment/modelGenerated/toolGenerated/guest all hard-coded false (lines 296-302). 3. The real provenance decision is made upstream at ingestion: telegram-types.ts:265-286 computes isDirectOwnerText from BORROWED_TEXT_KEYS (forward_origin, forward_from, via_bot, ...), QUOTED_TEXT_KEYS, attachment keys and blockquote/code entities, and that value becomes payload.directOwnerText. 4. There is no other production constructor: grep for 'guest:' and 'memoryIntent:' across apps/cloud-gateway/src finds memory-owner-controls.ts:238-244 (a pass-through validator that only type-checks the booleans) plus the telegram builder; every other construction is in test/.",
          "what_it_breaks": "The repository boundary cannot refuse the class of request it names, so the guarantee a reader would attribute to validateOwnerTurn ('a forwarded, pasted or guest turn is never a memory command') is provided only by the Telegram ingestion rule. Nothing misbehaves today because directOwnerText covers the same cases - but the fields are part of the exported MemoryOwnerTurnInput contract, are validated and threaded through memory-owner-controls, and any new channel that builds a turn without re-deriving provenance inherits a guard that always passes because the only worked example fills it with false.",
          "evidence": "read",
          "fix": "Either delete the seven flags and replace them with one server-derived provenance field the repository can check (the committed payload's directOwnerText), or have readTelegramMemoryOwnerTurn pass the real values from the verified envelope instead of constants, so the repository check is the one that can fail. Test change: a test that a turn whose committed envelope is not directOwnerText is refused by the repository rather than only by the adapter.",
          "falsifier": "A second production construction site that sets any flag from real message metadata - grep for forwarded:/pasted:/guest: in apps/cloud-gateway/src returns only telegram-memory-controls.ts:296-302 (constants), memory-owner-controls.ts:238-244 (pass-through) and tests. If such a site is added, the guard becomes live."
        },
        {
          "title": "readAutomaticTopicPromptTree silently truncates the area list handed to the distiller, so the model is told a partial tree is complete",
          "symbol": "apps/cloud-gateway/src/memory/memory-repository.ts:1843-1859 MemoryRepository.readAutomaticTopicPromptTree as of ca88bf4",
          "line_range": "1790-1863",
          "severity": "mislead",
          "what_is_wrong": "The tree is filled round-robin and every child that pushes the canonical JSON past AUTOMATIC_TOPIC_PROMPT_TREE_BYTES (4096, line 435) is silently popped, so callers receive a subset of the real topic tree with no indication that anything was dropped.",
          "mechanism": "1. bounded starts as every top-level area with an empty child list and is checked once against the 4096-byte cap at 1844. 2. The loop at 1845-1859 then pushes one child per area per round, recomputing canonicalJson over the whole structure after each push and popping the child when the total exceeds the cap; the result is returned at 1860-1861 with no truncated flag. 3. Reachability: the prompt query reads two levels (root -> area -> child, 1795-1823). Area count is bounded at 40 by AUTOMATIC_TOPIC_CHILD_LIMIT (431) enforced in resolveOrCreateAutomaticTopicPath at 1729, and each name is bounded at 64 UTF-8 bytes by AUTOMATIC_TOPIC_COMPONENT_BYTES (434) enforced at 710; 64 bytes per entry plus JSON overhead means the 4096-byte cap is crossed at roughly 55-60 areas and sub-areas, well inside the ~40 x 40 x 72 bytes the schema permits. 4. The caller passes the array straight into the prompt with the instruction 'existingTopicTree lists current area names as [area, [sub-areas]] ... Reuse a listed name when one fits' (automatic-distillation.ts:566, built into the prompt at 843) and treats it as the whole tree (fetched once per run at 827-836; when the repository method is absent the tree silently becomes [] instead).",
          "what_it_breaks": "Once Sid's areas pass the byte cap, the model sees a round-robin subset of them and re-invents near-duplicate names for areas it cannot see instead of reusing them, so the duplicate-area drift the prompt tree exists to prevent returns - silently, with the distillation run reporting success. The same method has the opposite defect at 1844: a base tree over the cap raises memory_corrupt for legitimate data, though that line looks unreachable while the area count stays under 40 with 64-byte names.",
          "evidence": "read",
          "fix": "Return the tree together with a truncation flag (or a count of omitted areas) and have the distiller tell the model the list is partial, e.g. append 'the area list is incomplete' to the instruction when truncated; alternatively bound by number of areas rather than bytes so the cutoff is at least stable and explainable. Test change: a test with a tree over the cap asserting the caller can observe the truncation. No migration.",
          "falsifier": "A caller that detects the truncation (automatic-distillation.ts:827-836 uses the value directly), or a bound that makes the cap unreachable - the prompt query reads at most two levels and both levels are capped at 40 entries in the automatic path, so the reachable size is tens of KB, well over 4096 bytes."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/memory/memory-repository.ts",
      "lines_in_file": 4200,
      "lines_read": "2101-4200 of 4200",
      "verdict": "issues",
      "findings": [
        {
          "title": "A child-count guard that silently inserts nothing turns an intended inbox fallback into a refused memory write",
          "symbol": "MemoryRepository.createAutomaticTopicStatement (apps/cloud-gateway/src/memory/memory-repository.ts:2836-2870, guard at 2854-2856; caller pre-check at 2974)",
          "line_range": "2836-2870",
          "severity": "mislead",
          "what_is_wrong": "The automatic topic-creation statement carries its own cap as `INSERT ... SELECT ... WHERE (SELECT count(*) FROM memory_topics child WHERE child.principal_id = ? AND child.parent_topic_id = ? AND child.status = 'active') < AUTOMATIC_TOPIC_CHILD_LIMIT` (line 2854-2856, limit 40 at line 431); when a concurrent writer has filled that parent to 40 in the window between the caller's `activeChildCount` read (line 2974) and the batch, the statement inserts zero rows without any error, the code never looks at the result, and the following statement in the same batch makes the whole capture fail instead of taking the `inbox_cap` fallback the same method already implements (lines 2930-2942, 2972-2976).",
          "mechanism": "1) `commitInitialItem` calls `prepareAutomaticCommit` (memory-repository.ts:1118), which resolves the model's proposed path and reads `activeChildCount(anchor)` at line 2974; the read returns 39, so it plans two new topics under that anchor and returns `plan.topicStatements` (lines 2985-2999). 2) Between that read and `this.transactions.batch(statements)` (line 1158) the code performs `validateSourceReceipts` (line 1135), which issues one D1 query per source and, for a stored-archived source, an R2 read (`validateArchivedEventEvidence` -> `readArchivedRange`, lines 3140-3148); there is no `blockConcurrencyWhile` anywhere in apps/cloud-gateway/src, and memory writes are plain D1 access from Worker request handlers, so a second capture for the same principal can interleave there. 3) That second capture creates one more child under the anchor, so at batch time the anchor has 40 active children; the first topic-event insert's WHERE is now false and inserts 0 rows, which fires no trigger and raises no error (the cap is not a DB constraint - grep of src/persistence/migrations shows AUTOMATIC_TOPIC_CHILD_LIMIT is enforced nowhere in SQL). 4) The second planned topic's parent is the topic that was never created, so `memory_topic_events_insert_guard` (0016_cloud_memory.sql:1883-1891, `NEW.new_parent_topic_id IS NOT NULL AND NOT EXISTS (parent active)`) RAISEs `memory_topic_event_invalid` (0016:2165); if only one new topic was planned, the item's own placement insert instead violates `memory_item_placement_events_insert_guard` (0016:2376-2384, new_topic_id must exist and be active) and RAISEs `memory_item_placement_event_invalid` (0016:2425). 5) D1 batches are transactional, so everything rolls back; `commitInitialItem` catches (line 1166), `inspectReplay` returns \"absent\" because nothing was written, and the retry loop (lines 1130-1180) re-plans and fails identically up to `maximumWriteAttempts`; `isConstraintRefusal` (lines 820-824) matches both trigger messages, so the caller sees `new MemoryRepositoryError(\"memory_refused\")` (line 1181). The same shape exists in the other caller, `resolveOrCreateAutomaticTopicPath` (pre-check at 1729, same helper at 1739).",
          "what_it_breaks": "A fact the owner just stated is never saved, and Jarvis is told the write was refused - `memory_refused` is the code the codebase uses for a policy refusal (a guard or a constraint on the request), so a caller cannot tell a full topic directory from a rejected request and will not retry or file it elsewhere. The design intent for exactly this situation is `inbox_cap`, i.e. still save the memory, in the inbox. It fires whenever a second capture lands between the child-count read and the batch while the anchor sits at 39 active children and the plan creates at least one child; the window is not sub-millisecond because it contains an R2 archive read per archived source.",
          "evidence": "read",
          "fix": "Close the read-to-write window instead of trusting the SQL guard. In `prepareAutomaticCommit`, replace the `>= AUTOMATIC_TOPIC_CHILD_LIMIT` test at line 2974 with `activeChildCount + missingCount > AUTOMATIC_TOPIC_CHILD_LIMIT` and return `inboxPlan(\"inbox_cap\", true)` (same for the pre-check at line 1729 in `resolveOrCreateAutomaticTopicPath`), and after the batch in `commitInitialItem` verify `plan.createdTopicCount` topics exist (`readTopicPath(principalId, plan.input.placement.topicId)` already corrupts on a missing topic, so call it before returning) or inspect `D1Result.meta.changes` for each `plan.topicStatements` entry and re-plan into the inbox when one is 0. No migration needed: keep the SQL guard as a backstop, but the code must stop treating a 0-row insert as success. A test change is needed - a seam that inserts a competing child between `activeChildCount` and `batch` should assert the item lands in the inbox and `automaticFilingCreatedTopicCount` is 0, not that the call throws `memory_refused`.",
          "falsifier": "If no second capture can run between the `activeChildCount` read (line 2974) and `transactions.batch` (line 1158) for one principal - e.g. every memory write is serialized by a Durable Object input gate or a lock I did not find - the guard can never be false and the finding collapses. Equally, if D1 `batch()` returns per-statement results instead of rolling back on the first trigger ABORT, the outcome would differ from the one described; I did not run D1."
        },
        {
          "title": "Lifting a forgotten memory silently rewrites its basis to \"confirmed\" once the archiver has sealed its sources",
          "symbol": "MemoryRepository.liftItem (apps/cloud-gateway/src/memory/memory-repository.ts:2326-2331, written at 2353)",
          "line_range": "2326-2353",
          "severity": "roadmap-violation",
          "what_is_wrong": "`liftItem` derives `restoredBasis = item.version.origin === \"authenticated_first_person\" && item.sources.every((source) => source.sourceLocation === \"archived\") ? \"confirmed\" : item.version.basis` and inserts the restored version with that basis, so an owner action that means \"make this visible again\" is recorded - and later reported to the model - as \"the owner confirmed this fact is true\", decided by code from a storage location rather than by Jarvis.",
          "mechanism": "1) Source locations are not stored facts on read: `readSources`/`validateSourceRows` derive `sourceLocation = currentR2SegmentId === null ? storedSourceLocation : \"archived\"` from a LEFT JOIN onto `archive_segment_events` (lines 3735-3736 and 4060-4061), so the moment the archive cursor seals an event every source of the item reports \"archived\" even though the source row still says \"live\". 2) `liftItem` reads the item through `readCurrentItemInternal` -> `readSources` (line 3845) and therefore takes the `\"confirmed\"` branch as soon as the archiver has run, keeping the text, `uncertain` and `origin` unchanged and writing `basis = restoredBasis` into the new `memory_item_versions` row (line 2353). The comment at 2326-2327 states the reasoning (\"the owner's lift is the confirmation that lets the active transition retain that evidence\"): the value is chosen to satisfy the transition trigger's owner+archived exception (0016_cloud_memory.sql:1442-1478, which requires `version.basis = 'confirmed'` for an owner transition to `active` when no live first-person source exists). 3) Nothing tells the caller: the lift receipt (`LiftMemoryItemResult`, lines 2434) has no basis field, and `validateCanonicalRow` passes the stored basis straight through (line 3943). 4) The value reaches the model: `composeMemorySearchResults` renders `const certainty = result.uncertain ? \"unconfirmed\" : result.basis` into the `memory_search` block the model reads (apps/cloud-gateway/src/memory/memory-search.ts:383-387), so for an active item (uncertain = 0) the line changes from e.g. `stated` to `confirmed`.",
          "what_it_breaks": "Certainty is Jarvis's decision in the roadmap (Phase 2: \"Whether it was stated or inferred\"; \"certainty, attribution\" is listed among the things everything else is Jarvis's), and code here mints the strongest value on the scale from a background storage event. Concretely, the same fact restored by the same owner command is reported as `stated` before the archive cursor passes its events and `confirmed` after, with no owner statement in between - so Jarvis, and any receipt derived from basis, treat a merely un-forgotten fact as owner-confirmed. The flip is invisible in every receipt and depends on archiver timing, so it cannot be reproduced or audited from the conversation.",
          "evidence": "read",
          "fix": "Stop deriving basis in `liftItem`: keep `item.version.basis` and make the database accept an owner lift without a live source on its own terms - extend the exception in `memory_item_transitions_insert_guard` (0016_cloud_memory.sql:1460-1478) to accept `NEW.actor = 'owner'` plus an `item.correct`/`item.transition` owner command whose payload names the archived sources, instead of requiring `version.basis = 'confirmed'` (this needs a migration). If the code must keep setting it, the lift input must carry an explicit owner confirmation and the receipt must say the basis changed. Test change: a lift of a forgotten item whose sources are archived must assert the restored version's basis equals the previous version's basis.",
          "falsifier": "If a canonical read of an item that was forgotten and then lifted after its events were archived reports `basis: \"stated\"` (i.e. the trigger exception at 0016:1460-1478 is satisfiable without `basis = 'confirmed'`, or `readSources` reports \"archived\" only for rows the trigger also treats as archived), the finding is wrong. Also wrong if `basis` never reaches a model-visible surface - I verified one (memory-search.ts:383), so a counterexample would have to show that result is filtered before composition (telegram-memory-retriever.ts:1229 filters suppressed items, not by basis)."
        },
        {
          "title": "The source-receipt validation rule exists twice in one class and is kept in step by hand",
          "symbol": "MemoryRepository.validateSourceRows (apps/cloud-gateway/src/memory/memory-repository.ts:3706-3767) and MemoryRepository.readSources (apps/cloud-gateway/src/memory/memory-repository.ts:4025-4093)",
          "line_range": "3706-3767, 4025-4093",
          "severity": "time",
          "what_is_wrong": "Two ~50-line copies of the same validation loop decide whether a stored `memory_item_sources` row matches the event store - the excerpt hash, the derived live/archived location and segment, the channel and the receipt check - and they feed the two different read paths (bulk retrieval versus single-item read) with no shared implementation.",
          "mechanism": "`readCurrentItemsWithVisibility` builds its sources through `validateSourceRows(resultAt(2), ...)` (line 1503) and `readCurrentItemInternal` builds its sources through `readSources(principalId, itemId, versionId)` (line 3845), which runs its own SELECT and then repeats the identical body. I compared the two ranges mechanically: after substituting `result.results` for `rows`, the only textual difference is the receipt call (`await validateReceipt(...)` with `principalId` passed in, versus `await this.validateReceipt(principalId, ...)`), so the copies are in step today. They are separate code, not a wrapper: `readSources` could be `validateSourceRows(rows, principalId, itemId, versionId, (eventId, sequence, source) => this.validateReceipt(principalId, eventId, sequence, source))` and would then be one rule.",
          "what_it_breaks": "The next change to this rule lands on one path only, and nothing detects it: `readCurrentItem` (used by the owner tools in memory-owner-controls.ts:824/1008/1095/1167/1319 and owner-telegram-agent.ts:1149/1190/1217/1235/1286) and `readCurrentItemsWithVisibility` (the retrieval path that decides what Jarvis recalls, filtered at telegram-memory-retriever.ts:1229) would then accept or reject the same stored row differently, and each path has its own tests, so both suites stay green. The failure surface is a source receipt check - the thing that ties a memory's excerpt to the owner's actual event - which is security-relevant on both paths.",
          "evidence": "read",
          "fix": "Delete the body of `readSources` and delegate: run the SELECT, then `return this.validateSourceRows(result.results, principalId, itemId, versionId, (eventId, eventSequence, source) => this.validateReceipt(principalId, eventId, eventSequence, source))`; `validateSourceRows` already takes exactly that callback shape (lines 3711-3715). No migration. Test change: none required if the existing tests for both paths still pass, which is the point - the shared implementation should be exercised by both.",
          "falsifier": "If the two blocks are not equivalent (I compared them line by line and found only the receipt-call difference), or if a future test asserts that the single-item and bulk paths may validate differently, the duplication is intentional rather than a hazard."
        }
      ]
    },
    {
      "path": "apps/cloud-gateway/src/memory/telegram-memory-retriever.ts",
      "lines_in_file": 2080,
      "lines_read": "1-2080",
      "verdict": "issues",
      "findings": [
        {
          "title": "findControlTargets rejects the pin/unpin operations its only production caller passes, so memory_pin and memory_unpin can never succeed",
          "symbol": "TelegramMemoryRetriever.findControlTargets (apps/cloud-gateway/src/memory/telegram-memory-retriever.ts:1811-1815); TelegramMemoryRetriever.targetStates pin branch (line 827)",
          "line_range": "1804-1828, 817-829",
          "severity": "dead-code",
          "what_is_wrong": "The runtime guard admits only forget/lift/confirm/explain/correct while the exported operation union (line 137) and its own targetStates() branch for pin/unpin say pin and unpin are legal, so a pin tool call throws before any eligibility work happens.",
          "mechanism": "1) OwnerTelegramAgent.setPin computes `const intent = pinned ? \"pin\" : \"unpin\"` (apps/cloud-gateway/src/channels/telegram/owner-telegram-agent.ts:1212) and calls `await this.requireEligibleItem(input, intent, itemId)` (line 1215). 2) requireEligibleItem (1053-1060) calls eligibleItemIds(input, operation) (1040-1051), whose FIRST statement is `await this.dependencies.targets.findControlTargets({principalId, operation, query: null, turnId: input.correlationId})` (1044-1049) - it is awaited before `contextItemIds(input)` is merged in, so the throw precedes every local eligibility source. 3) `targets` is the production TelegramMemoryRetriever: index.ts:233 constructs it and index.ts:303 passes `targets: memory` into OwnerTelegramAgentAdapter (the adapter forwards its dependency object unchanged, owner-telegram-agent.ts:632-657). 4) In findControlTargets, `input.operation` = \"pin\" fails the four-way test at 1811-1813 and throws TypeError(\"telegram_memory_target_invalid\") at 1814. 5) executeCalls wraps executeCall in `try { ... } catch { return refusedTool(call, \"I could not safely apply that tool call, so nothing changed.\") }` (owner-telegram-agent.ts:857-864), so the throw is swallowed into a refusal receipt. 6) `controls().pin(...)` (owner-telegram-agent.ts:1219) is never reached; grep shows `appendPin` has exactly one caller, MemoryOwnerControlsService.setPin (memory-owner-controls.ts:1501), reachable only from that line. 7) The reason the test suite is green: test/channels/owner-telegram-agent.test.ts:189 injects a targets double whose findControlTargets ignores `operation` entirely (`async findControlTargets() { return Object.freeze([...(input.controlTargetIds ?? [])]); }`), and the pin test (2465-2502) supplies eligibility from recalled context, not from the finder.",
          "what_it_breaks": "The whole pin feature is dead on Sid's only Telegram path: memory_pin and memory_unpin (tier-1 classified in autonomy/tool-capabilities.ts:65-66, defined in memory-tools.ts:128, dispatched at owner-telegram-agent.ts:894-895) are always answered with \"I could not safely apply that tool call, so nothing changed.\" readCoreProfile (memory/core-profile.ts:51-58) can therefore only ever see an empty memory_pinned_item_versions view, composeCoreProfile returns null (core-profile.ts:81), and the agent's system prompt (owner-telegram-agent.ts:684-690) never carries a pinned memory - the capability is structurally always empty, which is exactly the state test/channels/owner-telegram-agent.test.ts:2466-2468 says the tool was added to end. targetStates' pin/unpin branch at line 827 is unreachable code.",
          "evidence": "read",
          "fix": "Add \"pin\" and \"unpin\" to the accepted set in findControlTargets (e.g. validate via a frozen ALLOWED_OPERATIONS set that the targetStates switch also derives from, so the union, the guard and the state map cannot disagree again), or move the operation check to accept every member of TelegramMemoryTargetOperation. Then add a regression test that calls the REAL TelegramMemoryRetriever.findControlTargets for each member of TelegramMemoryTargetOperation (including pin/unpin) and asserts it resolves rather than throws; the current agent test cannot catch this because its double ignores the operation. Test change required; no migration.",
          "falsifier": "A test constructing TelegramMemoryRetriever and calling findControlTargets({principalId, operation: \"pin\", query: null, turnId}) that resolves instead of throwing; or a production call path to MemoryOwnerControlsService.pin/unpin that does not go through eligibleItemIds (grep for `.pin(` / `.unpin(` finds only owner-telegram-agent.ts:1219,1223, which is inside the failing path)."
        },
        {
          "title": "The forgotten-memory guard compares a count of item VERSION rows against an item constant and, when exceeded, empties the entire context on every turn",
          "symbol": "TelegramMemoryRetriever.withoutForgottenTurns (apps/cloud-gateway/src/memory/telegram-memory-retriever.ts:1729-1731, SQL at 1669-1678, LIMIT at 1707)",
          "line_range": "1659-1802",
          "severity": "mislead",
          "what_is_wrong": "MAX_FORGOTTEN_ITEMS is documented and used as a cap on forgotten items, but the limited query joins memory_item_versions without pinning to state.current_version_id, so one forgotten item with N versions consumes N of the 128 allowed rows; crossing the cap does not degrade suppression, it throws.",
          "mechanism": "1) Every Telegram turn with at least one base context calls retrieveBase -> withoutForgottenTurns (retrieveBase at 1646-1657, always invoked with filterSuppressions = true from line 923). 2) The `forgotten_items` CTE is `SELECT state.item_id, version.text FROM memory_item_state state JOIN memory_item_versions version ON version.principal_id = state.principal_id AND version.item_id = state.item_id WHERE state.principal_id = ?1 AND state.lifecycle_state = 'forgotten' ORDER BY state.item_id ASC LIMIT ?<n+2>` (1669-1677) - the join has no `version.version_id = state.current_version_id` predicate, so it returns one row per stored version of each forgotten item. 3) The LIMIT is bound to MAX_FORGOTTEN_ITEMS + 1 = 129 (line 1707). 4) Line 1729 rejects the whole read when `forgottenRows.length > MAX_FORGOTTEN_ITEMS`, i.e. when more than 128 (item x version) rows exist - the constant is named and reasoned about as a count of items. 5) The throw is TypeError(\"telegram_memory_suppression_invalid\"), which rejects the base stage; in retrieve() `if (baseOutcome.status !== \"fulfilled\") { ...log RETRIEVAL_BASE_ERROR_CODE...; return Object.freeze([]); }` (966-978) discards BOTH the base history and the successfully retrieved memory contexts. 6) Because the condition depends only on stored rows, it is permanent: if the principal accumulates >128 forgotten item-version rows, EVERY later turn runs wi
… [truncated: 380630 more characters]
