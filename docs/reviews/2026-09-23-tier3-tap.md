# Single-use tier-3 taps

Builder: Codex, 2026-09-23. Base: `a666097ffe6e0b2c99dc83ce29fc43efacdf7f4d`.

## Behavior and design

`ToolAutonomyGate` now calls `D1ToolConfirmationStore.consumeStandingDecision`.
One `INSERT ... SELECT ... ON CONFLICT DO NOTHING RETURNING` checks the principal,
capability, canonical argument fingerprint and tap time while claiming the tap.
Only the insert winner receives the decision id and permission to continue.
`tool_confirmation_consumptions` records the unique decision id and `consumed_at`.
The original answer remains immutable. Backup and restore include the claim.

Expiry is ten minutes from the immutable response's `responded_at`, exclusive at
the deadline. Future taps refuse. The injected clock is sampled at consumption,
after the first autonomy audit, so a slow audit cannot extend a tap's life.
No channel or call-session restriction was added: a Telegram answer can authorize
the same exact action on a call once. Receipts explain that used or expired taps
need a new confirmation.

The newest matching approval is selected. A spent newest tap does not fall back
to an older unused approval for the same action. A new tap authorizes another
attempt. Consumption is not refunded after an audit or tool error: an unknown
side-effect outcome must not become permission to retry. This is at-most-once
authorization, not a promise that an external side effect will complete.

A separate claim table avoids weakening `decision_responses_reject_update` or
rewriting the answer schema. A read followed by an unguarded write would allow
two Workers to win; the database unique key is the arbiter. Enforced taps are
the proof role expressly allowed by `CODE-VS-JUDGMENT.md`.

The agent checks channel authority, memory authority, reply target, direct
pipeline text and pipeline availability before invoking the consuming gate.
Each dispatch branch gates immediately before its tool body. This avoids a
second evaluate/claim protocol while preserving the no-refund rule once the
body starts. Voice currently has no pipeline adapter, so a pipeline request on
voice refuses without spending its Telegram tap or writing an authorized audit.
Cross-channel binding remains unchanged for actions the destination supports.

## Premises checked

- Main matched the supplied `a666097` and contains `0001` through `0035`, plus
  `0038`. Open PR #96 adds `0036`. `0037` stays unused. **Round 1 corrects the
  reviewer's erroneous reservation of `0039` for an abandoned builder.** Sid
  assigns `0039` to this PR, `0040` to D2L collector keys and `0041` to owner
  reminders. The former `0040` migration is renamed to `0039` everywhere in
  current code, fixtures and rollout instructions. Historical log entries are
  preserved. Recheck main and every open PR before merge.
  Round 1's final pre-validation inventory paginated all files of all twelve
  open PRs: #96, #122 and #154 through #163. Only #96 adds `0036`; #159 still
  published its old `0040` before this push. No other PR claims `0039`.
  Fresh main remained `a666097ffe6e0b2c99dc83ce29fc43efacdf7f4d`.
- The old lookup **already enforced a TTL**, using an inclusive lower bound on
  mutable `resolved_at`. The missing consumption record allowed repeats inside
  that window, not indefinitely. The new check uses the answer timestamp and
  the same timestamp that is persisted as consumption time.
- The 2026-09-22 log explicitly records the separate schema-change scope and
  deliberate cross-channel binding. A separate 2026-09-23 review was not found;
  the current user instruction supplies that authority.
- The CASE-wrapped RAISE hazard is still prohibited by the migration syntax
  test. This migration uses plain `BEGIN SELECT RAISE ... END` triggers.
- The claim that a fresh remote database cannot replay `0001` is stale in the
  checked-out source: commit `8a977a1` repaired it and the scratch runbook now
  says fresh replay works. This task did not verify remote replay.
- The specified profile incident report was absent from Downloads. Its sections
  could not be read. All PC safety rules supplied in the task were followed;
  only the gateway's isolated tests ran. No local-agent tests ran.

## Deployment order and limits

Prefer **migration 0039 first, gateway deploy second**. It only adds a table and
triggers, leaving existing rows and old readers unchanged. Old code still has
the old replay behavior until replaced. Deploying the new code first is safe
against unauthorized execution but temporarily refuses tier-3 actions: the
missing-table error reaches the core's existing safety-check refusal. Tier 1
and tier 2 do not consult the claim store. Applying 0039 then enables claims.
The local rollout test covers both compatibility boundaries.

Existing taps may already have been used by old code, which recorded no claim.
Before opening tier-3 actions after cutover, let the ten-minute window for old
taps expire and obtain fresh confirmations. Keep this in the owner rollout.
Rolling back to old code also restores the old replay behavior even if the new
table remains. Do not remove the consumption table to retry a failed action.

No migration was applied remotely, no gateway was deployed, no real call or
Telegram action ran, and no external tool side effect was exercised. The
cross-channel gate test uses independent real gates over local D1 and a real
decision-service answer from a Telegram identity. Agent regressions also run
the real Telegram and voice adapters and conversation service against local D1,
with synthetic model/provider responses and a test-only promotion of supported
tools to tier 3. Live acceptance and remote
D1 rehearsal remain owner actions. The separate queued second-autonomy-evaluation
verdict issue is unchanged by this slice.

## Round 1 evidence

Implementation and mutation head: `b69c7284809f0d8c24e6f74efccfd9f7c0ddc93a`.
The subsequent evidence commit changes documentation only. The normal merge
of freshly fetched `origin/main` reported already up to date at `a666097`,
both before implementation and after mutations. No force push is used.

| Gate | Observed result |
|---|---|
| Initial agent run | 99 passed, 6 failed, 0 skipped; five fixtures asserted the canned model reply instead of the refusal tool result, and an audit query selected another principal's earlier row |
| Corrected agent run | 105 passed, 0 failed, 0 skipped; only the fixture assertions/query changed |
| Focused gate, agent, voice, migration and backup suites | **238 passed, 0 failed, 0 skipped; 10 files** |
| Gateway source typecheck | Exit 0; 0 errors |
| Gateway test typecheck | Exit 1; **144 errors in 32 files**, all diagnostic messages identical to the previous head after normalizing line/column positions; none in the new dispatch tests |
| `node scripts/check-state.mjs` | Exit 0; 3 carriers passed |
| `git diff --check` | Exit 0 |

For the full gateway suite's **final-head counts and SHA**, see
[PR #159](https://github.com/stremysid/jarvis/pull/159) and the external ledger
alongside `tier3-round1-full-gateway.json`. The full suite runs after this
evidence commit so its result belongs to the exact published head.

### Round 1 mutations

Specs: `reviewer-tools/mutation-specs-tier3-dispatch.json`, plus `consume-write`,
`ttl` and `race-unique-key` from `mutation-specs-tier3-tap.json`. The same external
copy of `reviewer-tools/mutate.ps1` retains temporary backups instead of
recursively deleting them. Every baseline passed; each required named test
failed twice with its mutation applied; every source restore was byte-identical.
Six restored agent suites each passed **7/7**, and four restored tap suites each
passed **15/15**: **102 passed, 0 failed, 0 skipped** in total.

**10 killed; 0 survived, 0 wrong-test kills, 0 unconfirmed, 0 not applied,
0 invalid.** No result from the independent review is counted as a builder run.

| Fault | Named test that failed twice |
|---|---|
| Claim before memory authority | preserves a Telegram tap when memory authority refuses dispatch |
| Claim before reply-target check | preserves a Telegram tap when reply target refuses dispatch |
| Claim before direct-pipeline check | preserves a Telegram tap when pipeline authority refuses dispatch |
| Claim before pipeline availability | preserves a Telegram tap after voice refuses an unsupported pipeline and lets a later Telegram turn claim it once |
| Bypass memory dispatch gate | requires a tap before dispatching the tier-3 tool memory_pin |
| Bypass pipeline dispatch gate | requires a tap before dispatching the tier-3 tool school_update |
| Change TTL to twenty minutes but hard-code a ten-minute receipt | describes tap expiry using the configured confirmation lifetime |
| Replace consumption with a read-only lookup | authorizes exactly one execution and explains why a second attempt needs a new tap |
| Remove TTL lower bound | refuses an expired tap at age 600000 milliseconds without consuming it |
| Remove the unique decision key | allows exactly one of two concurrent executions racing for the same tap |

## Initial review-head evidence (26d0fec)

All runs were on Windows with Node 24.19.0 and pnpm 11.19.0. Dependencies came
from the existing cache with `pnpm.cmd install --offline --frozen-lockfile
--ignore-scripts`. No local-agent, real provider or remote D1 test ran.

| Gate | Observed result |
|---|---|
| Original `tool-gate.test.ts` baseline | 9 passed, 0 failed, 0 skipped; 1 file |
| First new focused run | 81 passed, 1 failed, 0 skipped; the rollout fixture used nonexistent `read.data`, corrected to the actual `read.archive` seed |
| First focused run including backups | 118 passed, 1 failed, 0 skipped; backup manifest still expected schema 0038, corrected to the new migration (then numbered 0040) |
| Final focused gate, migration and backup suites | 120 passed, 0 failed, 0 skipped; 7 files |
| Full gateway: `pnpm.cmd exec vitest --config vitest.workspace.ts run apps/cloud-gateway/test` | **5,125 passed, 0 failed, 0 skipped; 192 files** |
| Gateway source typecheck | Exit 0; 0 errors |
| Gateway test typecheck | Exit 1; **144 errors in 32 files**. Four are unchanged ULID fixture errors in `memory-backup-restore.test.ts`; no errors in the new gate/rollout tests. All 144 diagnostic messages match the earlier run after normalizing line numbers |
| `node scripts/check-state.mjs` | Exit 0; 3 carriers passed |
| `git diff --check` | Exit 0 |

No unrelated flaky failure occurred in the full run, so no isolated flake rerun
was needed. Full test JSON and detailed logs remain beside the external ledger
at `C:\Users\Sid\codex-ledgers\tier3-tap.md`.

### Initial mutations

Specifications: `reviewer-tools/mutation-specs-tier3-tap.json`. Each mutation ran
against implementation commit `567590fe3f11bbc5d4041860250c785d462044fa` using
`reviewer-tools/mutate.ps1`, via an external copy whose only change retained its
temporary backups instead of recursively deleting them. Each had a green
baseline, the named test failed **twice with the fault present**, the source was
restored byte-for-byte, and a separate restored run passed **14/14, 0 failed,
0 skipped**. Subsequent repository edits only finalize documentation.

**12 killed; 0 survived, 0 wrong-test kills, 0 unconfirmed, 0 not applied,
0 invalid.** The extra restored runs total 168 passed, 0 failed, 0 skipped.

| Fault | Named test that failed twice |
|---|---|
| Replace the consuming insert with the old read-only lookup | authorizes exactly one execution and explains why a second attempt needs a new tap |
| Remove TTL lower bound | refuses an expired tap at age 600000 milliseconds without consuming it |
| Admit the exact TTL boundary | refuses an expired tap at age 600000 milliseconds without consuming it |
| Remove future-time bound | refuses a tap timestamped after the claim clock |
| Freeze the clock at construction | checks expiry at consumption after a delayed audit rather than using its earlier timestamp |
| Remove the unique decision key | allows exactly one of two concurrent executions racing for the same tap |
| Disable update rejection | refuses to erase or rewrite a consumed mark |
| Disable delete rejection | refuses to erase or rewrite a consumed mark |
| Remove timestamp format check | requires a real response and a canonical nonnull consumption timestamp |
| Allow a null decision key | requires a real response and a canonical nonnull consumption timestamp |
| Allow a null consumption time | requires a real response and a canonical nonnull consumption timestamp |
| Remove the response foreign key | requires a real response and a canonical nonnull consumption timestamp |
