# Owner actions

Everything that only Sid can do. Nothing here can be done by a builder or a reviewer
session — if it could, it would be in [QUEUE.md](QUEUE.md) instead.

**One row per action.** The whole point of this file is that an action cannot be
asked for twice: if it is already here, the answer is "see the row", not a second
request. That failure has happened — Google Classroom consent was ruled out on
2026-09-17 and walked through again on 2026-09-18, because the fact lived in an
agent's memory and never in the repository.

Last regenerated: 2026-09-24, evening, from `gh pr view` on every open PR, the
orchestrator's read-only production checks, and Sid's chat statements of that evening.
Production was not queried by this builder. Order within a section is the order to do them in.

## Deploy results (2026-09-24 evening)

**Done**, from Sid's report of his `wrangler` output. The release became `a7cd355` (#189)
rather than the planned `68675ba`, because #189 merged first. The merge freeze is lifted.

| Item | Result |
|---|---|
| Migrations applied | `0040`, `0043`, `0045`, applied 2026-09-25 01:40:48–49 UTC; D1 now at `0045` |
| Worker deployed | `a7cd3553166e10293e27656c4b800f26b0dec7cb` at about 9:41 PM EDT 2026-09-24. Gateway `d69bd158-3d1b-4a67-a359-f561ebbd0908` (rollback `bda73930-8240-47d1-95ba-b206b67a5362`); watchdog `3018f5fd-7f5f-4192-9c23-496718aedbef` (rollback `c940f9b7-99cf-4194-8f41-489038a34139`) |
| Health and acceptance | Both `/health` returned 200; Telegram `/status`, `/queue`, `/digest` and a normal message answered. The migration-linked acceptance rows below are not covered by this |

## Done — kept so they are not asked for again

| Action | Evidence |
|---|---|
| Load the calling bindings | Twilio secrets are set, and `call_sessions` holds six inbound owner calls from 2026-09-17, queried 2026-09-21 |
| Enroll the owner phone | `channel_identities` holds an active `voice` identity in production, queried 2026-09-21 |
| Apply `0038`, then deploy | `0038` applied 2026-09-20; Worker `78cb6e98` deployed 2026-09-21 from `352991e`; the gateway heartbeat records |
| Which model does `DEEPSEEK_MODEL` name? | Flash — every memory-extraction call through 2026-09-21T23:30Z was `deepseek-flash`, and extraction reads that secret. See `FACTS.md` |
| Route the school email address to the Worker | Configured (Sid, 2026-09-21). It delivers nothing useful: D2L's notification email carries no deadline |
| Is the deployed `DEFAULT_GUEST_PIN` the committed test value? | No — the committed `4827` is a placeholder. Sid, 2026-09-19 |
| Does the reviewer keep merge authority? | Yes, for PRs it has cleared, at the exact reviewed head. Sid has directed merges throughout 2026-09-20 |
| Who reviews reviewer-authored PRs? | DeepSeek reviews; GPT-5.6 Sol builds. Sid, 2026-09-20 |
| Set `JARVIS_ARCHIVE_PATH` and `JARVIS_MEMORY_PATH` at user scope | Set 2026-09-23 to `%LOCALAPPDATA%\Jarvis\data\archive.sqlite` and `memory.sqlite`. Sid ran `jarvis config` from `C:\javis` at `ec5ebb5` and it printed `configuration ready` |
| Install the PC boot chain, elevated | Auto-login was already configured. Sid registered the `Jarvis boot chain` task on 2026-09-23 (`SID\Sid`, Interactive, RunLevel Highest, restart 3 × PT1M) and started it; `jarvis status` answered `status running`. A real reboot has not yet been observed |
| Deploy [#154](https://github.com/stremysid/jarvis/pull/154) after independent clearance and merge | **Done:** included in Sid's 2026-09-23 deploy at 20:41 EDT. That revision was later superseded by the second deploy recorded on the row below; a fresh Confirm tap after either deploy remains unverified; see [STATE](STATE.md#production) |
| **Deploy an explicitly approved main revision** | **Done twice.** Sid ran `scripts/deploy.ps1 -Publish` from `C:\javis` on 2026-09-23 at 20:41 EDT, then again on the night of 2026-09-23: the orchestrator's read-only checks on 2026-09-24 found source `0d69556` (#165) with D1 at `0039` applied and the Worker modified at `2026-09-24T03:47:07Z`. The second deploy's Worker version id is **unverified**. Source: orchestrator checks, recorded in [STATE](STATE.md#production) |
| Renumber the older `0041` and `0042` migrations above main's maximum | **Done — decision, not migration application.** Sid, 2026-09-24, when asked about both renumberings: "okay go ahead with both then". [Recorded on #168](https://github.com/stremysid/jarvis/pull/168#issuecomment-5819802422). #166 has merged; #168 renamed its migration to `0050` on 2026-09-25 after checking main and every open PR |
| Have test calls already worked? | **Yes, owner-reported.** Sid, 2026-09-24: "ive already done test calling and it works". This does not establish the separate #171 streaming/receipt checks or full release gate |
| What is the status of **St. Remy**? | **Paused.** Sid, orchestrator chat, 2026-09-24: "lets put it on pause for now". No St. Remy code was touched. The conflicting scope instructions ([DECISIONS.md](../DECISIONS.md#planning-session-record-from-2026-09-03-unconfirmed-and-superseded-in-part) against [AGENTS.md](../AGENTS.md#things-that-are-not-this-repository)) are superseded for now by this pause, not resolved |
| Which school date source comes first when builders resume? | **The email inbox.** Sid, orchestrator chat, 2026-09-24: "agreeded" to putting the email inbox first. Teacher emails as a date source go first among unfinished school work. It does not revive the D2L notification-email route, which carries no deadline |
| Remove the deadline tool's "proof contract" refusals? | **Yes — decided by Sid; built in [#193](https://github.com/stremysid/jarvis/pull/193), awaiting DeepSeek audit and merge.** Sid, orchestrator chat, 2026-09-24, 7:14 PM EDT: "why would jarvis refuse? … why dosnt he jsut aks for calirty? … the AI IS THE BRAIN IT CAN ThINK AND DECIED"; 7:15 PM EDT: "so why is hell is code refusing? … code should never make a decions or retaruict jarvis" (both transcript-checked by the orchestrator, his spelling kept). The orchestrator's paraphrase, not a verbatim quote: code gives jarvis unfiltered unrestricted access and words, jarvis makes decisions. Remove `DUE_PHRASE` and the "code cannot choose" refusals in `apps/cloud-gateway/src/deadlines/deadline-date-proof.ts`, and the `statusOf` keyword list (`STATUS_WORDS`) in `apps/cloud-gateway/src/deadlines/deadline-tool.ts`. The model decides and asks when unsure; code checks only that the date exists, and keeps the raw message as the receipt. **This supersedes the two former questions about `OWNER_SMALL_HOURS_END_HOUR` and the #166 round-7 Z separator arm**, dropped from the queue as no longer his to answer. Until #193 merges and deploys, the old grammar is still what runs in production |

## Waiting on Sid

| Action | Why only you | State |
|---|---|---|
| Rehearse migration 0044 and deploy channel-parity ([#174](https://github.com/stremysid/jarvis/pull/174)) after independent review and merge | Only Sid authorizes a real database or deployment. Production D1 is already at `0045`, so `0044` applies below the applied maximum; it replaces only the 19 school/university pipeline owner-turn triggers and shares no object with `0045`. Rehearse on an explicitly authorized scratch database (scratch after `0045` is an orchestrator-reported PASS, not independently re-run), then apply before deploying. Without 0044, voice pipeline saves fail closed. After rollout, verify a real school/university/study call, exact-wording memory confirmation and a Telegram tier-3 tap reused on voice only once. [Audit and local evidence](reviews/2026-09-23-channel-parity.md). No spoken PIN or complete cross-channel transcript is claimed | awaiting review and separately authorized rollout |
| Apply `0049_web_tools.sql` with [#195](https://github.com/stremysid/jarvis/pull/195)'s deploy | Applying migrations and deploying are yours. `0049` seeds the tier-1 capability `read.web` and the `web_tool_receipts` table. If the gateway deploys first, the tier gate refuses `web_read` and `web_search` with its "capability is not registered" receipt. That fails closed: nothing is fetched. Apply it in migration order, after `0048` and after any lower-numbered migration merged by then. `web_read` also needs the `PUBLIC_ORIGIN` secret. The deploy runbook lists it as already set in production, but nobody checked. Without it, `web_read` refuses every page and says the secret is missing, while `web_search` still works. After deploy, one real `web_search` from Telegram confirms Exa answers a Worker. That has not been tested live | **not started; after #195 is audited and merged** |
| Optional: turn on `web_read`'s JavaScript rendering ([#195](https://github.com/stremysid/jarvis/pull/195)) | Only you can create Cloudflare API tokens and set secrets. In the Cloudflare dashboard, create an API token with **only** the account permission **Browser Rendering - Edit**, on this one account. Then run `wrangler secret put BROWSER_RENDERING_ACCOUNT_ID` and `wrangler secret put BROWSER_RENDERING_API_TOKEN` for `jarvis-cloud-gateway`, from `C:\javis\apps\cloud-gateway` in PowerShell. Without them, `web_read` works for ordinary pages, and asking it to render tells Jarvis the path is not configured. Browser Rendering usage may be billed by Cloudflare. The price was not checked | **optional; not started** |
| Optional: an Exa API key for `web_search` ([#195](https://github.com/stremysid/jarvis/pull/195)) | Only you can sign up and hold the key. `web_search` is keyless by default, but Exa's free tier is rate-limited per IP, and a Cloudflare Worker shares egress IPs. Exa's source shows the limit answers 429. How often that happens is unverified. If Jarvis reports the Exa rate limit, create a key at exa.ai and run `wrangler secret put EXA_API_KEY` from `C:\javis\apps\cloud-gateway` in PowerShell. Exa pricing was not checked | **optional; only if the keyless limit is hit** |
| Try one real deadline after [#193](https://github.com/stremysid/jarvis/pull/193) is merged and deployed | Only you can judge whether Jarvis read you right. In Telegram, and once on a call, tell Jarvis an assignment and when it is due in your own words (for example `Chem lab report. due 3pm friday`). Check that the receipt shows the date and time you meant in your zone, that a day with no time says date-only, and that Jarvis asks you instead of refusing when it is unsure. Mention a submitted one again and check it stays submitted | **not started; after #193 merge and deployment** |
| Which St. Remy scope instruction is current? | [DECISIONS.md](../DECISIONS.md#planning-session-record-from-2026-09-03-unconfirmed-and-superseded-in-part) says St. Remy is "no longer off limits" under a section marked "unconfirmed and superseded in part"; [AGENTS.md](../AGENTS.md#things-that-are-not-this-repository) says its code is out of scope for a Jarvis session, and [BUILDING.md](BUILDING.md#who-builds-and-who-reviews) also says St. Remy is out of scope. These conflict; this builder has not resolved the decision. Sid paused St. Remy on 2026-09-24, which defers rather than settles it | **paused by Sid 2026-09-24; not asked until he resumes it**; no St. Remy code touched |
| First live check of merged #171 owner voice streaming | After its deployment and explicit authority to use provider credit, check one ordinary answer, one harmless test memory and one public synthetic guided draft. Record first-sentence latency, indexed tool fragments, one completed tool receipt and spoken/final transcript agreement. Check claim markers stay unspoken, proofs belong to this turn, unsupported claims get the honest line, and header/bearer (machine-credential) redaction holds while Sid's own codes and numbers are spoken as they are. Keep only redacted protocol/timing evidence. [Streaming evidence](voice-streaming.md). Sid's working-test-call report is recorded above; these narrower acceptance checks remain unverified | **merged; deployment and this acceptance check not started** |
| Roll out guided assignment (#172 merged) | `0043_guided_assignment.sql` is on main with its gateway. [Scratch rehearsal](reviews/2026-09-24-scratch-d1-rehearsal.md), 2026-09-24: The Claude orchestrator ran it; results are transcribed from its harness-supplied summary and table; not independently re-run. Sid, 2026-09-24 (orchestrator chat, supplied by the harness): '2: yes'. Use the reviewed ordered migration set, then deploy and check Telegram and owner voice scribe fidelity. [Design and limits](reviews/2026-09-23-guided-assignment.md) | **scratch done / orchestrator-reported PASS; production application and deploy not started** |
| Check the digest after [#167](https://github.com/stremysid/jarvis/pull/167)'s deploy | Removes retired D2L-email and Classroom health warnings from scheduled/manual digests. No migration or data deletion for this change. Deployed: its merge `c5310be` is an ancestor of `0d69556` (`git merge-base --is-ancestor`, exit 0). Check `/digest` and the next 07:30 digest | **deployed in `0d69556` (orchestrator-observed 2026-09-24); `/digest` and 07:30 digest check not recorded** |
| Apply `0048_note_sources_without_markdown_citation.sql` after [#194](https://github.com/stremysid/jarvis/pull/194) is independently reviewed, then deploy the matching gateway | Only you apply a migration to the real D1. Before applying, recheck the number against main and every open PR: `0044` ([#174](https://github.com/stremysid/jarvis/pull/174)), `0047` ([#196](https://github.com/stremysid/jarvis/pull/196)) and `0049` ([#195](https://github.com/stremysid/jarvis/pull/195)) are merged; `0050` ([#168](https://github.com/stremysid/jarvis/pull/168)) is merged; `0052` ([#190](https://github.com/stremysid/jarvis/pull/190), renumbered from `0046`) is merged as `f43fcf42`; open [#199](https://github.com/stremysid/jarvis/pull/199) holds `0051` (checked 2026-09-25). This migration was renumbered from `0047` to `0048` because `0047` was already claimed. It only drops and recreates `memory_topic_note_sources_insert_guard` without the Markdown-citation clause; it adds no table and touches no row. Until it is applied, nightly consolidation still fails as `memory_topic_note_source_invalid` even with the reviewed code, because the trigger is the second place that rule lived. History indexing in the same PR needs no migration (round 2 writes line breaks as spaces in the search copy only), so it resumes by itself once the code is deployed | **not started; no migration applied by the builder** |
| Deploy the history_search PR ([#198](https://github.com/stremysid/jarvis/pull/198)) once it is independently reviewed and merged, then try it on a call and on Telegram | Only you deploy. It adds **no migration** of its own; #194 (merged) must be deployed with or before it, and its `0048` applied first. After the next hourly run has indexed history, ask on Telegram and then on a call about something you said days ago; both should answer from the same messages. Call replies Jarvis spoke before this deploy are indexed by the hourly job while they are still in D1; ones already archived to R2 are not. The builder ran no live check | **awaiting review and merge** |
| **Load the probe, click Run probe, paste the summary** | Only your Opera GX session can test LDSB access. Follow [the D2L probe runbook](runbooks/d2l-extension.md): one pass with every D2L tab closed, then one with a refreshed D2L tab open. Copy the combined shape-only summary to the reviewer, then remove the extension. This single action replaces #160's overlapping D2L capability-check and collector-probe requests; do not repeat those separately | **in-tab pass done** (2026-09-23 about 8:25 PM EDT; results in [the owner run](research/2026-09-23-d2l-probe-owner-run.md)); **tabs-closed (background) pass not run yet**, so background mode is still unverified |
| **Load and pair the D2L collector on the PC and laptop** | Follow [the collector runbook](runbooks/d2l-extension.md), approve each device code in Telegram, enter the Durham hop once and check both hosts with D2L tabs closed. The receiver fixes are merged; the extension compatibility hold and host-failure follow-up remain in QUEUE. The shape probe was already supplied; do not ask for it again | **awaiting-owner after reviewed compatibility follow-up and rollout**; background access remains unverified |
| Check one fresh tier-3 confirmation after #159's rollout | [0039 scratch rehearsal](reviews/2026-09-24-scratch-d1-rehearsal.md), 2026-09-24: The Claude orchestrator ran it; results are transcribed from its harness-supplied summary and table; not independently re-run. `0039_tool_confirmation_consumptions.sql` is applied and #159's gateway is deployed (its merge `6e3f1ef` is an ancestor of `0d69556`). What remains is one fresh confirmation on a supported tool. **Do it before `0051` is applied** ([#199](https://github.com/stremysid/jarvis/pull/199)): revoking a school collector is the only tool that asks today, and after `0051` no tool Jarvis has asks, so this check waits until one of your five gets a tool (email is next). After #174, voice pipelines are dispatchable, so voice claims an approved tap before their bodies just as Telegram does. Any confirmation pending when #182 deploys needs a fresh tap; see [the compatibility note](../KNOWN_ISSUES.md#tier-3-confirmations-issued-before-tool-binding-2026-09-24). [Rollout and evidence](reviews/2026-09-23-tier3-tap.md) | **`0039` applied 2026-09-24 03:46:58 UTC and gateway deployed 03:47:07Z (orchestrator-observed); remaining: one fresh confirmation on a supported tool, unverified** |
| Try one real D2L assignment paste (#164) | Only Sid can compare saved per-course notes and daily workload with his real assignment list and pinned capacity. Paste as direct Telegram text, check new/already-saved counts and stored blocks, then re-paste for deduplication. No migration for this change. Deployed: its merge `0b63b91` is an ancestor of `0d69556` | **deployed in `0d69556`; owner paste acceptance not started** |
| Set `CALENDAR_FEED_TOKEN` if it is not set, then subscribe in iPhone Calendar (#165) | Set the required random secret through the authorized setup, then subscribe to its private URL. [Setup, rotation and alert checks](runbooks/iphone-calendar-feed.md). Any **Remove Alerts** switch must be off; device behavior remains unverified. Deployed: `0d69556` is #165's own merge | **deployed in `0d69556`; whether `CALENDAR_FEED_TOKEN` is set is unverified; subscription not started** |
| Accept the D2L collector on the PC and laptop after review and rollout | Confirm each device's matching Telegram code. Compare complete reads from **LDSB and Durham**, including CIA4U1, quiz dates, announcements, undated work and 403/404 evidence. The empty myItems/submission shapes are already owner-observed in #170; positive own-submission shapes remain unverified. Test expiry, revocation, pairing-delivery retry and a visible host session failure. See the [collector design](plan/2026-09-23-d2l-collector-design.md) | **waiting for reviewed extension and receiver compatibility rollout; no builder live acceptance** |
| Apply the D2L receiver migrations and deploy (#169/#175/#176 merged) | [Scratch rehearsal](reviews/2026-09-24-scratch-d1-rehearsal.md), including `0040` and `0045`, 2026-09-24: The Claude orchestrator ran it; results are transcribed from its harness-supplied summary and table; not independently re-run. Sid, 2026-09-24 (orchestrator chat, supplied by the harness): '2: yes'. Quiesce collector uploads, run the linked `0045` duplicate pre-check grouped by principal and origin reference, then apply the reviewed main set `0040`, `0043`, `0045` in order (`0039` is already applied). `0044` is on main since #174 merged; scratch after `0045` is an orchestrator-reported PASS; include it only after a fresh rollout check. Deploy the matching gateway, resume collectors and use the two-board acceptance row | **scratch done / orchestrator-reported PASS; production application and deploy not started** |
| Prove and apply reminder migration `0050_owner_reminders.sql` before deploying [#168](https://github.com/stremysid/jarvis/pull/168) | Only you apply a migration to the real D1. Renumbered from `0041` to `0050` under your 2026-09-24 decision, so it sorts after every migration on main (newest `0049`, from merged #195). [#190](https://github.com/stremysid/jarvis/pull/190) merged `0052` (renumbered from `0046`) as `f43fcf42`; open [#199](https://github.com/stremysid/jarvis/pull/199) holds `0051` (checked 2026-09-25); recheck before applying. After independent review, use the [remote D1 scratch-proof runbook](runbooks/migration-scratch-proof.md) on a newly confirmed disposable database, then apply it before deploying the reminder tools and drain. It only creates `owner_reminders` and its insert trigger. The builder applied nothing to a real database | **not started; before reminder deployment** |
| Accept reminder scheduling and delivery after deployment | Ask Jarvis for a study reminder once in Telegram and once on a call. Check the receipt shows your local time, zone, UTC instant and words; observe one Telegram delivery after that instant outside quiet hours; cancel another and confirm it never arrives. A failed/unconfirmed reminder may already have arrived; check before replacing it. A rejected reminder was not delivered; authentication and rate-limit refusals stay pending for retry | **not started; after migration and deployment** |
| Apply `0051_confirm_only_five_actions.sql` after [#199](https://github.com/stremysid/jarvis/pull/199) is independently reviewed and merged, with the gateway it ships in | Only you apply a migration to the real D1. It changes rows only: tier 3 (the tap or the call PIN) becomes exactly your five actions — spending money, sending an email, making a call, submitting school work, texting or calling someone for you. Revoking a school collector stops asking; deleting data, touching production and unlocking the car move to tier 2, which never asks (it runs once `/shadow off`). Needs `0040` applied first. Before applying, recheck `0051` against main and every open PR (checked 2026-09-25 after merging main: `0049` #195, `0050` #168 and `0052` #190 are merged). Undoing it is an UPDATE of those four rows back to tier 3 | **not started; nothing applied by the builder** |
| Run the live voice smoke and commit the redacted evidence | `pnpm smoke:voice`, then `pnpm release:voice-gate`. Both are built and neither has ever run against production. It needs your phone | **not started** |
| **Set `OWNER_ACTION_PIN` — the 4-digit PIN a call asks for before an action done as you** | Only you can choose it, and only you can write a Worker secret. **PowerShell 7**, in the reviewed deployment checkout, following the [calendar-feed runbook's secret steps](runbooks/brightspace-calendar-feed.md#store-the-worker-secret-from-powershell-7): `cd 'C:\Users\Sid\jarvis-deploy'`, then `pnpm.cmd install --frozen-lockfile`, `$PSNativeCommandArgumentPassing = 'Standard'`, `$wrangler = (Resolve-Path 'node_modules/wrangler/bin/wrangler.js').Path`, `$gateway = (Resolve-Path 'apps/cloud-gateway/wrangler.toml').Path` and `& node $wrangler whoami` (check it names the right Cloudflare account). Then `& node $wrangler secret put OWNER_ACTION_PIN --config $gateway --env ''`, type exactly four digits at its prompt, and check with `& node $wrangler secret list --config $gateway --env ''`. The code that reads it ships with #196's deploy. **What it is for:** your five actions: spending money, sending an email, making a call, submitting school work, and texting or calling someone for you. Reading or saying anything Jarvis knows never needs it, memories included. Until it is set, an ordinary owner call works normally and an action that would ask for it is sent to `/queue` in Telegram instead. You can say the PIN or key it in; each try waits 20 seconds, you get five tries, and "cancel" stops it. A run of wrong guesses pauses new questions for up to fifteen minutes, never permanently. To change it, run the same `secret put` again. The list the code checks is the tier-3 list. [#199](https://github.com/stremysid/jarvis/pull/199) (migration `0051`) makes it exactly your five; until `0051` is applied, revoking a school collector also asks. None of the five has a tool Jarvis can use yet, so after `0051` nothing Jarvis can do today asks for the PIN | **not started** |
| Send one real Google Classroom notification, forwarded | The Classroom REST route is impossible on this board, so the notification email is the only route. A parser cannot be written honestly from a guessed format. Forward one real notification to the school mailbox | **not started** |
| Say whether that forward is an **automatic M365 rule** or a **manual Outlook Forward** | It decides whether Classroom can work at all. An automatic M365 forward preserves the original DKIM signature; a manual Forward recomposes the body, destroys it, and every message quarantines as `from_domain_unpinned` | **not started** |
| **Finish the Gmail forward to Jarvis's email address** | Only your Gmail account can confirm the forwarding for `school@onesid.ca`, and only you can read the code out of the confirmation mail. Open Gmail's forwarding settings, click **Re-send email** on the verification for `school@onesid.ca`, then ask Jarvis for the confirmation code and enter it. The school M365 forward is already active and needs no action | **not started — after this inbox is deployed with `0052_email_inbox.sql` applied (only you apply it)** |
| Decide and verify R2 backup bucket-lock settings when backup retention is reviewed | Repository verification/retention code cannot establish the account's bucket-lock settings. No production configuration was queried by this audit | **unverified** |
| Configure and prove an external watchdog monitor when monitoring setup is authorized | The watchdog's own health endpoint cannot report total cron failure without an outside poller. STATE records no external watchdog; this audit did not query production | **not started** |

### [PR #157](https://github.com/stremysid/jarvis/pull/157) — the store-folder work, in order

| Action | Why only you | State |
|---|---|---|
| If `%LOCALAPPDATA%\Jarvis` is owned by **Administrators**, run the one-time repair: either `jarvis serve` once from an **elevated** shell, or the exact `icacls` line the refusal prints | The DACL write is checked against the object's access, and a non-elevated process is not named on it — nothing the code can do changes that. This is the state the whole PR exists to get out of, and it is the one repair no builder may perform | **not started** — check the startup output for `WARNING ... is owned by` first; if it is absent, there is nothing to repair |
| Run the **manual acceptance**: start `jarvis serve` by hand **non-elevated**, confirm `%LOCALAPPDATA%\Jarvis\data` is reachable, then stop it and start it **elevated** and confirm the same | This is the only check that meets the real DACL, and only you have the elevated shell. It is also the test nobody can run for you: it decides whether the boot task may be re-enabled at all | **not started** — #157 is merged; do this before re-enabling the boot task |
| Re-enable the `Jarvis boot chain` task | It has to stay disabled until the row above passes — the elevated task is what created the Administrators-owned folder in the first place | **not started** — blocked on the row above |
| Delete `C:\jarvis-test-scratch` after the checkpoint-5 integration run | The scratch directory is the signal that a real-permission test is wanted, so leaving it in place means the next `uv run pytest` runs five tests that change real permissions without anyone choosing to | **not started** — after the checkpoint-5 run, and only when you decide no further real-DACL run is needed |

**Builder work stays in [QUEUE.md](QUEUE.md):** calls already have memory tools;
remaining channel parity and conversation-state work are not owner setup.

## Rule

A session that needs something from Sid adds a row here **in the same commit** as the
work that needs it. A session that needs the same thing twice reads this file first.

