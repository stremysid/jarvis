# Queue

What is in flight, who owns the next action, and what it blocks. **This file is the
working queue.** The roadmap says what should exist; this says what is actually
moving. Regenerate it rather than appending to it.

`state` is one of `awaiting-review`, `changes-requested`, `awaiting-owner`,
`blocked`, `ready-to-merge`. `BLOCKS` names the phase a pull request gates.

Last regenerated: 2026-09-23, against `origin/main` at `a666097` (#153). That was `main` when this was written, not a claim about now: run `git log --oneline origin/main -1`. The #96 and #122 conflict notes preserve main's checks against `f9472d1`; they are dated evidence, not fresh checks against `a666097`. #154 merges `a666097` normally and resolves its only conflict, this file.

## Pull requests, and the order they have to land in

Open when re-queried on 2026-09-23 at 23:07 UTC (`gh pr list --state open`): #96, #122, #154, #155, #156, #157, #158, #159 and #160. #153 has merged. The review request named only #96, #122 and #154; six more PRs opened meanwhile.

| PR | State | Next action | Owner | BLOCKS | Notes |
|---|---|---|---|---|---|
| [#171](https://github.com/stremysid/jarvis/pull/171) | **awaiting-review** | Review round 2 at its pushed head after CI; owner live check after clearance | reviewer | guided assignment on calls, Phase 5 | `codex/voice-streaming`; normal main merge through `7b805fa2` including #172. Model claim markers and unsplit redaction; final restored focused 193/0/0, 39 final mutations killed twice. Full suites run in GitHub Actions under Sid's local-load rule; read the PR's CI comment. Keeps #159 tap dispatch and main's catalogues/Telegram judgment; shared quoted-escape redaction is strengthened on both channels; [evidence](voice-streaming.md) |
| [#172](https://github.com/stremysid/jarvis/pull/172) | **awaiting-owner** | After its 2026-09-24 merge, coordinate sentence receipt proof with #171 before authorized rollout | reviewer / voice-streaming builder | Phase 3 | `codex/guided-assignment`; [tools, judgment and round-1 evidence](reviews/2026-09-23-guided-assignment.md); migration `0043`; full gateway 5286/2/0 with the two timeout files alone 70/0/0 and 71/0/0; 18 new mutation kills plus 3 post-merge rechecks; collector follow-up `guided-assignment-d2l-evidence` is ready after #169 |
| [#167](https://github.com/stremysid/jarvis/pull/167) | **awaiting-review** | Re-review round 1: shared school-observation store failures stay visible while Classroom scan health is retired | reviewer | none | `codex/digest-retire`; normal main merge; focused 177/0/0; mutation killed twice; full gateway 5,116/1/0 (known meaning-search timeout), isolated file rerun 70/0/0 pass/fail/skip |
| [#96](https://github.com/stremysid/jarvis/pull/96) | **awaiting-owner** | Sid is consulting the head builder (2026-09-23). **The one plan on the table, not yet his decision: rebuild it fresh from `main`, using #96 as reference**, rather than reviving the branch. Its head `6a322d1` is 112 commits behind `f9472d1`, and `git merge-tree` against `f9472d1` names 14 conflicting files. It overlaps [#149](https://github.com/stremysid/jarvis/pull/149), which merged and changed the same redaction rule: the two share 7 files, among them `calls.ts`, `projection_policy.py` and `redaction.test.ts`. Its migration is `0036`, in the gap below `0038`: `main` has `0001`–`0035` and `0038`. The rebuild takes whatever number is next when it lands. Sid now assigns `0039` to tier-3 taps, `0040` to D2L collector keys and `0041` to owner reminders; recheck main and every open PR before allocating a rebuild number | Sid | Phase 5 | Spoken PIN before sensitive actions, and the redaction fix. 57 files |
| [#122](https://github.com/stremysid/jarvis/pull/122) | **awaiting-owner** | Sid is consulting the DeepSeek builder (2026-09-23) on whether this is still the Phase 2 plan. Against `f9472d1` it conflicts only in `docs/AGENT_LOG.md`. But its head is from 2026-09-19, and 7 commits have touched `apps/cloud-gateway/src/memory` since, so it needs a refresh before it merges | Sid | none | The memory redesign spec |
| [#154](https://github.com/stremysid/jarvis/pull/154) | **awaiting-review** | Review round 1: virtual <=500 ms and stage-overlap guards, 5 confirmed mutation kills, 20/20 complete memory-file loops, full gateway 5,114/0/0 pass/fail/skip | reviewer | every gate | Callback fix unchanged from `28c9463`; normal merge of `a666097`; evidence in the PR and AGENT_LOG |
| [#155](https://github.com/stremysid/jarvis/pull/155) | **awaiting-review** | Review the check-state hardening; retain the work-item details below | reviewer | none | `codex/check-state-harden` |
| [#156](https://github.com/stremysid/jarvis/pull/156) | **awaiting-review** | Review the Hermes #24 Windows Store host fix; retain the work-item details below | reviewer | none | `codex/hermes-msix` |
| [#157](https://github.com/stremysid/jarvis/pull/157) | **awaiting-review** | Review sync recovery and store access; this builder does not touch those files | reviewer | P2, Phase 3 | `goal/sync-recovery` |
| [#158](https://github.com/stremysid/jarvis/pull/158) | **awaiting-review** | Review requirements, testing and known-issue verification | reviewer | none | `codex/docs-verify` |
| [#159](https://github.com/stremysid/jarvis/pull/159) | **awaiting-review** | Review single-use, expiring tier-3 confirmation taps | reviewer | first tier-3 hand | `codex/tier3-tap` |
| [#160](https://github.com/stremysid/jarvis/pull/160) | **awaiting-review** | Review the Brightspace access and scoped school-collector design | reviewer | none | `codex/d2l-access-design` |

**Merged on 2026-09-23:** `#152` (`f9472d1`), which runs `check-state.mjs` in CI; `#151` (`12a64b2`), the P2 brief; and `#145` (`ec5ebb5`). #145 merged after its review's three findings were fixed in `18364fb`/`7e69f3f`: the Linux-only CI failures, `serve` lingering on shutdown, and `jarvis node` no longer refusing Windows. The last two were re-checked on the home PC against `ec5ebb5`. **On 2026-09-21/22:** `#133`, `#134` (`ca88bf4`), `#135` (`352991e`), `#137`, `#138`, `#139`, `#140`, `#141` (`e3e3527`), `#143` (`cdfdd4b`),
`#144` (`af5a618`), `#146` (`3a482c4`), `#147` (`bde0a9b`), `#148`, `#149` (`2855105`),
`#150` (`f6bab5b`).

**Closed unmerged:**
- `#117` (2026-09-23): superseded by `#152`, which carried over only its CI wiring.
- `#113` (Sid, 2026-09-23): absorbed. Its closing comment says four of its seven findings are fixed on `main` and the other three are rows here.
- `#118` (Sid, 2026-09-23): it edited only `docs/HANDOFF.md`, which `main` has deleted.
- `#111` (Sid, 2026-09-22): its premise was measured before `testTimeout` was set. Setting it made a timeout a signal, so the `gate.ps1` isolation re-runs have nothing left to explain.
- `#127` (2026-09-21): superseded. It edits `docs/HANDOFF.md`, which `main` has deleted.
- `#136` (2026-09-21): superseded by `#137`, which carries the same fix and a test.
- `#142` (2026-09-21): redundant with a row `#137` added.

## The 2026-09-22 audit of `d0ec419`

A read-only audit ran on 2026-09-22 and produced a ranked list. **Two of its own claims were
withdrawn after challenge, so read its corrections before acting on any of it.** The figures it
reports are from `d0ec419`; `#143` has since landed, which resolved the `DEEPSEEK_MODEL`
question (production runs Flash) and moved the deploy row. The findings below are what survived.

**Only the first two were traced end to end, at both ends of the chain.** The rest are
labeled as they stand, and one has an external premise nobody measured.

| Finding | State | Next action | Owner |
|---|---|---|---|
| **The four-digit PIN is unredacted** | **FIXED and merged in [#149](https://github.com/stremysid/jarvis/pull/149)** (`2855105`), deployed as of `a6a0efd` / Worker `7e027a1f` (owner-observed, see [STATE](STATE.md#production)) | A four-digit run after a credential word is redacted on every channel, tested through `handleTurn`. **The fix this row used to name is withdrawn** (Sid, 2026-09-22): a bare four-digit rule would also redact every year, time and price, including in replies `sanitizeRedaction` cleans, and ungating the DTMF branch would never reach `handleTurn` — it passes field `conversation.turn.text`, which matches no DTMF suffix on either channel. **Still uncaught: a PIN with no credential word before it, and a PIN spoken as words** | builder |
| **`scripts/deploy.ps1` ships an unknown revision** | **FIXED and merged in [#150](https://github.com/stremysid/jarvis/pull/150)** (`f6bab5b`) | `-Publish` now fetches, then refuses unless the checkout is exactly `origin/main` with nothing uncommitted. **Never write "the last five CI runs pass" in this file** — that phrasing was false and this row is where it kept coming back from | builder |
| The digest can say "nothing due" while school has deadlines, because a source is "not set up" only when **unconfigured**, and all three school routes are dead **with configuration present** | audit finding, **not** traced end to end | Record rows-yielded beside `last_success_at` and treat "succeeding, never yielded" as a gap the digest must state | builder |
| Three states are recorded and never delivered: a failed Telegram reply is never retried (`retry_wait` stored, no job claims it); `pushSourceGap` returns `lastFailure` before its age check; one failed send burns that day's only backup notice (empty `catch`, no counterpart `console.error`) | audit findings, **not** traced end to end | Each is a missing caller or a missing log line | builder |
| A spoken 4-digit PIN reaches the model and transcript after Durable Object eviction, because `#interaction` is a plain instance field and hibernation discards it | **unproven** — the load-bearing premise is Cloudflare's hibernation window, which nobody measured | Check the premise against Cloudflare's own documentation before building anything | builder |
| Voice can read the forgetting guarantee but cannot invoke it: `MemoryOwnerControlsService` has four construction sites and none is reachable from `voice/**` | audit finding. Near-vacuous today (5 items, 0 active), so a correctness gap rather than a live leak | Give voice a memory-control invocation path | builder |

**Do not act on the audit's §2.3.** It is withdrawn: `composeCoreProfile`
(`core-profile.ts:81-85`) applies `CORE_PROFILE_PREFIX`, and its only caller feeds that into
`ownerTelegramAgentSystemPrompt`. The label is applied.

## Work with no pull request yet

| Item | State | Next action | Owner | BLOCKS |
|---|---|---|---|---|
| Redaction assignments, phone formats and newline bearer pairs beyond #149 | **awaiting-review** | When the headless harness receives `codex/redaction-gaps`, run its focused and full gates, then request Claude review at the resulting exact head. [Evidence and remaining limits](reviews/2026-09-24-redaction-gaps.md). No migration or rollout performed | harness, then reviewer | Privacy on calls and text |
| D2L receiver compatibility with the real two-board collector | **awaiting-review** | After local gates, open `codex/d2l-receiver-fix` for independent review; then the #170 builder updates its compatibility hold and emits host failures. [Contract and evidence](reviews/2026-09-24-d2l-receiver-fix.md). #169 merged 2026-09-24; this follow-up adds host provenance, Durham/news/quizzes, observed shapes, unknown projections, 404 tools and pairing retry | receiver builder, then reviewer / extension builder | School automatic D2L evidence |
| The operation guard in `findControlTargets` is a hand-kept list | **half fixed** | The guard is now `SUPPORTED_OPERATIONS`, a `satisfies Readonly<Record<MemoryTargetOperation, true>>` map in `src/memory/memory-control-targets.ts`, so a new union member is a compile error. **What remains:** `control-targets.test.ts` still hand-lists the seven operations instead of deriving them from the union, so the test would not catch a map entry missing from the union's own coverage | builder | Phase 2 |
| **PC controls: boot chain, D2L read, daily report** | **P1 merged; old P2 login-and-scrape reader parked; receiver merged, compatibility follow-up in review** | **P2** now uses the approved Opera GX extension on PC and laptop to GET Valence JSON with the existing session. Receiver [#169](https://github.com/stremysid/jarvis/pull/169) merged on 2026-09-24 with separate school-only keys. Real collector [#170](https://github.com/stremysid/jarvis/pull/170) exposed the two-board compatibility gaps addressed by `codex/d2l-receiver-fix`. This supersedes the ordinary-device-key reader in the [parked P2 brief](briefs-p2-d2l-read.md). After both components pass independent review and owner acceptance, **P3** can report the last good whole read and coverage gaps in Telegram. The unrelated PC sync recovery remains its own work item | reviewer / separate extension builder | **Phase 3 and Phase 4** |
| **No Windows launcher for the local agent** | **built; merged in [#145](https://github.com/stremysid/jarvis/pull/145) (`ec5ebb5`); running from the logon task since 2026-09-23** | `jarvis serve` binds `\\.\pipe\jarvis-local-agent` through the existing `NamedPipeServer`, so `ops/jarvis-boot.ps1` resolves a real command and the chain reaches a live agent. `jarvis config` answers what `serve` would refuse on without starting it. Sid set the store paths and installed the logon task on 2026-09-23. Started with `Start-ScheduledTask`, the boot script logged the agent listening and exited, and the agent was still cycling six minutes later, so **it outlives the boot script under Task Scheduler**. **Not yet seen at a real logon** (a reboot). The review's lingering-service finding was fixed before merge (re-checked at `ec5ebb5`). What remains is in the two rows below: its sync fails, and its store folder is locked. Rejected in #145 as too much: a `--pipe-name` flag on `serve`, because `cli._control` sends to `DEFAULT_PIPE_NAME` unless told otherwise, and a second name is a service the CLI cannot find | builder | none |
| **The PC agent's sync fails, because the cloud thinks this device already synced through 267** | **open, cause found** | Observed 2026-09-23, after the logon task was installed. Every cycle ends `sync: request failed`, not `authentication`. This PC's key is accepted: a signed pull from it succeeded. **Cause, from read-only production queries (Sid approved) plus the code:** `sync_ack_receipts` show this device acknowledging every event through sequence **267** on 2026-09-22 between 00:20 and 00:34 UTC, in six snapshots (0→48 … 240→267). That was during #145's testing, with stores that no longer exist. The logon-started agent has a fresh, empty archive, so it pulls from 0 (each of its later cycles creates a root snapshot 0→48 under a second before it fails). It then acknowledges with `expectedCurrent` 0, and `SyncService.acknowledgeDurableReceipt` refuses any `expectedCurrent` that is not the consumer's cursor (`cursor_compare_failed`). Recovery re-pulls and re-acknowledges the same way, so it fails every cycle, forever. **Not verified:** the cursor row itself, because that read was refused by the session's permission check; the value 267 is the last receipt's `current_sequence`. Also unexplained: the agent's first two cycles created no snapshot. **Decided (Sid, 2026-09-23): the builder fix**, not a hand edit of production. Brief: [briefs-sync-recovery.md](briefs-sync-recovery.md) | builder | **P2, Phase 3** |
| **The elevated agent's store folder is unreadable to Sid's normal session** | **open** | Observed 2026-09-23. The logon task runs `jarvis serve` elevated, and it created `%LOCALAPPDATA%\Jarvis\data`. From Sid's normal session, `Test-Path`, `icacls` and `Get-Acl` on that folder are all denied, and `jarvis config` in a normal window now crashes with an uncaught `PermissionError` on `archive.sqlite`. `jarvis status` still works, because it goes through the pipe. Likely mechanism, **not verified**: `_ensure_sqlite_directory` creates the folder with `mkdir(mode=0o700)`, and Python 3.12.4+ on Windows turns that mode into an owner-only ACL, which for an elevated token means the elevated identity. Brief: [briefs-sync-recovery.md](briefs-sync-recovery.md), problem 2 | builder | P2 |
| **Memory has saved nothing since the promotion fix went live** | **open** | Since the 2026-09-20 deploy, 4 owner Telegram turns reached distillation as `eligible` and produced **zero** new memory items; the newest item is from 2026-09-17, and all 5 are still `proposed`. Not yet known whether those 4 turns held anything worth saving. Next: send Jarvis a plain fact, wait for the hourly run, and read the extraction output for that turn | reviewer | Phase 2 |
| **The finder is now reachable from the voice path, and one of its two lookups is still Telegram-only** | **half done** | `D1MemoryControlTargetFinder` (`src/memory/memory-control-targets.ts`) is the extracted finder; `D1ContextRetriever` still implements `ContextRetriever` only, and the voice adapter is passed the finder instead of inheriting it, so the `itemId` tools can resolve a target. **What remains:** `findLastReferencedTarget` — the lookup the agent uses for every control (`query: null`, `turnId`) — needs a `conversation_deliveries` row and a `conversation.assistant_delivered` event, and a voice turn stages nothing, so it returns empty on a call. Over voice, "the memory I just mentioned" therefore resolves only through the item ids in the model's context | builder | Phase 5 |
| Voice has no tool dispatch — a call can talk and cannot act | **done; on `main` through [#147](https://github.com/stremysid/jarvis/pull/147) (`bde0a9b`)** | The voice agent adapter exists: `OwnerVoiceAgentAdapter` (`src/voice/voice-agent.ts`) is a `ModelAdapter` whose `stream` drives `ModelAgentProvider` and runs the tool call, mirroring `OwnerTelegramAgentAdapter`, with the tier gate in front and the receipts spoken. No test asserted which tools need the finder — that was a read, not a measurement | builder | Phase 5 |
| One brain: two composition sites for what should be one assistant | **core extracted, not collapsed** | The loop, the caps, the tier gate, the receipt guard and the nine memory tools now live once in `OwnerAgentCore` (`src/agent/owner-agent-core.ts`), and both channels subclass it. Two composition sites remain, and deliberately: each channel supplies its own authority check, its own reply composition, its own prompt addition and its own tool catalogue. What is *not* done is the full collapse — a Durable Object holding conversation state does not exist, so `CallSession` and the stateless Telegram Worker still reach the brain by different routes | builder | Phase 1 |
| `hermes-runtime` `artifact-security-review3` is a timing flake | **open, unowned** | "uses one absolute cancellation deadline…" failed at 12,975 ms on `352991e`, in code that commit did not touch, and passed on re-run. Cause not yet established | builder | none |
| `local-agent` `test_enqueue_lock_contention_returns_a_definite_failure_without_accepting_work` is load-sensitive | **open, unowned** | Failed in one of two full `uv run pytest -q --ignore=tests/integration` runs on the home PC on 2026-09-24, at `tests/test_quarantine_control.py:681` — `assert not thread.is_alive()` after `thread.join(timeout=0.5)`, so the budget assumes an unloaded machine. It passed on the re-run and passes when its file is run alone (**40 passed, 2 skipped**). The runtime it builds takes its own `EmptyCycleOpener` and touches no permission code, so it is unrelated to PR #157. Cause not established | builder | none |
| **Hermes' SBOM generator refuses Store-installed PowerShell** ([#24](https://github.com/stremysid/jarvis/issues/24)) | **in progress**: a Codex builder started on 2026-09-23, on `codex/hermes-msix`, which was not yet on `origin` when this was written | `apps/hermes-runtime/src/generate-sbom.mjs` trusts only `C:\Program Files\PowerShell\7\pwsh.exe`. The home PC's PowerShell 7.6.6 is the Store/MSIX build, and that folder does not exist there (re-checked 2026-09-23). At `12a64b2` on the home PC, 3 of 18 tests in `sbom-integrity-round2` and `sbom-security-review3` fail: two with `trusted PowerShell host is unavailable`, one with a raw `ENOENT`. CI's `hermes-runtime suite (windows)` job uses the MSI layout, so that job stays green. It passed at `f9472d1`, but that proves nothing about the Store build. No production path uses the generator. It breaks `pnpm test:all` on the only target machine. **What the issue asks for:** trust the MSI and MSIX hosts as explicit absolute paths, never through `PATH`, and establish the MSIX package identity rather than trusting the directory name. A missing host must produce a diagnostic naming PowerShell 7, not an `ENOENT` stack. The inherited-`PATH` shadow test must keep passing. The tests must use the source's trusted-host resolution, and there must be regression tests for an accepted MSIX identity, a rejected impostor identity or path, a missing host, and MSI compatibility. The real PC has no MSI install, so it can verify only the Store/MSIX build, and the issue wants that evidence recorded apart from the runner's. **The fix must also remove the three SBOM entries from `reviewer-tools/gate.ps1`'s `$KnownPreExistingFailures`** (two `sbom-integrity-round2`, one `sbom-security-review3`), or the gate keeps excusing them. The issue says "R3", but the repository has no GitHub milestones, and no carrier names R3 | builder | none |
| **`check-state.mjs` is weaker than its job name** | **in progress**: a Codex builder started on 2026-09-23, on `codex/check-state-harden`, which was not yet on `origin` when this was written | An adjudicated review of [#152](https://github.com/stremysid/jarvis/pull/152) confirmed each of these against the script on 2026-09-23. (1) Malformed `FACTS.md` rows pass silently: 3-cell rows, indented rows, `2026-13-45`, and a source of `TODO` or `-`. (2) Stale warnings are plain `console.warn` with no `::warning`, *Still true?* empty or `no` never warns, and `Last regenerated` dates are never aged. `FACTS.md` claims unset rows are listed. (3) False positives: a backticked pipe in a cell, tables after the register, a CI run id beside `origin/main`, link syntax in code. The STATE budget is also off by one. (4) The link check skips `FACTS.md`, anchors, reference-style and `<spaced>` links, and passes wrong-case links on Windows that Linux CI would fail. The failing mutations are in the adjudicator's harness (scratch, not in the repo) | builder | none |
| `owner-telegram-agent.test.ts` and `telegram-memory.test.ts` redden unrelated CI | **awaiting-review**, [#154](https://github.com/stremysid/jarvis/pull/154), round 1 | **Proven causes:** redaction corrupts six-digit runs inside valid decision ULIDs, breaking production memory confirmation/forget authorization; real D1/R2 delays plus injected sleeps trip the archived test's 450 ms search deadlines independently of its 60 s runner timeout. The callback fix is retained. The archived test now charges 25 virtual ms per D1 trip, waits for real I/O, checks <=500 ms and stage overlap, and retains content/trip/statement/timer checks. The review's serialization mutation survived the old head and is now killed twice. All 5 mutations are killed with confirmation, all 20 complete memory-file loops are green, and the full gateway suite passes 5,114/0/0 (pass/fail/skip). Review the updated head. The earlier claim that `testTimeout` proves an ordering defect is withdrawn | Codex builder | every gate |
| The model cannot state its own certainty | **decided, not started** | Sid ruled 2026-09-20 that certainty is the model's. Remove the assignment at `extraction-policy.ts`'s validation boundary and drop it from `FORBIDDEN_PROPOSAL_KEYS`. **Keep `origin` and lifecycle code-assigned** — those are provenance and enforcement | builder | Phase 2 |
| A spoken-word PIN, a phone number and the owner passphrase match no redaction rule | **live defect** | #96 fixes the digit half only | builder | Phase 5 |
| `explain` / `forget` / `restore` print the memory text in the result that says it was withheld | **live defect** | Pass the string the service already sanitised instead of re-reading the repository | builder | Phase 2 |
| `selectControlTargets` read `memory_item_fts` with no suppression anti-join | **fixed on `main` by `#144`, deployed as of `a6a0efd` / Worker `7e027a1f` (owner-observed, see [STATE](STATE.md#production))** | Both `NOT EXISTS` clauses the FTS arm of `readCandidates` carries, plus the `memory_items` join the first one needs, reach `D1MemoryControlTargetFinder` (`memory-control-targets.ts`), where `#147` moved the arm. `#144`'s merge hand-ported them there, because git could not place them. `#146` then moved the clause text into `suppression-clauses.ts`, and the finder composes it. **How to check the fix is still there:** `apps/cloud-gateway/src/memory/suppression-clauses.ts` must contain `memory_active_event_suppressions` twice and `creation_event_sequence BETWEEN` once, and `memory-control-targets.ts` must compose `${CANDIDATE_SUPPRESSION_CLAUSES}`. Since `#146` the tests pin this as well. In `suppression-predicate-parity.test.ts`, guard 4 fails if the range half leaves the shared text, guard 2 fails if the finder stops composing it, and guard 1 fails if the comparison is written out anywhere but `suppression-clauses.ts`. Fold this row away at the next regeneration | builder | Phase 2 |
| The item-level suppression predicate is still written by hand outside the retriever | **open, unowned** | `suppression-clauses.ts` now holds it once (`CANDIDATE_SUPPRESSION_CLAUSES` / `NOTE_SOURCE_SUPPRESSION_CLAUSES`). The retriever and `memory-control-targets.ts` compose it, guarded by `test/memory/suppression-predicate-parity.test.ts`, but three copies remain: `readItemVisibility` and `retrievalItemStatements` in `memory-repository.ts` hold it as `EXISTS` **projections** (reporting suppression rather than hiding a row), and migration `0016`'s `memory_retrievable_item_versions` view holds it as the filter `readMeaningHits` depends on. Neither is a copy of the composer's text and the view cannot be edited in place, so folding them in is its own change | builder | Phase 2 |
| Tier-3 taps must be single-use and expiring while retaining cross-channel binding | **awaiting-review** | Review `codex/tier3-tap`: atomic consumption after dispatch refusals, ten-minute TTL and migration `0039`. The earlier reservation was a reviewer error; `0040` is assigned to D2L collector keys and `0041` to owner reminders. Recheck main and every open PR before merge. [Design and observed evidence](reviews/2026-09-23-tier3-tap.md); rollout is in OWNER-ACTIONS | reviewer | first tier-3 hand |
| The watchdog declares none of its alerting secrets | **not started** | The heartbeat itself **works as of 2026-09-20** — the redeploy proved it. What remains: `apps/watchdog/wrangler.toml` declares none of `WATCHDOG_TELEGRAM_BOT_TOKEN`, `_CHAT_ID` or `_HEARTBEAT_SECRET`, so a misconfigured watchdog deploys fine and cannot alert anyone. Brief staged at `C:\w\briefs\wd.md` | builder | Phase 7 |
| `channel_identities` has no `BEFORE INSERT` trigger; `capability_tiers` has no update or delete guard | not started | One migration, four triggers. Recheck main and every open PR for the next free number; `0039`–`0041` are assigned to tier-3 taps, D2L collector keys and owner reminders | builder | Phase 2 |
| `tool-gate.ts` returns `verdict: "permit"` as a literal | not started | Deny when the second evaluation is not the outcome the first one was. `verdictFor(confirmed)` is the **wrong** fix | builder | first tier-3 hand |
| `telegram-provider.ts` clears its abort timer before the body read | not started | Keep the timer armed across `response.json()`, as `twilio-provider.ts` does | builder | none |
| `jarvis vault sync` can never see past the first 64 notes | not started | Persist a position; `documents_examined` counts unchanged files | builder | Phase 7 |
| Documentation truth audit | **awaiting-review** | Review the [56-section disposition record](DOCS-VERIFY.md) and corrected root documents; deployment claims are bounded to STATE's recorded source | reviewer | none |
| `typecheck:tests` reports 144 errors in 32 files, gated nowhere | awaiting-triage | Fix or gate them | builder | none |
| Telegram rate limiter and provider circuit breaker are per-isolate | not started | Move both into a Durable Object | builder | none |
| `handleReadiness` has zero call sites | awaiting-triage | Route it, or delete it | builder | none |
| External uptime monitor | blocked | Owner action, after a deployment proves the heartbeat | Sid | Phase 7 |

## How this file stays true

- A pull request appears here from the moment it is opened, and leaves when it is
  merged or closed.
- The reviewer writes the verdict into the row when it posts one, so the queue is
  never more than one review behind reality.
- Anything only Sid can do belongs in [OWNER-ACTIONS.md](OWNER-ACTIONS.md), not here.
- Nothing in this file may state a revision as current. Query it.
- `scripts/check-state.mjs` checks the carriers' format, in CI's `state carriers
  are honest` job (advisory; `main` does not require it).
