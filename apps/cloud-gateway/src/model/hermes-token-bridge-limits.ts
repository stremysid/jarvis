/** Maximum canonical request bytes under the frozen ModelAdapter input caps and JSON escaping rules. */
export const HERMES_TOKEN_BRIDGE_REQUEST_LIMITS = Object.freeze({
  maximumCanonicalBytes: 252_664,
});
