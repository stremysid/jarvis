The complete python-build-standalone license rollup is acquired only from the
official hash-pinned URL in `runtime-artifacts-lock.json`. The runtime fetcher
verifies its exact 105875-byte SHA-256 before retaining it alongside the
installed CPython artifact. This tracked notice records the immutable source
and does not substitute for the verified runtime copy.
